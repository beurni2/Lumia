Proceed with W2-QA-FIX-2 — Final-Composition First-Card Rotation.

Choose Option A from W2-QA-AUDIT-2.

Goal:
Fix cold-start first-card duplication at the actual user-visible ordering layer.

Root cause:
W2-QA-FIX-1 correctly rotated the W2 reserved candidate inside the W2 reservation step, but final user-visible slot 0 is decided later by annotateAndSortByWillingness in hybridIdeator.ts. That final willingness re-sort moves high-willingness pattern_variation candidates, especially "the fridge knows i'm lying", back into slot 0.

Do not patch patternIdeator again for this issue.
Do not hard-ban "the fridge knows i'm lying".
Do not force W2 into slot 0 yet.
Do not add cross-creator shared memory.
Do not change validators.
Do not change prompts.
Do not change production flags.
Do not change ideaSchema.
Do not import another batch.
Do not touch NG/N1/India/Philippines-specific code.
Do not implement deterministic variation.

Implement:
Inside annotateAndSortByWillingness or immediately after it, add a cold-start deterministic top-quality-band rotation for the final first card.

Behavior:
- Applies only when creatorId is available.
- Applies only on cold-start / empty-memory first batch.
- Does not change which 3 ideas ship.
- Only reorders the already-selected final 3 ideas.
- Same creatorId must always produce the same ordering.
- Different creatorIds should spread across eligible top-band candidates.
- If the top-quality band has size 1, keep current order.
- Refresh batches must be unchanged.
- No randomness.

Quality band:
- Use willingnessScore after annotateAndSortByWillingness.
- Candidate must be within K points of the leader.
- Suggested K = 10.
- Rotate only among candidates in that top band.
- Never rotate a clearly weaker card into slot 0.
- Do not under-fill because this is reorder-only.

Expected:
The same three cards may still be selected, but different fresh creators should not all see the same card first.

Tests required:
1. cold-start + same creatorId remains deterministic.
2. cold-start + many creatorIds spread across at least 2 first-card candidates when band size > 1.
3. cold-start + band size 1 keeps original leader.
4. non-empty memory / refresh bypasses rotation.
5. shipping count unchanged.
6. no candidate outside the willingness quality band becomes slot 0.
7. W2 composition unchanged.
8. non-Western cohorts do not break.

Run:
- full api-server tests
- api-server typecheck

Post-fix QA:
Rerun W2-QA sweep:
- Western/clean
- LIVE flag ON dev/staging only
- production flags untouched
- 10 simulated creators
- 10 batches each if feasible
- count=3
- batch 0 regenerate=false
- batches 1+ regenerate=true
- fresh memory per creator
- x-lumina-qa-expose-meta:1

Report:
1. first-card distinct hooks across 10 creators
2. first-card source mix
3. first-card willingnessScore range
4. first-batch 3-card composition
5. whether "the fridge knows i'm lying" still over-dominates slot 0
6. pattern_variation share of idea[0]
7. W2 share and W2-T share
8. batch composition: 2 W2 + 1 old/core, 1 W2 + 2 old/core, 3 W2
9. per-creator entryId/hook/skeleton repeats
10. in-batch duplicate entry/hook/skeleton
11. fallback count
12. latency avg/median/max
13. errors
14. production flags unchanged

Success bars:
- first-card distinct hooks improves materially from 2/10.
- target: at least 6/10, ideally 7+/10, if quality band allows.
- no repeat regression.
- no fallback increase.
- no latency regression.
- no production flag changes.
- no validator/schema changes.
- no output quality degradation.

Stop and report if:
- implementing this requires changing candidate generation.
- implementing this changes which 3 ideas ship instead of only ordering them.
- the top-quality band usually has size 1, making rotation ineffective.
- rotation would put weak cards first.