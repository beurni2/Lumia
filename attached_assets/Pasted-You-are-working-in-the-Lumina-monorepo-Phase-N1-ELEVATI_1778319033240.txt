You are working in the Lumina monorepo.

Phase:
N1-ELEVATION-P1 — Chunk A-R3 final one-entry repair.

Context:
Chunk A-R2 fixed DRAFT-290, but DRAFT-356 is still rejected. Current approved count is 203, rejected count is 14. This is not acceptable. We need to restore approved count to 204 and get all 19 Chunk A entries stamped BI 2026-05-09.

Root cause:
`dm` is dropped by scenarioCoherence tokenizer because length < 3. Hook tokens are `innocent`, `refused`, `stay`. The current whatToShow does not contain any of those tokens, so `show_missing_hook_anchor` fires.

Goal:
Fix only DRAFT-356.

Use Option A only:
- update `.local/REGIONAL_N1_REWRITES.yaml`
- rerun `tsx artifacts/api-server/src/qa/buildApprovedNigerianPack.ts`
- let codegen regenerate `artifacts/api-server/src/lib/nigerianHookPackApproved.ts`

Do not hand-edit approved file.
Do not edit worksheet CSV.
Do not touch source code, tests, validators, schema, selectors, prompts, flags, env, migrations, API shape, or mobile files.
Do not proceed to Chunk B.

Reviewer stamp:
BI 2026-05-09

============================================================
APPLY EXACTLY — DRAFT-356
============================================================

Replace the latest DRAFT-356 overlay with this full overlay:

draftId:
DRAFT-356

newAnchor:
dm

rewrittenHook:
one innocent dm started full investigation.

rewrittenWhatToShow:
A fake dm pops up and your face changes before you can hide it. Someone nearby notices the innocent message, leans in, and you quickly tilt the phone away like the investigation has started.

rewrittenHowToFilm:
Frame your phone and face from the side so the dm notification and your reaction both read. Let the nearby person lean in, then cut on you hiding the screen like the investigation has officially opened.

rewrittenCaption:
dm no dey stay private.

reviewedBy:
BI 2026-05-09

Why this should pass:
- Anchor `dm` appears in hook and whatToShow.
- Hook token `innocent` appears in whatToShow.
- Hook token `investigation` appears in whatToShow.
- `dm` is not relied on for scenarioCoherence because tokenizer drops two-letter tokens.

============================================================
VERIFICATION
============================================================

Run:

1. Codegen:
tsx artifacts/api-server/src/qa/buildApprovedNigerianPack.ts

2. Nigerian pack-specific tests.

3. Full api-server suite.

4. API-server TypeScript:
pnpm --filter @workspace/api-server exec tsc --noEmit

5. Static checks:
- total approved count returns to 204
- rejected count returns to 13 pre-existing only
- BI 2026-05-09 stamp count in approved file is 19
- all 19 Chunk A entries are present in approved file
- DRAFT-356 passes anchor cohesion and scenario coherence
- no old seven howToFilm templates remain in the 19 Chunk A entries
- no user-facing NEPA / nepa / jollof / village people / my enemies in Chunk A entries
- no broken abstract-anchor filming remains

FINAL REPORT:
- files changed
- confirm Option A
- confirm no worksheet edit
- confirm approved file regenerated by codegen
- DRAFT-356 result
- approved/rejected counts
- BI 2026-05-09 stamp count
- full suite result
- TypeScript result
- static check results
- confirm Chunk B was not executed