You are working in the Lumina monorepo.

Phase:
N1-ELEVATION-P1 — Preflight Rewrite Validator Audit for Future Batches.

Context:
Chunk A is now fully accepted:
- 19/19 Chunk A entries present in approved file
- approved count restored to 204
- rejected count restored to the 13 pre-existing rows only
- BI 2026-05-09 stamp count = 19
- full api-server suite green: 1312/1312
- api-server TypeScript clean
- no worksheet edits
- approved file regenerated by codegen only

Before Chunk B, we want to avoid the same rejection cycle.

Chunk A revealed important constraints:
1. `nigerianHookPackApproved.ts` is auto-generated and must not be hand-edited.
2. Correct path is `.local/REGIONAL_N1_REWRITES.yaml` overlay + `qa/buildApprovedNigerianPack.ts` codegen.
3. The overlay system uses Map last-wins by draftId.
4. A later overlay block shadows all prior overlay fields for that draftId.
5. If a field is left empty in the latest overlay, it falls back to the worksheet baseline, NOT to a prior overlay.
6. Codegen validators can reject otherwise-good copy because of anchor cohesion, scenario coherence token overlap, naturalness scoring, stereotype/mocking patterns, field bounds, or reviewer stamp rules.
7. We need a preflight checklist before authoring/applying Chunk B and future chunks.

Goal:
Produce a complete, practical preflight audit of all codegen requirements and validator traps that future Nigerian rewrite batches must satisfy BEFORE copy is handed to the coding agent for application.

This is AUDIT ONLY.
Do not modify source, corpus, worksheet, overlay, tests, validators, schema, prompts, flags, env, migrations, API shape, or mobile files.
You may create `.local/` audit notes/scripts only.

============================================================
FILES / AREAS TO INSPECT
============================================================

Inspect:

- `.local/REGIONAL_N1_REVIEWER_WORKSHEET.csv`
- `.local/REGIONAL_N1_REWRITES.yaml`
- `artifacts/api-server/src/qa/buildApprovedNigerianPack.ts`
- `artifacts/api-server/src/lib/nigerianHookPack.ts`
- `artifacts/api-server/src/lib/nigerianHookPackApproved.ts`
- `artifacts/api-server/src/lib/nigerianHookQuality.ts`
- `artifacts/api-server/src/lib/nigerianPackAuthor.ts`
- `artifacts/api-server/src/lib/scenarioCoherence.ts`
- `artifacts/api-server/src/lib/comedyValidation.ts`
- `artifacts/api-server/src/lib/wtsHtfQualityGate.ts`
- Nigerian pack tests:
  - `nigerianHookPack*.test.ts`
  - `nigerianPack*.test.ts`
  - `nigerianStylePenalty*.test.ts`
  - any approved-pack integrity tests
- `.local/N1_REJECTION_REPORT.md`
- Chunk A / A-R / A-R2 / A-R3 reports if present

============================================================
QUESTIONS TO ANSWER
============================================================

Answer directly:

1. What exact fields can `.local/REGIONAL_N1_REWRITES.yaml` override?
2. For each override field, what happens if it is omitted or empty?
3. Does latest overlay fully replace prior overlay by draftId, or merge field-by-field?
4. What exact reviewer stamp format passes?
5. Which stamp values are rejected?
6. What exact field bounds apply to hook, whatToShow, howToFilm, caption, anchor, etc.?
7. What anchor cohesion rules apply?
   - anchor must appear in hook?
   - anchor must appear in whatToShow?
   - case sensitivity?
   - substring or token?
8. What scenario coherence token-overlap rules apply?
   - token length floor
   - stopwords
   - which hook tokens must appear in whatToShow
   - why `dm` failed as a two-letter token
9. What phrases cause `template_stiffness_phrase`?
10. What phrases cause `show_missing_hook_anchor`?
11. What phrases/patterns cause naturalness penalties or `PIDGIN_MOCKING_PATTERNS` hits?
12. What exact scoring dimensions feed `scoreNigerianPackEntry`?
13. What score floor must be met?
14. What common rewrite patterns are likely to lower the score?
15. What banned/stereotype/cultural-crutch tokens are enforced vs only documented?
16. Which old boilerplate filming phrases should future chunks never use?
17. What user-facing terms are safe in metadata comments but unsafe in hook/whatToShow/howToFilm/caption?
18. How should future overlay blocks be structured so they do not shadow prior overlays accidentally?
19. Should future rewrite batches always restate all 5 user-facing fields plus anchor, even if only one field changed?
20. What exact preflight local script/check can be run on proposed overlay blocks before codegen?

============================================================
DELIVERABLE 1 — HUMAN CHECKLIST
============================================================

Create:

`.local/N1_REWRITE_PREFLIGHT_REQUIREMENTS.md`

Include:

1. Executive summary:
   - why this audit exists
   - what caused Chunk A rejections
   - how to avoid them

2. Source-of-truth pipeline:
   - worksheet → overlay → codegen → validators → approved file
   - explain Map last-wins behavior clearly

3. Overlay authoring rules:
   - required fields to restate
   - exact field names
   - empty-field behavior
   - stamp rule
   - recommended safe overlay shape for every rewrite row

4. Validator requirements:
   - anchor cohesion
   - scenario coherence token overlap
   - field bounds
   - pidgin naturalness / mocking patterns
   - score floor
   - stereotype/mocking rules
   - V2 gate / banned filming phrase rules if relevant

5. Copywriting danger list:
   - words/phrases likely to trigger rejection
   - naturalness traps
   - too-short-token traps like `dm`
   - abstract-anchor traps
   - old boilerplate traps
   - stereotype traps

6. Safe copywriting patterns:
   - how to mention anchors naturally
   - how to create hook/whatToShow overlap
   - how to write simple Nigerian-light-pidgin without triggering mocking patterns
   - how to write filmable howToFilm copy that scores well

7. Future batch workflow:
   - Step 1: extract source-of-truth packet from worksheet + active overlay
   - Step 2: ChatGPT/user writes final copy
   - Step 3: preflight proposed overlay
   - Step 4: codegen
   - Step 5: tests
   - Step 6: runtime QA sample

8. Required verification commands.

9. Hard no-touch rules.

============================================================
DELIVERABLE 2 — MACHINE PREFLIGHT SPEC
============================================================

Create:

`.local/qa-runs/n1_rewrite_preflight_requirements.json`

Include:

{
  "overlayFields": [...],
  "recommendedFullOverlayFields": [...],
  "fieldBounds": {...},
  "reviewerStampRules": {...},
  "anchorRules": {...},
  "scenarioCoherenceRules": {...},
  "scoreFloor": number,
  "scoreDimensions": [...],
  "bannedUserFacingTokens": [...],
  "pidginMockingPatternsSummary": [...],
  "templateStiffnessPhrases": [...],
  "oldBoilerplateHtfPhrases": [...],
  "preflightChecks": [...]
}

============================================================
DELIVERABLE 3 — OPTIONAL PREFLIGHT SCRIPT
============================================================

If practical, create a read-only script:

`.local/scripts/n1RewritePreflightCheck.mjs`

Purpose:
Given a proposed YAML overlay file, run local checks BEFORE codegen.

It should check at least:
- every overlay has reviewedBy
- reviewedBy is not empty / PENDING / AGENT-PROPOSED
- every overlay restates recommended full fields
- anchor appears in hook and whatToShow
- scenario token overlap between hook and whatToShow is likely ≥1
- no banned stereotype tokens in user-facing fields
- no old boilerplate howToFilm phrases
- no broken abstract-anchor filming
- no obvious too-short anchor-only coherence trap
- field lengths within known bounds

Do not modify runtime source.
Do not make this a production script.
It is okay if it is conservative and only audit-side.

============================================================
FINAL RESPONSE
============================================================

Return:

1. Files inspected.
2. Files created under `.local`.
3. Key rules discovered.
4. Biggest rejection traps.
5. Recommended overlay shape for Chunk B onward.
6. Whether a preflight script was created.
7. Confirmation no source/corpus/test/config files changed.

Do not apply Chunk B.
Do not rewrite any entries.