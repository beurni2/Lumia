You are working in the Lumina monorepo.

Phase:
N1-ELEVATION-P1 — Chunk B source-of-truth packet.

Context:
Chunk A is fully accepted:
- 19/19 Chunk A entries present in approved file
- approved count restored to 204
- rejected count restored to 13 pre-existing rows only
- BI 2026-05-09 stamp count = 19
- full api-server suite green: 1312/1312
- api-server TypeScript clean
- no worksheet edits
- approved file regenerated by codegen only

The N1 preflight audit is also accepted.

Critical preflight rules:
- Do NOT use the old approved-file packet as source of truth.
- Extract from worksheet + latest overlay state.
- `.local/REGIONAL_N1_REWRITES.yaml` uses Map last-wins by draftId.
- Future rewrite overlays must restate:
  - draftId
  - newAnchor
  - rewrittenHook
  - rewrittenWhatToShow
  - rewrittenHowToFilm
  - rewrittenCaption
  - reviewedBy
- Missing/empty fields fall back to worksheet baseline, not prior overlay.
- Anchor must appear in hook and whatToShow.
- Scenario coherence needs at least one shared token length ≥3 between hook and whatToShow.
- Short anchors like `dm` do not satisfy scenario token overlap by themselves.
- Avoid stiffness phrases, stereotype tokens, mock pidgin, and old boilerplate filming.

Goal:
Extract the Chunk B source-of-truth packet from the actual codegen pipeline state so the user/ChatGPT can author final rewrite copy safely.

Do not rewrite.
Do not apply overlays.
Do not modify source/corpus/test files.
Do not edit worksheet.
Do not run Chunk B codegen apply.
This is extraction/planning only.

============================================================
SOURCE OF TRUTH
============================================================

Use:

1. `.local/REGIONAL_N1_REVIEWER_WORKSHEET.csv`
2. `.local/REGIONAL_N1_REWRITES.yaml`
3. `.local/qa-runs/n1_elevation_targets.json`
4. `.local/N1_REWRITE_PREFLIGHT_REQUIREMENTS.md`
5. `artifacts/api-server/src/qa/buildApprovedNigerianPack.ts`

For each entry, resolve the effective current value exactly as codegen would:
worksheet row + latest overlay block for that draftId, with Map last-wins behavior.

============================================================
CHUNK B SELECTION
============================================================

Use the `recommendedBatch1` list from `.local/qa-runs/n1_elevation_targets.json`.

Exclude all completed Chunk A entries.

Completed Chunk A draft IDs:
- DRAFT-256
- DRAFT-242
- DRAFT-033
- DRAFT-272
- DRAFT-301
- DRAFT-290
- DRAFT-075
- DRAFT-008
- DRAFT-011
- DRAFT-021
- DRAFT-025
- DRAFT-172
- DRAFT-351
- DRAFT-202
- DRAFT-219
- DRAFT-269
- DRAFT-282
- DRAFT-356
- DRAFT-357

Chunk B should be the next 15 entries from `recommendedBatch1` after excluding Chunk A.

If there is ambiguity because `recommendedBatch1` is ordered by entryId not draftId, resolve by preserving the original order in `recommendedBatch1` and selecting the next 15 not in the completed Chunk A set.

Do not include more than 15 entries.

============================================================
FOR EACH CHUNK B ENTRY, OUTPUT
============================================================

For each selected entry, include:

- entry id
- draft id
- current effective anchor
- current effective hook
- current effective whatToShow
- current effective howToFilm
- current effective caption
- pidginLevel
- domain
- worksheet baseline values
- latest active overlay values, if any
- primary class from target-selection JSON
- secondary tags
- priority
- fields needing rewrite from target-selection JSON
- diagnosis / rewrite direction from target-selection JSON
- runtimeHits, if present
- preflight risk notes:
  - anchor length < 3?
  - anchor not naturally reusable?
  - hook/whatToShow token overlap currently weak?
  - current hook has no pidgin marker?
  - current hook lacks visceral/punch verb?
  - current whatToShow lacks visible turn/payoff?
  - current howToFilm is old boilerplate?
  - stereotype token risk?
  - template stiffness risk?
  - any prior overlay shadow risk?

Also include a recommended safe overlay skeleton for each entry with all keys present but values left as placeholders, like:

- draftId: DRAFT-XXX
  newAnchor: "<must be single token, appears in hook + whatToShow>"
  rewrittenHook: "<full final hook>"
  rewrittenWhatToShow: "<full final whatToShow>"
  rewrittenHowToFilm: "<full final howToFilm>"
  rewrittenCaption: "<full final caption>"
  reviewedBy: "BI 2026-05-09"

Do NOT fill replacement copy. Placeholders only.

============================================================
GROUPING
============================================================

Group Chunk B entries by rewrite class:

1. Hook + scenario + HTF
2. Scenario + HTF
3. HTF-only
4. Any stereotype/abstract-anchor leftovers if they appear unexpectedly

Within each group, keep the order from recommendedBatch1.

============================================================
OUTPUT FILES
============================================================

Create:

`.local/N1_CHUNK_B_SOURCE_PACKET.md`

and

`.local/qa-runs/n1_chunk_b_source_packet.json`

The JSON should include:

{
  "generatedAt": "...",
  "selectionRule": "next 15 from recommendedBatch1 excluding completed Chunk A",
  "completedChunkADraftIds": ["..."],
  "entries": [
    {
      "entryId": "...",
      "draftId": "...",
      "currentEffective": {
        "anchor": "...",
        "hook": "...",
        "whatToShow": "...",
        "howToFilm": "...",
        "caption": "..."
      },
      "worksheet": {
        "anchor": "...",
        "hook": "...",
        "whatToShow": "...",
        "howToFilm": "...",
        "caption": "..."
      },
      "latestOverlay": {
        "present": true,
        "newAnchor": "...",
        "rewrittenHook": "...",
        "rewrittenWhatToShow": "...",
        "rewrittenHowToFilm": "...",
        "rewrittenCaption": "...",
        "reviewedBy": "..."
      },
      "targetSelection": {
        "primaryClass": "...",
        "secondaryTags": ["..."],
        "priority": "...",
        "fieldsNeedingRewrite": ["..."]
      },
      "diagnosis": "...",
      "preflightRisks": ["..."]
    }
  ]
}

============================================================
VALIDATION DURING EXTRACTION
============================================================

Run read-only preflight analysis on the current effective values for the selected Chunk B entries:

- anchor in hook
- anchor in whatToShow
- hook/whatToShow token overlap
- field bounds
- old boilerplate howToFilm
- banned stereotype tokens
- mock pidgin patterns
- scenario stiffness phrase risks

This is diagnostic only. Do not fix.

============================================================
FINAL REPORT
============================================================

Return:

1. Files inspected.
2. Files created under `.local`.
3. Exact Chunk B entries selected:
   - draft id
   - entry id
   - primary class
   - fields needing rewrite
4. Counts by rewrite class.
5. Biggest preflight risks for Chunk B.
6. Confirmation no source/corpus/test/config files changed.
7. Confirmation no overlays were applied and no codegen apply was run for Chunk B.

Do not proceed to rewrites.
Do not proceed to apply.