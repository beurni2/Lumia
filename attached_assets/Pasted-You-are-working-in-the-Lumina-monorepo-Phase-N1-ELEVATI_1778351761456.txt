You are working in the Lumina monorepo.

Phase:
N1-ELEVATION-P1 — Single-entry airtime HTF cleanup.

Context:
Hot-6 runtime recheck is accepted with one tiny follow-up.

Hot-6 itself passed:
- 6/6 Hot-6 entries appeared at runtime
- 25 Hot-6 deliveries
- 0 banned-filming hits from Hot-6
- 0 stereotype hits
- 0 mock-pidgin hits
- 0 scenario-coherence failures
- 0 structural failures
- TypeScript clean
- no source/corpus/overlay/worksheet edits during QA
- regional-decoration issue untouched

Only remaining pack issue found:
- entryId: ng_82b4bff9
- hook: airtime finished, but somehow data is also judging me
- issue: howToFilm ends with `Keep the airtime centered.`
- runtime impact: 5 banned-filming hits in 60 ideas, all from this one entry

Goal:
Apply a single-entry HTF-only overlay that removes the trailing `Keep the airtime centered.` sentence.

Use Option A only:
- save proposed overlay first
- preflight it
- append to `.local/REGIONAL_N1_REWRITES.yaml`
- run `tsx artifacts/api-server/src/qa/buildApprovedNigerianPack.ts`
- let codegen regenerate `artifacts/api-server/src/lib/nigerianHookPackApproved.ts`

Do not hand-edit the generated approved file.
Do not edit the worksheet CSV.
Do not touch source code, tests, validators, schema, selectors, prompts, flags, env, migrations, API shape, or mobile files.
Do not touch regional-decoration / ng_clean / ng_null issue in this phase.

Critical:
Map last-wins means the overlay must restate ALL fields:
- draftId
- newAnchor
- rewrittenHook
- rewrittenWhatToShow
- rewrittenHowToFilm
- rewrittenCaption
- reviewedBy

Reviewer stamp:
BI 2026-05-09

============================================================
FIND SOURCE-OF-TRUTH FIRST
============================================================

Before applying, map `ng_82b4bff9` to its draftId from the regenerated approved pack and confirm current effective values.

Expected current values from runtime QA:
- entryId: ng_82b4bff9
- hook: airtime finished, but somehow data is also judging me
- whatToShow: Open a fake recharge screen while the network spinner keeps loading. You tap once, wait, tap again, then stare at the phone like airtime and data are having a meeting without you.
- howToFilm: Frame the recharge screen and your tapping hand from over the shoulder. Let the loading spinner stay visible, then end on your face looking at the phone like even the network has joined the problem. Keep the airtime centered.
- caption: connection without cooperation.

If the draftId or current text differs, stop and report before applying.

============================================================
APPLY EXACTLY
============================================================

Use the actual draftId found for `ng_82b4bff9`.

Overlay copy:

- draftId: <ACTUAL_DRAFT_ID_FOR_ng_82b4bff9>
  newAnchor: airtime
  rewrittenHook: "airtime finished, but somehow data is also judging me"
  rewrittenWhatToShow: "Open a fake recharge screen while the network spinner keeps loading. You tap once, wait, tap again, then stare at the phone like airtime and data are having a meeting without you."
  rewrittenHowToFilm: "Frame the recharge screen and your tapping hand from over the shoulder. Let the loading spinner stay visible, then end on your face looking at the phone like even the network has joined the problem."
  rewrittenCaption: "connection without cooperation."
  reviewedBy: "BI 2026-05-09"

============================================================
PREFLIGHT BEFORE APPLY
============================================================

Before appending to `.local/REGIONAL_N1_REWRITES.yaml`, save proposed overlay as:

`.local/qa-runs/n1_airtime_single_fix_proposed_overlay.yaml`

Run the preflight script/check if available:

`node .local/scripts/n1RewritePreflightCheck.mjs .local/qa-runs/n1_airtime_single_fix_proposed_overlay.yaml`

If the preflight script flags a real hard failure, stop and report before appending.

============================================================
APPLY + CODEGEN
============================================================

If preflight passes or only advisory warnings remain:

1. Append the exact overlay block to `.local/REGIONAL_N1_REWRITES.yaml`.

2. Run:
`tsx artifacts/api-server/src/qa/buildApprovedNigerianPack.ts`

3. Confirm:
- total approved count remains 204
- rejected count remains 13 pre-existing only
- no new rejected-from-rewrites rows
- `ng_82b4bff9` remains present in approved file
- `ng_82b4bff9` no longer contains `Keep the airtime centered`
- no `Keep the \w+ centered` hit remains in this entry

============================================================
POST-APPLY VERIFICATION
============================================================

Run:

1. API-server TypeScript:
`pnpm --filter @workspace/api-server exec tsc --noEmit`

2. Nigerian pack-specific tests if codegen/static checks show any risk.

3. Static checks on `ng_82b4bff9`:
- no `Keep the airtime centered`
- no `Keep the \w+ centered`
- no old boilerplate filming phrase
- anchor appears in hook and whatToShow
- scenario coherence still passes
- no stereotype tokens
- no mock-pidgin hit

Full api-server suite is not required unless codegen/static checks show risk.

============================================================
FINAL REPORT — KEEP IT SHORT
============================================================

Return only:

1. Verdict.
2. Actual draftId for `ng_82b4bff9`.
3. Preflight result.
4. Codegen result.
5. Approved/rejected counts.
6. TypeScript result.
7. Static check result for `ng_82b4bff9`.
8. Files changed.
9. Confirm:
   - no worksheet edits
   - generated approved file regenerated by codegen only
   - no source/test/validator/scorer/selector/prompt/flag/API/mobile changes
   - regional-decoration issue not touched

Do not include long tables unless something fails.
Do not ask for next steps.