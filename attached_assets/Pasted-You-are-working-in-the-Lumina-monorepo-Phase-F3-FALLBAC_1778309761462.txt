You are working in the Lumina monorepo.

Phase:
F3-FALLBACK ROOT-CAUSE AUDIT.

Context:
The F3 LLM fallback stress sweep returned ACCEPT WITH FOLLOW-UP, but we are NOT approving a timeout bump or any implementation yet.

The user explicitly does not want gambling, circling around, or worthless builds. If the issue is not fully understood, audit first.

Known F3 findings:
- Forced fallback stress sweep used count=10, regenerate/excludeHooks pressure.
- 12/12 forced batches engaged fallback via guards_failed.
- Claude fallback timed out repeatedly:
  `hybrid_ideator.fallback_timeout: Claude generateIdeas exceeded 45000ms`
- During the sweep window, there were 38 fallback timeout events.
- Final served output contained 0 `source="claude_fallback"` ideas out of 120.
- Local best-effort still returned full batches: 0 under-filled batches.
- Fallback added roughly 50 seconds latency and contributed 0 final ideas.
- No source changes were made during F3.

Goal:
Perform a full root-cause audit of the LLM fallback path and recommend the smallest correct fix. Do not implement anything.

This is AUDIT ONLY.

============================================================
STRICT RULES
============================================================

Do NOT modify source code.
Do NOT modify prompts.
Do NOT modify tests.
Do NOT modify configs.
Do NOT change env flags.
Do NOT change timeouts.
Do NOT change API shape.
Do NOT change corpus.
Do NOT run migrations.
Do NOT apply fixes.

You may create local audit notes and read-only scripts under `.local/` if needed.

============================================================
PRIMARY QUESTIONS
============================================================

Answer these directly:

1. Is the 45s timeout the true root cause, or only a symptom?
2. Where exactly is the fallback timeout defined?
3. When does the 45s fallback timer start relative to the whole request?
4. How much request budget has already been spent before fallback begins?
5. Does Claude receive an unusually large prompt or candidate request during fallback?
6. Is `count=10` forcing an unrealistic fallback scenario, or is it a valid production-risk scenario?
7. Are Claude candidates ever returned but later discarded by merge/select/gates?
8. Are candidates blocked by schema validation, anti-copy checks, novelty checks, safety checks, or selection scoring?
9. Is the fallback prompt asking Claude for too many ideas at once?
10. Is fallback supposed to produce the whole missing batch or only patch gaps?
11. Would increasing timeout to 55s actually solve the issue, or just make requests slower?
12. Would reducing fallback candidate count be safer than increasing timeout?
13. Would exposing QA-only raw fallback candidates help, or is the core issue earlier?
14. Is fallback currently useful enough for beta, or should it be disabled/rarely used until redesigned?
15. What is the smallest safe fix that improves reliability without creating slow, expensive, or low-quality output?

============================================================
FILES / AREAS TO INSPECT
============================================================

Inspect the full fallback path, especially:

- `artifacts/api-server/src/lib/hybridIdeator.ts`
- `artifacts/api-server/src/lib/ideaGen.ts`
- `artifacts/api-server/src/lib/ai.ts`
- fallback decision logic
- timeout wrapper around `generateIdeas`
- merge/select logic after fallback returns
- qaTelemetry fallback fields
- schema validation for fallback ideas
- anti-copy / novelty / safety gates that may reject fallback candidates
- request route timeout/client timeout behavior
- any comments/docs in `replit.md` or local reports about P4 timeout intent
- server logs from the F3 sweep if available
- `.local/F3_LLM_FALLBACK_STRESS_REPORT.md`
- `.local/qa-runs/f3_llm_fallback_stress_qa.json`
- `.local/qa-runs/f3_llm_fallback_stress_summary.json` if present

============================================================
LOG / TELEMETRY AUDIT
============================================================

Use existing logs and QA artifacts to reconstruct the fallback timeline.

For at least several representative forced fallback batches, determine:

1. total request duration
2. time spent before fallback decision
3. time spent inside Claude fallback
4. timeout moment
5. whether any partial/raw candidates existed
6. whether local best-effort filled before/after fallback failure
7. final source distribution

If exact timestamps are not available, say so clearly and recommend the smallest telemetry improvement needed.

============================================================
STATIC CODE AUDIT
============================================================

Map the fallback pipeline step-by-step:

1. request enters ideator route
2. local candidates are generated
3. guards/selectors run
4. fallback decision is made
5. Claude/OpenRouter call is invoked
6. timeout wrapper applies
7. returned candidates, if any, are parsed/validated
8. candidates merge with local pool
9. final ideas selected
10. qaTelemetry is emitted

For each step, identify:
- file/function
- key inputs
- key outputs
- failure/drop conditions
- logging/telemetry available
- missing observability

============================================================
CANDIDATE FIX OPTIONS TO EVALUATE
============================================================

Evaluate at least these options, but do not implement:

A. Increase fallback timeout from 45s to 55s.
- Pros
- Cons
- whether it likely solves the issue
- risk to user latency
- risk to client timeout

B. Reduce fallback request size.
Example: ask Claude for only the number of missing ideas, capped at 1–3, instead of trying to satisfy count=10 pressure.
- Pros
- Cons
- likely latency impact
- likely quality impact

C. Start fallback earlier / parallelize with local pipeline.
- Pros
- Cons
- cost risk
- complexity risk
- whether this violates current zero-cost/local-first philosophy

D. QA-only raw fallback telemetry.
Expose raw fallback candidates behind `x-lumina-qa-expose-meta: 1`.
- Pros
- Cons
- whether it helps grade quality
- whether it fixes production reliability

E. Disable fallback for beta / rely on local best-effort.
- Pros
- Cons
- quality risk
- cost/latency benefit
- when to re-enable

F. Keep fallback but make it stricter/rarer.
Only call fallback when local count < 3, not when guards fail.
- Pros
- Cons
- effect on latency
- effect on quality

G. Replace fallback with deterministic expansion for now.
- Pros
- Cons
- engineering scope
- whether current W2-AUTHOR work already makes this viable

============================================================
WHAT NOT TO DO
============================================================

Do not recommend a broad rebuild unless the evidence demands it.

Do not recommend:
- new model provider switch
- fine-tuning
- RAG
- new database tables
- prompt rewrite pass
- changing public API
- loosening validators
- removing safety gates
- increasing Claude usage broadly
- speculative architecture work

The recommendation must be minimal, evidence-led, and beta-practical.

============================================================
REPORT REQUIRED
============================================================

Create:

`.local/F3_FALLBACK_ROOT_CAUSE_AUDIT.md`

Report structure:

1. Executive verdict:
   - ROOT CAUSE IDENTIFIED
   - ROOT CAUSE PARTIAL
   - ROOT CAUSE NOT PROVEN

2. One-paragraph plain-English explanation of what is happening.

3. Fallback pipeline map:
   - step
   - file/function
   - what happens
   - where it can fail/drop

4. Evidence from F3 logs/artifacts:
   - timeout counts
   - request durations
   - candidate counts
   - final source counts
   - any missing telemetry

5. Root cause analysis:
   - timeout vs prompt size vs requested count vs late fallback vs candidate rejection
   - state clearly which are proven and which are hypotheses

6. Option comparison table:
   - A through G above
   - benefit
   - risk
   - implementation scope
   - expected effect on latency
   - expected effect on quality
   - recommendation status

7. Recommended next action:
   - exactly one primary recommendation
   - optionally one secondary QA-only observability recommendation
   - exact files likely to touch if implemented later
   - exact tests/QA needed
   - why this is not circular/worthless work

8. What NOT to do and why.

9. Final answer:
   - Is fallback safe for beta as currently implemented?
   - Should we fix it now, disable it, or defer it?
   - What is the smallest safe next build, if any?

10. Hard no-touch confirmation:
   - no source files modified
   - no prompts/configs/tests changed
   - no flags/env/API/schema/migrations changed

Remember:
The purpose is to avoid guessing. Diagnose before building.