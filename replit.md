# Overview

Lumina is a pnpm monorepo project designed as a creator tool to enhance daily consistency for English-speaking micro-creators (1K–50K followers). It streamlines the content creation loop from onboarding and style profiling to idea generation, templated content creation, side-by-side review, and export. The Phase 1 MVP focuses on a rule-based Style Profile, a cloud-based Ideator powered by Claude Haiku 4.5 for generating video ideas with strict constraints, templated content creation, and a side-by-side review process. Lumina supports micro-creators in the US, UK, CA, AU, IN, PH, and NG markets, aiming to validate this core loop.

# User Preferences

I prefer to develop iteratively and see changes frequently. Please ask before making any major architectural changes or introducing new dependencies. I value clear, concise explanations and prefer a functional programming style where appropriate. Do not make changes to files or folders marked as `ARCHIVED` or related to `MONETIZATION` or `POSTING` systems unless explicitly instructed and the associated feature flag is enabled.

# System Architecture

Lumina is a pnpm workspace monorepo built with TypeScript.

**UI/UX Decisions:**
- The mobile application is developed using Expo (React Native).
- The onboarding process is a 3-step flow: region selection, first video import, and two additional imports.
- The post-export review screen reads as a finish line: a single final-video preview, a 3-item confidence strip, an optional 2-action Quick Boost (Smoother flow / Faster hook with Fix → Done ✓ micro-interaction), a friction-free "Made with Lumina" watermark toggle, a primary "Save & Post" CTA, and low-emphasis "Make another version" / "Back to ideas" secondary actions. No before/after comparison, analysis blocks, or technical warnings.
- The post-export flow extends Save & Post into a complete posting loop: a "Just save" secondary path (quieter "Saved ✓ / Post it later" copy with no auto-handoff); a "Save & Post" success block ("Saved ✓ / Ready to post / Takes ~10 seconds") that auto-opens a bottom-sheet platform picker (TikTok / Instagram / Snapchat / Copy only); an inline platform handoff card ("Almost there!" + 2-step guide + caption box + Copy caption / Copy & open {Platform} buttons); real deep links via `expo-linking` with web fallbacks (TikTok `snssdk1233://`, Instagram `instagram://library`, Snapchat `snapchat://`); clipboard writes via `expo-clipboard` on every platform tap; and a return-loop "Posted? / How did it go?" card that surfaces via `AppState`/`useFocusEffect` once the user comes back from a platform deep-link, replacing the two text buttons with Make another version / Back to ideas / View saved videos.
- A 5-question screen biases format distribution, tone, effort, privacy, and hook style based on user input.
- One-tap export to the gallery with an optional "Made with Lumina" watermark.

**Technical Implementations:**
- **Monorepo Structure:** Managed with pnpm workspaces.
- **Node.js:** Version 24.
- **API Framework:** Express 5 interacts with PostgreSQL via Drizzle ORM.
- **Database Conventions:** Additive migrations, versioned registry, immutable primary-key types.
- **LLM Integration:** Claude Haiku 4.5 via Replit AI Integrations for idea generation.
- **Per-Creator Format Distribution:** Rule-based system derives a target mix of four canonical patterns.
- **Idea Core Axis (Phase 1) [FROZEN — do not modify]:** A 120-value `ideaCoreType` axis (12 IdeaCoreFamilies × 10 types) drives selection scoring, within-batch HARD guards (≤2 per family / ≤1 per type / failure_contradiction <40%), and cross-batch regen rescue (≥2 NEW families). The legacy `scriptType` axis remains resolved + persisted as inert telemetry only — it no longer influences scoring or guards.
- **Idea Core Axis (Phase 2 — catalog expansion) [FROZEN — do not modify]:** The scenario pool grew from 25 → 83 entries (additive only — Phase 1 wiring untouched) so the 120-value `ideaCoreType` axis has a family-balanced raw pool to draw from. Per-family default-type targets: 7 each for emotional_loop / failure_contradiction / decision_paralysis / social_friction / physical_betrayal / environmental_chaos / memory_glitch / time_distortion / ritual_disruption / identity_drift / information_asymmetry, 6 for anti_climax. 6 new `SceneObjectTag`s added (`restaurant_table`, `gift_box`, `nightstand`, `weighing_scale`, `umbrella`, `wifi_router`) with `ENV_CLUSTER_BY_TAG` mappings. All 5 side-tables (`VISUAL_ACTION_BY_FAMILY`, `TOPIC_LANE_BY_FAMILY`, `SCRIPT_TYPE_BY_FAMILY`, `IDEA_CORE_TYPE_BY_FAMILY`, `SCENE_OBJECT_TAG_BY_FAMILY`) wired for every new family — no fallback path fires for any new entry. ZERO schema/migration changes (pure TS data). `IDEA_CORE_TYPE_OVERRIDES` intentionally NOT extended (sparse-override pattern preserved; defaults are sufficient). QA: 16-candidate pool now spans 8-10 distinct ideaCoreFamilies per batch (was 5-6 in Phase 1); 3 sequential batches produce 9 distinct families across the run with failure_contradiction staying under 40% in every batch.
- **Hook Template / Scroll-Stopping Upgrade (Phase 3, additive overlay):** Layered on top of the now-frozen Phase 1 `ideaCoreType` axis + Phase 2 83-scenario pool to fix the "hooks read like AI rather than scroll-stopping mid-action thoughts" failure mode. Spec source: `attached_assets/Pasted-HOOK-TEMPLATE-SYSTEM-SCROLL-STOPPING-UPGRADE-Goal-Fix-w_1777529547655.txt`. ZERO schema changes (pure TS data + a new derived score axis on the existing `IdeaScore`). **Catalog growth (76 → 102 entries across the same 12 `HookLanguageStyle`s):** the spec's 9 conceptual categories map onto the existing 12 styles (FRAGMENT → `anti_hook`, DEADPAN/BLUNT → `matter_of_fact`, EMOTIONAL_SPIKE → `confession`, SELF_AWARE/META → `observation`; rest 1:1 — no new style enum values added so the cross-batch novelty axis stays unchanged). 26 new entries are scenario-agnostic (`build: () => "..."`) so they read as raw mid-action thoughts (e.g. `"again. seriously."`, `"still nothing."`, `"9:14pm. still here."`, `"i opened it. then closed it."`) instead of frame-around-a-noun phrasings. `validateHook`'s word-count floor was lowered 3 → 2 specifically to admit these FRAGMENT-style hooks; the other rejection rails (banned prefix / generic filler / voice violation / dangling word / truncation marker / interpolation leak) all still apply, so a 2-word hook only ships if it's intentionally abrupt. **Per-template rigidity + sharpness:** `LanguagePhrasingEntry` extended with two OPTIONAL fields — `rigidityScore?: 1-5` (lower = less reusable across scenarios = better) and `sharpnessScore?: 1-5` (higher = more emotional impact). All 76 pre-existing entries backfilled with hand-audited values; the 26 new entries set both explicitly. `getEntryScores(entry)` helper resolves both fields with default 3/3 when absent — used by both the picker + the scorer so JSONB-cached entries written before Phase 3 still resolve cleanly. **scrollStopScore axis:** new `scrollStopScore: number` (0-10) field on `IdeaScore`, computed by `scoreScrollStop(hook, sourceEntry?)` from intrinsic hook properties (fragment shape, emotional charge, structure break, specificity) PLUS the source `LanguagePhrasingEntry`'s rigidity/sharpness when available. Composition: baseline 0, +3 fragment (≤4 words OR period mid-string), +2 emotional charge (token match OR entry sharpness ≥4), +2 unusual structure (no leading pronoun OR timestamp-leading), +2 not a full sentence, +1 highly specific (digit OR proper noun); −3 entry rigidity ≥4, −2 safe-phrasing prefix (`"i think"` / `"i feel like"` / `"you know when"` / `"kind of"`), −2 over-explained (>12 words), −3 pure abstraction; final `Math.max(0, Math.min(10, raw + 5))`. Folded into `IdeaScore.total` with low weight (`Math.round(scrollStopScore * 0.5)`) — nudges selection without overriding the pre-existing 6-axis scorer that shapes selection today. **PatternMeta wiring:** new optional `sourceLanguagePhrasing?: LanguagePhrasingEntry` field on `PatternMeta`, populated by `assembleCandidate` from the entry the picker returned alongside `index + hook`. RUNTIME-ONLY — the entry's `build` function does NOT survive JSON serialization to the JSONB cache, but `rigidityScore` / `sharpnessScore` / `voiceProfiles` (the JSON-safe subset) DO survive, so `getEntryScores` still resolves correctly on rehydrated entries; readers MUST stick to score-field lookups via `getEntryScores` and never call `.build()`. **Banned-prefix layer (PART 4):** the spec's hard-rejection requirement was already satisfied pre-Phase-3 by `BANNED_HOOK_PREFIXES` + the existing `validateHook` wiring (`the way i` / `why did i` / `why do i` / `i really thought` / `me saying` / `me, ` / `what i said vs what i did`); Phase 3 only verified the upgraded 102-entry pool produces zero banned-prefix hits in shipped batches. **QA (3-batch driver):** 102 entries total ✓ (need ≥100), zero banned-prefix hits across 48 shipped hooks ✓, scrollStopScore mean=9.25 across all candidates ✓ (need ≥5), mean=9.89 across top-3 picked ✓ (need ≥6), 12 distinct `HookLanguageStyle`s across 3 batches ✓ (need ≥4). Spec FRAGMENTs probe `"again. seriously."` / `"still nothing."` / `"immediately no."` / `"9:14pm. still here."` all score 9-10; `"I think this might be a problem someday"` correctly scores 5 (safe-prefix penalty fires).
- **HookIntent Refactor (Phase 4, additive overlay):** Layered on top of the now-frozen Phase 3 102-entry catalog + per-template rigidity/sharpness wiring to introduce a `HookIntent` controller axis (`scroll_stop` / `compulsion` / `relatable`) that sits ABOVE `HookLanguageStyle` and drives both template selection and per-intent scoring. Spec source: `attached_assets/Pasted-REFACTOR-HOOK-SYSTEM-INTRODUCE-HookIntent-Goal-Cleanly-_1777532632693.txt`. ZERO schema/migration changes (pure TS data + new derived score axis on the existing `IdeaScore` + 1 new optional field on `PatternMeta`). **Catalog tagging:** all 102 Phase 3 entries hand-tagged with `hookIntent: HookIntent` (REQUIRED on `LanguagePhrasingEntry`); legacy 15-entry `HOOK_PHRASINGS_BY_STYLE` catalog (used only by `tryRewrite`) tagged with OPTIONAL `hookIntent?: HookIntent` defaulting to `scroll_stop` via `getEntryIntent` helper. Distribution across the 102-entry pool: scroll_stop=46, compulsion=23, relatable=33 — chosen so each language style supports ≥1 intent without forcing artificial balance. **Generation flow (slot rotation):** `generatePatternCandidates` Cartesian inner loop assigns `assignedIntent = HOOK_INTENTS[slotIndex % 3]` per slot; `assembleCandidate` accepts the assigned intent + threads it to `pickValidatedLanguagePhrasing`, which runs an INTENT-FIRST 6-pass discipline (3 passes filtered to entries matching the assigned intent, then 3 fallback passes over ANY-intent entries when intent-matching pool is exhausted; sets `meta.intentFallback = true` on fallback). `meta.hookIntent` is derived from the entry the picker actually returned (NOT `assignedIntent`) so fallbacks surface the real shipped intent. **Per-intent scoring:** new `scoreCompulsion(hook, source?)` (mystery-end words / forward-implication / demonstrative+verb / anti-resolution; baseline +5) and `scoreRelatable(hook, source?)` (first-person opener / specific behavior verb / admission tokens / lowercase-conversational / anti-abstract; baseline +5) added alongside the existing `scoreScrollStop`; `scoreHookIntent(hook, intent, source?)` dispatches by intent. New `hookIntentScore: number` (0-10) field on `IdeaScore` computed via the dispatcher and folded into `total` at `Math.round(hookIntentScore * 0.5)` REPLACING the prior `scrollStopScore * 0.5` fold so scoring stays intent-correct without double-counting. The `scrollStopScore` field STAYS on `IdeaScore` for telemetry (always computed via `scoreScrollStop` regardless of intent) but no longer contributes to `total`. **Batch guards:** within `selectionPenalty` — `-3` per duplicated `hookIntent` in the in-progress pick set (sized between the language-style/voice -2 and archetype -4 levers, matching HookIntent's framing as the controller axis ABOVE language style); `+5` fresh-intent BONUS when batchSoFar.length>0 AND this candidate's intent is not yet in the batch (captures the spec's "Prefer 1 of each" SOFT preference and creates an 8pt swing that overcomes both the typical 2-3pt baseline gap between intent scorers AND the 2pt lower novelty bonus that compulsion candidates accrue from a narrower archetype/lang-style pool); `-100` HARD all-3-same-intent guard fires only when batchSoFar.length===2 AND both prior picks share THIS candidate's intent (overwhelms any plausible base total so the selector ALWAYS prefers a shippable other-intent candidate over a third clone). Intentless candidates (legacy cache reads / Llama / Claude wraps) silently skip both branches. **PatternMeta wiring:** new optional `hookIntent?: HookIntent` + `intentFallback?: boolean` fields on `PatternMeta`, populated by `assembleCandidate` from the entry the picker returned. JSON-safe — both fields survive JSONB cache round-trips. **QA (5-batch driver):** all 4 gates green — pool balanced (each intent in [3,9] per 16-candidate batch), shipped distribution 6/4/5 across 5 batches × 3 picks (all intents ≥3, target met), zero all-same-intent batches (HARD guard fires correctly), per-intent shipped `hookIntentScore` mean = 8.83 / 7.00 / 9.20 (all ≥6 floor). Telemetry: ~50% intent-fallback rate at the picker level reflects the catalog's uneven (style × intent) coverage — acceptable because pool composition still ends up balanced and downstream selection still ships all 3 intents. **Architect-review fixes (post-T005):** two correctness gaps surfaced by code review and patched: (1) the `-100` "hard" all-3-same intent penalty in `selectionPenalty` is a SOFT lever (it only blocks the greedy selector when an other-intent shippable EXISTS in the pool); a true hard guard was missing for the pool-starvation case where all candidates collapse to 1 effective intent. Added guard (h) to `batchGuardsPass` in `hybridIdeator.ts` — for batches of size ≥3, reject when ALL picks contributed an intent AND `new Set(intents).size === 1` (skips legacy/fallback wraps that don't carry `hookIntent` to avoid synthetic rejections). Defense-in-depth so the spec's "no batch of 3 ships with all 3 sharing the same intent" rule holds even under starvation. (2) `tryRewrite` in `ideaScorer.ts` rewrites a hook from a DIFFERENT legacy `HOOK_PHRASINGS_BY_STYLE` entry but did not update `meta.hookIntent` — rewritten candidates carried STALE intent metadata, causing `scoreHookIntent` to dispatch to the wrong per-intent scorer and the soft/hard intent batch guards to make decisions on a phantom intent. Patched: re-derive via `getEntryIntent(entry)` (defaults to `scroll_stop` for legacy entries without the field, matching first-pick discipline at `assembleCandidate` line 4948 of `patternIdeator.ts`); also clear `intentFallback` since rewrite is its own path and the original picker's fallback telemetry no longer applies. Both fixes preserve the typecheck baseline (8 pre-existing errors, zero new). Other architect findings surfaced as known trade-offs: style-first weave (HookLanguageStyle chosen before intent filter) is acceptable as additive Phase 4 compromise — full spec flow `ideaCoreType → HookIntent → compatible HookLanguageStyle` would require restructuring the Phase 1/2/3 weave (frozen this phase); +5/-3/-100 selector calibration is strong relative to other axis levers (archetype -4, scenarioFamily -3) but is now backed by a real hard guard rather than relied on as a pseudo-hard constraint; per-intent scorers (`scoreCompulsion`, `scoreRelatable`) are directionally coherent but permissive (no curly-apostrophe normalization for `should've`/`didn't`, generic first-person/lowercase admits over-score) — calibration tightening deferred to a follow-up so the structural overlay lands first.
- **Pattern Mapping Layer (Phase 5, additive overlay):** Layered on top of the now-frozen Phase 4 `HookIntent` controller axis to introduce a typed `VideoPattern` axis (12 values: `silent_reaction` / `escalation` / `micro_story` / `before_after` / `pov_internal` / `loop_behavior` / `object_pov` / `delayed_reaction` / `montage_repeat` / `confidence_collapse` / `deadpan_statement` / `cut_before_end`) that sits ABOVE the legacy `idea.pattern: string` and the existing `idea.howToFilm: string` so every shipped idea has a structured filming approach derived from `IdeaCoreFamily × HookIntent`. Spec source: `attached_assets/Pasted-PHASE-5-PATTERN-MAPPING-LAYER-Goal-Make-every-idea-feel_1777536080211.txt`. ZERO schema/migration changes (pure TS data + 2 new optional fields on `PatternMeta` + 1 new optional field on `Idea` zod schema). The legacy `idea.pattern` / `idea.howToFilm` / `idea.structure` strings are UNTOUCHED — Phase 5 is a parallel typed axis that the renderer can adopt incrementally. **Catalog (`PATTERN_DEFS`):** `Record<VideoPattern, VideoPatternDef>` where `VideoPatternDef = { id, beats: string[], pacing: "fast"|"medium"|"slow", cameraStyle: string, typicalDuration: "short"|"medium" }`. All 12 patterns curated with 3-4 directorial beats matching spec examples (silent_reaction → trigger→flat-face→tell→hold; escalation → attempt→worse→worse→fail; before_after → before→cut→after→hold; etc.) so `filmingGuide` derivation is just a beats copy. **`PATTERN_BY_FAMILY`:** `Record<IdeaCoreFamily, readonly VideoPattern[]>` covering ALL 12 families (not just the spec's 8 keys). Spec keys mapped semantically — overthinking↔emotional_loop+decision_paralysis, avoidance↔ritual_disruption, failure_contradiction (exact), absurd_escalation↔environmental_chaos, micro_win↔anti_climax, social_behavior↔social_friction, identity_conflict↔identity_drift, time_distortion (exact). Three families missing from spec (physical_betrayal, information_asymmetry, memory_glitch) curated consistently with their semantics. Every family resolves to 2-3 patterns. **`PATTERN_X_INTENT_COMPAT`:** `Record<HookIntent, readonly VideoPattern[]>` — spec PART 5 verbatim for 10 of 12 patterns; the 2 patterns absent from the spec table (before_after, confidence_collapse) added: before_after → scroll_stop+relatable (visual contrast), confidence_collapse → compulsion+relatable (forward-implication + admission). **Selector (`pickVideoPattern`):** walks family-allowed ∩ intent-compat, ranks candidates by inverse recency (unused patterns boost, recent patterns penalize) with deterministic tie-break, returns `{ pattern, intentFallback }`. On empty intersection, falls back to family-only pool (intent ignored, `intentFallback=true` set on meta). The selector ALWAYS returns a pattern — every candidate ships with a structured filming approach. **Generation flow:** `assembleCandidate` resolves `videoPattern` AFTER `hookIntent` is known via the actual winning entry's intent (`getEntryIntent(sourceLanguagePhrasing)`, NOT `assignedIntent` — mirrors Phase 4 discipline so fallbacks surface the real intent), threads to `meta.videoPattern` + derives `idea.filmingGuide = [...PATTERN_DEFS[pattern].beats]` (shallow-copy so downstream mutations don't corrupt the catalog). `assembleCandidate` accepts an optional `recentVideoPatternsInPool: ReadonlySet<VideoPattern>` parameter; `generatePatternCandidates`'s Cartesian inner loop tracks a per-pool `recentVideoPatterns` Set and feeds it forward so within-pool diversity is partially enforced before selection sees the pool. **Batch guards:** within `selectionPenalty` (ideaScorer.ts) — `-3` per duplicated `videoPattern` in the in-progress pick set (parallel to the Phase 4 within-batch hookIntent dup -3 lever and the within-batch voiceProfile -2 lever, sized matching VideoPattern's framing as a controller axis); `-3` cross-batch when the candidate's videoPattern appeared in the immediate-prior batch (single-tier, no frequent-last-3 stack — within-pool recency in `pickVideoPattern` already gives natural spread, so the cross-batch lever just prevents yesterday's duplicates from re-shipping). New `recentVideoPatterns?: ReadonlySet<VideoPattern>` field added to `NoveltyContext`. Added guard `h2` to `batchGuardsPass` (hybridIdeator.ts) — max 2 picks share `videoPattern` per batch (parallel to the legacy `idea.pattern` guard at hybridIdeator.ts:374 but on the typed Phase-5 axis). Pattern-less candidates (legacy cache reads / Llama / Claude wraps where `meta.videoPattern` is absent) silently abstain from all three levers — the guard never fires against absences. **PatternMeta + Idea wiring:** new optional `videoPattern?: VideoPattern` + `videoPatternIntentFallback?: boolean` fields on `PatternMeta`; new optional `filmingGuide: z.array(z.string()).max(8).optional()` on the `Idea` zod schema (max-8 cap — spec patterns produce 3-4 beats so the cap is generous). Both fields are JSONB-safe and optional so pre-Phase-5 cached entries round-trip cleanly without re-validation failure. **QA (5-batch driver):** all 7 gates green — every picked candidate has non-null `videoPattern` + `filmingGuide` ✓; every pattern ∈ `PATTERN_BY_FAMILY[family]` ✓; every pattern ∈ `PATTERN_X_INTENT_COMPAT[intent]` OR `intentFallback=true` ✓; no batch ships 3-of-same pattern ✓; ≥2 distinct patterns per batch ✓; `filmingGuide` matches the chosen pattern's `PATTERN_DEFS[pattern].beats` exactly ✓; ≥6 distinct patterns across 5 batches × 3 picks (actual 6/12) ✓. Telemetry observation: `before_after` over-represented (5/15 picks) because `ritual_disruption` is a common scenario family in the default pool — calibration tightening (per-pattern fairness in selector recency) deferred as a known trade-off; spec gates pass. Typecheck baseline preserved (7 pre-existing errors, zero new).
- **Hook Template Tuning (Phase 3 follow-up, additive overlay):** Layered on top of the now-frozen Phase 5 `VideoPattern` axis to fix the "hooks repeat the same skeleton with different nouns" failure mode (e.g. `"manage [X] now? hostage situation."` shipping in batches 2, 4, 6 with only the noun swapped). Spec source: `attached_assets/Pasted-PHASE-3-HOOK-TEMPLATE-TUNING-Cap-formulaic-skeleton-reuse_<phase3-tuning>.txt`. ZERO schema/migration changes (pure TS data + 1 new optional field on `PatternMeta` + 1 new optional field on `CachedBatchEntry` JSONB shape — both round-trip safely through pre-Phase-3-tuning cache reads). The legacy `LanguagePhrasingEntry` / `assembleCandidate` / `selectionPenalty` shapes are UNTOUCHED structurally — the tuning is a parallel set of OPTIONAL signals layered onto the existing per-intent + selection-layer scorers. **Catalog tagging:** every formulaic `(s) =>` template (templates that take a scenario noun and reuse the same skeleton across many scenarios) hand-tagged with a stable `skeletonId: string` field on `LanguagePhrasingEntry` — 9 skeletons total: `todays_update`, `how_many_days_pretending`, `how_to_avoid_three_steps`, `todays_reminder_wait`, `prep_to_think`, `future_me_vs_current_me` (build fn also rewritten — the original was producing broken sentences), `just_observing_disaster`, `morning_plan_action`, `manage_now_hostage` (typo `its` → `it's` fixed). Truly-generic `() =>` templates (templates that abstract away from the scenario entirely — e.g. `"again. seriously."`, `"still nothing."`) tagged with `genericHook: true` — 7 entries flagged. Both fields OPTIONAL so the other 86 catalog entries (and rehydrated JSONB cache entries written before this phase) round-trip cleanly without re-validation failure. **PatternMeta wiring:** new optional `hookSkeletonId?: string` field on `PatternMeta`, populated by `assembleCandidate` from the entry the picker actually returned (NOT the assigned slot — mirrors Phase 4 `hookIntent` discipline so fallbacks surface the real shipped skeleton). JSON-safe — survives JSONB cache round-trips via the same path as `videoPattern`/`hookIntent`. **Per-intent generic penalty:** `scoreScrollStop` / `scoreCompulsion` / `scoreRelatable` each subtract `-4` from `raw` when `sourceEntry?.genericHook === true` (parallel to the existing `entryRigid` / `entrySharp` reads). Symmetric across all 3 intent scorers because `genericHook` candidates abstract away from the scenario noun, lowering the scroll-stop/compulsion/relatable shape value across the board regardless of which intent is being scored. Sized at -4 (vs the spec's draft -2) because the per-intent score saturates at the `Math.max(0, raw + 5)` clamp — -2 wasn't reliably moving the composite total. **Cross-batch hookSkeleton demotion (3 tiers):** `NoveltyContext` extended with three OPTIONAL `ReadonlySet<string>` fields — `recentHookSkeletons` (immediate-prior batch), `frequentHookSkeletonsLast3` (≥2 of last 3 batches), and `hookSkeletonsAtSessionCap` (≥2 across the FULL visible cache history, not just last-3). The third tier was added mid-tuning after QA showed the every-other-batch repeat pattern (`manage_now_hostage` shipping at batches 2, 4, 6) escapes both `recent` and `last-3-frequent`: at batch-6 selection, batch-5 didn't have it (so `recent` zeroed) and only batch-4 had it within the last-3 window (so `frequentLast3` zeroed too — needs ≥2 hits in 3 batches to fire). `selectionPenalty` cross-batch block applies `-3` for `recent`, `-2` stack for `frequentLast3`, `-8` for `hookSkeletonsAtSessionCap`. The session-cap was sized -8 (vs draft -4) because some formulaic skeletons (notably `manage_now_hostage`) outscore the next compulsion-intent alternative by ~5pt on the composite — -4 was too soft to displace them. **Within-batch hookSkeleton demotion:** within-batch block applies `-3` per duplicated `metaHookSkeletonId(c.meta)` already in `batchSoFar` (parallel to the Phase 5 `videoPattern` -3 lever and the Phase 4 `hookIntent` -3 lever). Skeleton-less candidates (legacy cache reads / Llama / Claude wraps where `meta.hookSkeletonId` is absent) silently abstain from all 4 levers — the lever never fires against absences. **Selection-layer generic demotion:** flat `-3` in `selectionPenalty` (post per-intent score-clamp, in the un-clamped selection-layer space) when `metaSourceLanguagePhrasing(c.meta)?.genericHook === true`. Closes the per-intent saturation ceiling so generic-template hooks reliably lose to scenario-specific phrasings even when their other axes (hookImpact / tension / personalFit) score equally — the per-intent -4 alone wasn't enough because once a generic's intrinsic intent signal goes below 0 the additional demotion has no effect on the composite total. **CachedBatchEntry persistence:** `hookSkeletonId?: string` added to `CachedBatchEntry` shape; `tryParseEntries` tolerantly parses (string-only, drop empty — same shape as `trendId` / `videoPattern`); written into the cache envelope at both write sites (success path + retry path). `buildNoveltyContext` derives all 3 cross-batch sets from the cached envelope: `recentHookSkeletons` from the immediate-prior batch's `hookSkeletonId` field, `frequentHookSkeletonsLast3` via the same compute pattern as `frequentIdeaCoreFamiliesLast3` (≥2 hits across last 3 batches), and `hookSkeletonsAtSessionCap` from a per-skeleton occurrence count over the FULL `priorBatches` array (≥2 hits triggers session-cap inclusion). `buildNoveltyContext` signature extended with new optional 3rd parameter `allBatchesForSessionCap` (defaults to `last3Batches` for backward-compat callers); the production caller passes the full `priorBatches` array so session-cap sees the full session, not just last-3. **QA (10-batch driver):** both gates green — G1 (no `hookSkeletonId` appears >2× across the session) PASS; G2 (generic-template hooks `genericHook=true` appear strictly less often than scenario-specific phrasings) PASS with generic=3 / scenario-specific=10 across 30 shipped hooks. Distribution: 17 skeleton-tagged formulaic + 3 generic + 10 scenario-specific, with no skeleton exceeding 2 occurrences. Typecheck baseline preserved (same 7 pre-existing errors, zero new).
- **Variation Injection:** Hard rule in the ideator system prompt forces variation across scenario, setting, emotional spike, hook style, payoff type, and prop/action, preventing repetition within a batch.
- **Caption Craft:** Ideator system prompt enforces caption quality for generated ideas.
- **Lumina Evolution Engine:** Derives pattern-level bias from recent feedback and action signals, aggregating data over 60 days to guide idea generation.
- **Style Extraction:** Rule-based extraction using regex, keyword frequency, and scene-change detection.
- **Trends:** Static, manually refreshed JSON bundles per region.
- **Authentication:** Clerk.
- **Web QA Mode:** Flag-gated bypass for testing onboarding with a seeded demo creator.
- **Validation:** Zod for schema validation.
- **API Codegen:** Orval generates API hooks and Zod schemas from OpenAPI specifications.
- **Build System:** esbuild for CJS bundle creation.
- **Feature Flags:** Key features guarded by feature flags.
- **Quota Management:** Manages per-creator daily quotas and AI spend caps.
- **Job Queue:** Postgres job queue with partial unique index for deduplication.
- **Error Handling:** Structured error capture, in-process per-IP rate limiting, and Pino structured logging.
- **Consent:** Endpoints for user consent, data export, and deletion.

**Feature Specifications:**
- The Ideator generates video ideas with hard constraints: hook ≤ 3 seconds/8 words, shoot ≤ 30 minutes, and ≥50% low-effort ideas per batch.
- **Hybrid Ideator Pipeline (Layers 1/2/4 shipped):** Cost-reduction pipeline routing requests through a deterministic local pattern engine first (`lib/patternIdeator.ts` — 6 templates × ~20 safe scenarios × 5 hook-style phrasings, fully memory-biased), filtering candidates through a 6-axis 0–10 scorer with one rewrite attempt (`lib/ideaScorer.ts`), and only falling back to Claude `generateIdeas` when fewer than 3 local candidates pass the gate. Daily per-creator cache on `creators.last_idea_batch_json` + `last_idea_batch_date` (migration #20) serves same-day repeat requests for free; demo creators bypass cache writes. The orchestrator (`lib/hybridIdeator.ts`) exposes `{ ideas, source: cache|pattern|fallback|mixed, usedFallback, counts }` and the `/api/ideator/generate` route shape is unchanged. Llama 3.1 fallback and Llama 3.2 Vision are deferred to follow-up sessions.
  - **Within-batch diversity (HARD):** `diversifiedSelect` is a four-pass picker (strict → structure-unique → fresh-axis → rescue) gated at score≥8, fed by a cartesian-diagonal weave so every 16-candidate pool covers all 5 hookStyles, all 6 structures, and ≥3 distinct families.
  - **Between-batch regenerate variation (HARD, with rolling history):** When `regenerate=true`, the orchestrator reads a rolling cache history (the most-recent batch + up to `MAX_HISTORY_BATCHES=4` prior batches = 5 batches total) and applies a SPLIT HARD exclusion: every HOOK shipped in the last 5 batches is forbidden (depth 5 — mathematically prevents an identical 3-hook batch from re-appearing), but FAMILY/style exclusion only spans the immediately-prior batch (depth 1 — extending it to depth 5 squeezed the ~20-family pattern pool to ~6 candidates and starved the per-batch axis guards). Older-batch family/topic repetition is handled SOFTLY by the novelty-context penalties. The pattern engine is salted with `((hashEntries(flatHistory) ^ Date.now()) >>> 0) % 997` so each regenerate produces a structurally different (template, scenario, style) starting offset. Claude fallback (when triggered) is hook-excluded too. The cache JSONB envelope is now `{ version: 2, current: [{idea, family, templateId}], history: [batch[], ...] }` — backward-compatible reader (`tryParseCachedEnvelope`) accepts both legacy `Idea[]` and `[{idea, family, templateId}]` shapes. JSONB column means no migration needed for the shape extension.
  - **Novelty-aware selection (SOFT, BOOST + PENALTY):** The greedy selector replaces the four-pass `diversifiedSelect`. Each candidate carries `visualActionPattern` (11-value enum: `kitchen_contradiction`, `desk_avoidance`, `phone_scroll_freeze`, `bedroom_avoidance`, `outfit_check_cut`, `text_message_panic`, `face_reaction_deadpan`, `mirror_self_call_out`, `gym_no_show`, `car_avoidance`, `meal_prep_chaos`) and `topicLane` (5 values: `food_home`, `work_productivity`, `social_texting`, `body_fitness`, `daily_routine`) derived from `scenarioFamily` via `VISUAL_ACTION_BY_FAMILY` / `TOPIC_LANE_BY_FAMILY` lookup tables (`patternIdeator.ts`). `scoreNovelty` (`ideaScorer.ts`) awards 0–5 across 5 dims (hookStyle, scenario, structure, visualAction, topic) compared to the prior batch + the in-progress pick set, but ONLY when `qualityScore≥8` (no quality-rescue). `selectionPenalty` re-applies per-pick demerits with set-based saturation (-2 hookStyle, -3 family, -1 structure, -2 topic, -2 visualAction). The orchestrator uses `selectWithNovelty` (greedy by `quality + novelty + penalty`) with HARD batch guards (never 3× same hookStyle/format/structure, never >2 share family/visualAction/topic); `exhaustiveReselect` (capped at count≤5, top-`count*4` candidates) brute-forces a guard-passing combination if greedy fails. Claude fallback now triggers on EITHER `merged.length<3` OR guard failure; if guards still fail post-Claude, ships best-effort with `hybrid_ideator.guards_failed_shipping_best_effort` warn log. QA: 5 regens → 6/6 unique batches, 0 duplicate hooks, `usedFallback=false`, ≥2 distinct visual scenes + ≥2 distinct hookStyles per batch.
  - **Llama 3.1 hook-mutation layer (v2 quality-first, stateless; Session 4 = scorer-aware prompt + empty-array telemetry):** Bolted on top of the now-frozen pattern engine — `lib/llamaHookMutator.ts` rewrites HOOK (and optionally caption) on the chosen 3-pick batch only AFTER local fallback merge in `hybridIdeator.ts`. Pattern engine still owns ALL structural work (scenarios, structure, filming, shot plan); Llama never touches them. Triggers (priority order): `regenerate`, `templated_openers` (2+ shared opener or family), `recent_history_similarity` (2+ overlap with novelty ctx), `all_pattern_batch`, `borderline_novelty` (hookImpact<2 + total≥6). `pickMutationTargets` caps per trigger then HARD-clamps to ≤5 (regen→top5, templated→top5, history→top3, all_pattern→top2, borderline→filter then top5). One `meta-llama/llama-3.1-8b-instruct` chat call via `@workspace/integrations-openrouter-ai` (45s timeout, temp 0.85, 800 max tokens, json_object, ≤3 hookOptions per slot enforced post-parse). Validators (in order): `too_long` (>12) → `too_short` (<3) → `banned_opener` (`pov:|when you|watching|reading|how to`) → `sensitive_content` (dm/bank/password/ssn/salary/venmo/etc) → `abstract_advice` (deserve/learning to/etc) → `incomplete_or_dangling` (validateHook for ≤10, light dangling-only check for 11-12) → `scene_drift` (must share content noun with original setting/family/sceneBeat) → `same_opener` (lookupHookOpener differs). Each surviving rewrite is fed back through `scoreIdea` and only replaces the original if `hookImpact > original` OR `personalFit > original` OR `total ≥ original + 1` (quality-first override) — first winning hookOption per slot wins, remainder skipped. Caption mutation runs independently per slot using the same +1 total or +1 captionStrength rule. On Llama timeout/network/parse failure the entire batch ships unchanged; the OpenRouter client is lazy-initialized via `getOpenRouterClient()` so missing env vars never crash boot. `source` ladder: `pattern` (no fallback, no mutation) → `mixed` (mutation selected at least one rewrite) → `fallback` (all-Claude). Telemetry logged to `hybrid_ideator.llama_mutation`: reason, candidatesSent, optionsReturned, mutationsSelected, costEstimateTokens, rejectedReasonCounts, errored. ZERO schema changes — purely in-memory; cache writes use the mutated batch so next-regen exclusion lists stay accurate. The mutator's `source: "llama_3_1"` flag also naturally suppresses templateId in `toCacheEntries` (which only sets it for `pattern_variation` source). QA: 45/45 unit + integration assertions passing (triggers, validators, end-to-end real Llama call); on real-batch end-to-end, Llama returned 5 valid hook options for a regenerate-triggered batch with 0 mutations selected because none scored strictly better than the strong pattern originals — quality-first override behaved exactly per spec.
  - **Llama cost-control + anti-abuse layer:** Per-creator usage gates layered on top of the hook-mutation pipeline. ZERO schema changes — reuses the existing `usage_counters(creator_id, day, kind, count)` composite-PK table with three new `kind` values (`idea_request` += `result.ideas.length` per generation, `regenerate_request` += 1 per regen attempt regardless of result-count, `llama_call` fire-and-forget +1 per mutator attempt). `lib/usageTracker.ts` owns the layer: DB-backed `incrementUsage` / `getUsageToday` for the daily counters; in-memory `Map<creatorId, number[]>` for the 2-min sliding Llama-call window; in-memory `Map<creatorId, lastTs>` for the rapid-regen cooldown. **Gates (priority order, demo creators bypass everything via the same `creator.isDemo` check the cache and quota paths already use):** (1) HARD LIMIT — route-layer, `regenerate=true && idea_request_count > 100` → 429 with `{ error: "rate_limit_take_a_break", message: "Take a break — come back later for fresh ideas" }`; quota is NOT consumed on this path. (2) RAPID-REGEN COOLDOWN — `acquireRegenSlot(creatorId)` is the atomic check-and-record primitive that returns 0 for the first tap and a randomized 3000-5000ms sleep for the second tap within `<3s`; closes the TOCTOU window two parallel taps would otherwise have with separate read+record calls. (3) THROTTLE — mutator-layer, `>3` Llama calls in the trailing 2-min window → skip mutation with `costControlSkipped: true, skipReason: "throttle_2min"`. The mutator refreshes `ctx.usage.llamaCallsLast2Min` from the live in-memory ring buffer at the top of `maybeMutateBatch` so parallel requests can't all pass against a stale snapshot the route handed them pre-await. (4) DAILY ADAPTATION — probabilistic skip with `Math.random()` priority `adaptation_40` (75% skip when `idea_request_count > 40`) > `adaptation_25` (50% skip when `> 25`). (5) FALLBACK — any cost-control skip ships pattern-engine output unchanged (existing graceful path, no new branches needed). **Mobile error mapping:** `studio.tsx` and `(tabs)/index.tsx` both inspect `ApiError.data` on 429 and surface the verbatim server `message` for `error === "rate_limit_take_a_break"` (rather than the generic `buildErrorMessage` "HTTP 429: …" prefix). **Telemetry:** `ideator.usage` (per request, INFO) logs `creatorId, creatorIsDemo, count, regenerate, cooldownAppliedMs, ideaRequestCountAfter, regenerateRequestCountAfter, llamaCallCountBefore, llamaCallsLast2Min`; `hybrid_ideator.llama_mutation` gains `costControlSkipped, skipReason, ideaRequestCountToday, llamaCallsLast2Min`; `ideator.hard_limit_blocked` and `ideator.cooldown_applied` log when those gates fire. **Deferred (architect-noted, bounded impact):** parallel-request DB-level race on the hard-limit check (would require advisory locks; in practice bounds overshoot to a few extra ideas before the next tap blocks). QA: 30/30 unit + DB-roundtrip assertions passing — gate priority/boundaries, demo bypass, throttle isolation across creators, atomic cooldown under simulated parallel taps, day-bucket UTC date.
  - **Llama 3.2 Vision style-extraction layer (privacy-first, soft-bias only):** Optional on-device→server visual-style learning bolted on top of the imported-videos flow. Spec source: `attached_assets/Pasted-LLAMA-3-2-VISION-STYLE-EXTRACTION-FROM-USER-VIDEOS-Goal_1777447049198.txt`. **Mobile flow:** after a creator imports a video, the mobile app samples 4 thumbnail frames on-device via `expo-video-thumbnails` at fractional offsets `[0.15, 0.4, 0.65, 0.9]` of duration (fixed-timestamp `[1, 3, 5, 8]s` fallback when duration unknown), reads each as JPEG base64 via `expo-file-system/legacy` (the v19 split: `readAsStringAsync` lives on the legacy import), and POSTs the data URLs to a new server endpoint — NO RAW VIDEO BYTES LEAVE THE PHONE, only sampled thumbnails. `Platform.OS === "web"` short-circuits to `[]` so QA-mode in browser can't crash. Both helpers (`extractVideoFrames.ts`, `uploadVisionFrames.ts`) are fail-open: any error → empty array → upload no-ops; the upload itself is fire-and-forget so onboarding flow timing is unchanged. Only `MvpOnboarding.tsx` wires the upload (BlackHoleUpload + style-twin-train don't POST imported-videos so there's no `id` to attach vision to). **Server flow:** new `POST /api/imported-videos/:id/vision-frames` (`routes/visionStyle.ts`) auths via `resolveCreator`, verifies ownership (`imported_videos.creator_id` must equal caller), validates body `{ frames: 1–5 base64 strings, ≤250KB each }`, then calls `extractStyleFromFrames` (`lib/visionStyleExtractor.ts`) → one multimodal `meta-llama/llama-3.2-11b-vision-instruct` call via `getOpenRouterClient()` (temp 0.2 — classification not creativity, max_tokens 500, 30s timeout, `response_format: json_object`). Result fed through `aggregateVisionStyle` (`lib/visionProfileAggregator.ts`) and persisted to new `creators.vision_style_json` jsonb column (migration #21, additive `ADD COLUMN IF NOT EXISTS`, runs through the existing advisory-locked migration registry — NOT drizzle push). **Privacy posture:** the extractor's free-text `visibleAction` and `privacyRiskReason` fields are DROPPED before persistence (the per-video signal stores enums + booleans only); when the model returns `privacyRisk=true` the entire analysis is suppressed from aggregation but `totalAnalyzed` and `totalDroppedForPrivacy` still bump for transparency (so we can tell the user "we looked at N clips, dropped K for privacy" without lying). `perVideoSignals` capped at `MAX_PER_VIDEO_SIGNALS=10` with FIFO eviction; idempotent on `importedVideoId` (re-POST replaces the existing entry, never double-counts). **Derived hints (≥2/N rule, scaled):** `derivedStyleHints` only computed when `perVideoSignals.length ≥ MIN_VIDEOS_FOR_HINTS=3`; for each category (`preferredFormats`/`Settings`/`Energy`/`Framing`/`ReactionTypes`) we take values appearing in `≥ ceil(window * 0.5)` videos (the 2/3 rule scaled), top-2 per category, `unknown` enum values filtered out so the model's "I can't classify" fallback is never promoted to a "preference". **Bias hookup (SOFT, additive only, scoring-time):** `runHybridIdeator` loads `vision_style_json` alongside the other creator JSON docs (`routes/ideator.ts`) and threads `derivedStyleHints` through `scoreIdea`. `applyVisionBoost` (`lib/ideaScorer.ts`) is the ONE point in the scoring stack vision touches — it can lift `personalFit` from `1 → 2` when `mapPatternSettingToVisionSetting(meta.scenario.setting)` matches `preferredSettings` (bed→bedroom, bathroom→bathroom_mirror; couch/other → null = no boost), but NEVER `0 → anything` (avoids over-weighting borderline candidates) and NEVER subtracts. Boost runs DOWNSTREAM of generation, quality filters, and safety filters — vision can never override an explicit answer, a yes/no signal, or a safety rule, and rolling back the layer is as simple as setting `derivedStyleHints` to empty (boost no-ops). Vision NEVER generates ideas, NEVER writes hooks, NEVER replaces the ideator. **Cost control:** new `vision_call` kind on `usageTracker.UsageKind` (no schema change — `kind` is varchar(32)) with daily cap `VISION_DAILY_CAP=20`; over-cap returns `429 { error: "rate_limit_take_a_break", message: "Take a break — we'll learn from your next videos tomorrow" }` reusing the EXACT existing error key so the mobile error-mapping path (`(tabs)/index.tsx`) surfaces the verbatim server message without an additional client branch. Demo creators bypass both the OpenRouter call (returns a fixed mock analysis so demo onboarding still exercises the aggregation path) AND the counter increment, matching the cost-control layer's demo-bypass posture. **Telemetry:** `vision_style.extracted` (analyzedAt, contentType, setting, privacyRisk, derivedStyleHintsChanged), `vision_style.privacy_dropped` (when `privacyRisk=true`), `vision_style.rate_limited` (over daily cap). **QA:** 42/42 pure-logic assertions passing — extractor JSON parsing + enum clamping + null-on-malformed; aggregator privacy drop + idempotency + FIFO eviction + ≥50% threshold + MIN_VIDEOS_FOR_HINTS gate; scoreIdea boost firing on matching settings, refusing to lift `0`, refusing to fire on unmapped pattern settings (couch), respecting empty-hints state. Architect review: PASS after one fix (429 error key parity with the existing `rate_limit_take_a_break` contract — non-vision-prefixed key so the existing mobile error mapper handles it). E2E test deferred: vision flow only fires on native (web QA short-circuits) and requires a real device + OpenRouter creds — pure-logic harness covers all branches the integration test would.
  - **Script-diversity expansion (7th categorical axis to break "same mental loop" failure):** Layered on top of the novelty selector to fix the failure mode where 3 ideas in a batch — with distinct hookStyles, families, structures, visualActions, and topics — still read as variations of the same video because they share the same narrative shape (e.g. all three are "i thought i would, but actually i didn't" realizations). Spec source: `attached_assets/Pasted-SCRIPT-DIVERSITY-EXPANSION-LARGE-SCRIPT-SYSTEM-Goal-Bre_1777476842045.txt`. ZERO schema changes — derived purely from existing fields, persisted through the existing JSONB cache envelope (legacy entries pre-dating taxonomy resolve via `lookupScriptType` returning `null` so the axis no-ops on cold-start). **Taxonomy:** new `ScriptType` enum (37 values across 6 narrative-shape groups: realization-family, avoidance-family, false-start-family, low-energy-reaction-family, internal-contradiction-family, social/situational-family) + `SCRIPT_TYPES` const array + `SCRIPT_TYPE_CLUSTERS` (two `Set<ScriptType>` keyed groupings: `avoidance` = 4-element cluster, `internal_contradiction` = 5-element cluster — used by the all-3-in-cluster batch-rejection guard). New `Energy` enum (`active`/`low`/`medium`) + `ENERGY_BY_VISUAL_ACTION` lookup derived from the existing 13-value `VisualActionPattern` enum (no scenario-level tagging needed; energy is a property of how the scene is filmed, not what it depicts). `SCRIPT_TYPE_BY_FAMILY` tags each of the 25 scenarios with its primary scriptType (~14 distinct types in active rotation); `SCRIPT_TYPE_OVERRIDES: Partial<Record<TemplateId, Partial<Record<string, ScriptType>>>>` (~17 entries) handles cases where a template fundamentally reshapes narrative — e.g. `expectation_vs_reality × gym → realization` (gym's default is `avoidance`), `routine_contradiction × productivity → internal_vs_external`, `small_panic × texting → quiet_panic`. `resolveScriptType(template, family)` (override → default) used at assembly in `assembleCandidate`; `lookupScriptType(family?, templateId?)` (returns `null` when family missing/unknown — caller skips the candidate's contribution to cross-batch sets) used by `buildNoveltyContext` for cache reads, preserving JSONB legacy safety. `PatternMeta` gains optional `scriptType` + `energy` fields. **Batch guards (HARD, in `batchGuardsPass`):** (a) max-2-same scriptType per batch (3-of-same rejected, like the existing family/visualAction/topic 2-cap); (b) all-3-in-same-cluster rejection (3 picks all from `SCRIPT_TYPE_CLUSTERS.avoidance` or all from `internal_contradiction` rejected even when their scriptType values differ); (c) all-3-low-energy rejection (any batch where all 3 picks have `meta.energy === "low"` is rejected). **Scoring (`scoreNovelty` in `ideaScorer.ts`):** added 8th dim `sctFresh` (+1 when scriptType differs from prior batch + already-picked) + `unusedBoost` (+3 when scriptType ∈ `unusedScriptTypesLast3`) — 0-8 range now. **Penalty (`selectionPenalty`, per-pick stacking):** -2 for shared scriptType in already-picked (within-batch), -3 for scriptType ∈ `recentScriptTypes` (immediate-prior batch only — scope-corrected after architect review), additional -2 for scriptType ∈ `frequentScriptTypesLast3` (appearing in ≥2 of last 3 batches). **History tiers (`buildNoveltyContext` extended signature `(prev, last3Batches=[])`):** `recentScriptTypes` is SCOPED to `last3Batches[0]` (the immediate-prior batch only — NOT the flat 5-batch view the other per-axis sets use, so the -3 penalty and the regen rescue's freshness baseline don't artificially starve picks on a small pool); `frequentScriptTypesLast3` derives from `last3Batches` (≥2-of-3 occurrence); `unusedScriptTypesLast3` = `SCRIPT_TYPES − union(last3Batches)`. Other axes (recentFamilies/styles/topics/visualActions/hookOpeners/settings) keep their established 5-batch scope — strict scope discipline. **Regen rule (`selectWithNovelty` when `regenerate=true`):** after greedy/exhaustive pick, calls `batchHasNewScriptType(batch, recentScriptTypes)`; if false runs ONE additional exhaustive pass with a HARD ≥1-fresh-scriptType constraint; if still impossible ships best-effort with `hybrid_ideator.regen_no_new_scripttype_shipping_best_effort` warn log (matches the existing `guards_failed_shipping_best_effort` pattern). **Generation distribution (`interleaveByScriptType` in `patternIdeator.ts`):** post-processes `generatePatternCandidates` output to bucket candidates by scriptType then round-robin emit, so the first 6+ candidates span ≥6 distinct scriptTypes when the pool naturally allows. SOFT — never drops candidates, only reorders. **QA: 22/22 pure-logic asserts passing** — override-vs-default priority (gym default=`avoidance`, `expectation_vs_reality × gym`=`realization`, `routine_contradiction × productivity`=`internal_vs_external`); back-compat (`null` on missing/unknown family even with templateId, scenario-default fallback on existing-family + missing/unknown templateId); 10-batch run produces 15 distinct scriptTypes across 30 ideas with max single share = `avoidance` @ 13.3% (well under the 40% cap); 3-of-same scriptType + all-3-in-avoidance-cluster + all-3-low-energy all rejected by `batchGuardsPass`; regen with achievable freshness contains ≥1 fresh scriptType; regen with structurally impossible freshness (all candidates forced to one scriptType + that scriptType in recent) → standard guards reject OR rescue-failure warn fires; 16-candidate pool spans 13 distinct scriptTypes with first 6 picks already spanning 6 distinct (interleave round-robin verified). Architect review: PASS after one fix (`recentScriptTypes` scope corrected from flat 5-batch view to immediate-prior-batch-only, matching spec — non-blocking targeted-unit-test suggestions for `buildNoveltyContext` deferred since the fix is correctness-by-construction and the QA harness exercises the downstream paths). **FINAL-spec follow-on (`Pasted-SCRIPT-SYSTEM-FINAL-IMPLEMENTATION-QUALITY-DISTRIBUTION_1777479331391.txt`) layered on top of the above:** (i) **Hard pool-distribution caps (`applyPoolCaps` in `patternIdeator.ts`, run BEFORE `interleaveByScriptType` in `generatePatternCandidates`):** per-scriptType cap = `ceil(N * 0.20)` (≤20% any single scriptType); per-cluster cap = `ceil(N * 0.40)` (≤40% combined for the avoidance OR internal_contradiction cluster — directly addresses the spec §3 "must NOT dominate" callout for avoidance / procrastination / quiet-contradiction by mapping procrastination → avoidance cluster, quiet-contradiction → internal_contradiction cluster). Tail-drop preserves the upstream Cartesian-weave + memory-bias ordering for survivors (overflow entries are skipped, never reordered). No-op for pools ≤5 (any single dominant scriptType is structural at that size). ScriptType-less candidates (legacy / Claude/Llama fallback) bypass per-type/cluster checks but count toward N — preserves cap denominator integrity. (ii) **Soft regen ≥2-fresh upgrade (spec §6, in `selectWithNovelty`):** after the existing hard ≥1-fresh rescue produces `chosen`, if `countNewScriptTypes(chosen, recentScriptTypes) < 2` runs ONE more `exhaustiveReselect` with predicate `countNewScriptTypes(b, recent) >= 2`. Best-effort — silently keeps the 1-fresh pick if no 2-fresh combination exists in the pool; 1-fresh already meets the hard requirement so no warn fires. Only runs when `opts.regenerate === true`. (iii) **Soft ≥1-active preference (spec §7, in `selectWithNovelty`):** after the regen blocks, if `!batchHasActive(chosen)` runs ONE more `exhaustiveReselect` with predicate `batchHasActive(b) && countNewScriptTypes(b, recent) >= minFreshRequired`. The composed predicate is the architect-flagged correctness fix: §7 MUST preserve the freshness invariant of the currently-chosen batch (snapshotted as `minFreshRequired = (regen && recent.size>0) ? countNewScriptTypes(chosen, recent) : 0`) — otherwise §7 could silently downgrade a fresh-batch produced by §6 / hard-rescue into an active-but-0-fresh batch, violating the §6 hard regen guarantee. Runs for both regen and non-regen (non-regen path has `minFreshRequired=0` so it acts as plain `batchHasActive`). New helpers `countNewScriptTypes(batch, recent)` (counts distinct fresh scriptTypes — duplicates of same fresh type count once) and `batchHasActive(batch)` (true iff any pick has `meta.energy === "active"`). Audit-driven decisions NOT to ship: spec §4A "strongly prefer 3 distinct" already met by the `-2` within-batch scriptType penalty (3-distinct beats 2-distinct beats 1-distinct in the additive score); spec §4B "script diversity BEFORE highest scores" interpreted pragmatically — current additive `quality + novelty + penalties` selector with high-tier quality gating is internally consistent and architect-confirmed defensible (lexicographic diversity-first restructure deferred as out-of-scope). **QA: 28/28 unit asserts** — applyPoolCaps (per-type cap, per-cluster cap for both clusters, small-pool no-op, scriptType-less preservation, tail-drop head-ordering preservation), countNewScriptTypes (recent-empty/undefined edge cases, distinct-only counting with duplicates, missing-scriptType skip), batchHasActive (true/false/empty/undefined-energy), AND 6 §7-predicate-composition regression tests (REJECTS active-but-1-fresh when chosen has 2-fresh, ACCEPTS active-AND-≥2-fresh, REJECTS active-0-fresh that would downgrade rescue's 1-fresh, non-regen path correctly drops the fresh constraint). Architect re-review after the §7 composed-predicate fix: PASS — fix is correctness-by-construction, monotonic w.r.t. §6 outcomes, non-regen behavior unchanged; remaining suggestion (orchestrated-path integration tests through full greedy → hard rescue → §6 → §7 ordering) acknowledged as nice-to-have but unit-level predicate coverage deemed sufficient regression-proofing. **IDEA ARCHETYPE + SCENE-OBJECT TAG layer (final-spec follow-on, layered on top of all of the above):** two NEW derived diversity axes added to the same template-based programmatic Cartesian pipeline (NO LLM, NO new scenarios, NO schema changes — vision-style FROZEN, quality scoring untouched). User explicitly opted to NOT write 60 new scenarios; instead sceneObjectTag layer tags each existing scenario with one of 53 user-supplied tags + a 5-value env-cluster. **Taxonomy 1 — `archetypeTaxonomy.ts`:** 12 `ArchetypeFamily` ids (`self_deception`, `micro_rituals`, `social_observation`, `object_personality`, `time_distortion`, `inner_monologue`, `escalation`, `weird_habits`, `petty_logic`, `identity`, `low_energy_realism`, `unexpected_competence`) × 10 archetypes each = 120 globally-distinct `Archetype` ids (e.g. `self_deception` family contains `ill_do_it_later`, `fake_productivity`, `pretend_busy`, … `denial_of_obvious`). Hand-tuned `PRIMARY_ARCHETYPE_BY_SCRIPT_TYPE` mapping covers all 37 ScriptType values, all mapped archetypes globally distinct, all 12 families covered (weird_habits assigned via `loop_behavior` since it didn't otherwise have a natural ScriptType anchor). Lookups: `ARCHETYPES_BY_FAMILY`, `FAMILY_BY_ARCHETYPE`. `resolveArchetype(scriptType): { archetype, family } | null` deterministic; `resolveArchetypeLoose` accepts unknown strings (returns null) for legacy/fallback safety. **Taxonomy 2 — `sceneObjectTaxonomy.ts`:** 53 `SceneObjectTag` ids (`fridge`, `sink`, `laundry`, `mirror`, `phone_lock_screen`, … `front_door`) verbatim from user spec, partitioned 1:1 into 5 `SceneEnvCluster` ids (`kitchen`, `bedroom`, `phone_digital`, `outside`, `bathroom`) via total `ENV_CLUSTER_BY_TAG` lookup. `SCENE_OBJECT_TAG_BY_FAMILY` tags each of the 25 existing scenarioFamilies with one primary tag (all 25 mappings distinct, ≥3 distinct env clusters represented across the 25 families). `lookupSceneObjectTag(family)` + `lookupSceneEnvCluster(tag)` + `lookupSceneEnvClusterForFamily(family)` helpers, all null-safe on unknown input. **Plumbing — `PatternMeta` / `CandidateMeta` extension:** `PatternMeta` gains optional `archetype` + `archetypeFamily` + `sceneObjectTag` + `sceneEnvCluster` fields; `CandidateMeta` fallback union (ideaScorer.ts) extended with the same optional fields so Claude/Llama fallback wraps that omit them are type-safe. `assembleCandidate` populates all four via `resolveArchetype(scriptType)` + `lookupSceneObjectTag(family)` (cluster derived from tag via `ENV_CLUSTER_BY_TAG`); only populated when both upstream fields resolve, preserving the established missing-field-skips-axis safety pattern. **Novelty context — `NoveltyContext` extension:** added `recentArchetypes` + `recentArchetypeFamilies` (immediate-prior batch only, mirrors `recentScriptTypes` scoping) + `recentSceneObjectTags` (immediate-prior batch only — for the -3 cross-batch penalty) + `frequentSceneObjectTagsLast3` (≥2-of-3 occurrence — for the -2 stack) + `unusedSceneObjectTagsLast3` (`SCENE_OBJECT_TAGS − union(last3Batches)` — for the +3 boost). `buildNoveltyContext` extended in hybridIdeator.ts to derive these from prev cache + last3 batches via `lookupScriptType` then `resolveArchetype`, plus `lookupSceneObjectTag(family)`; absent on cold-start (preserves null-safe behavior). **Scoring — `scoreNovelty` (now 10-dim):** added `archetypeFresh` (+1 when archetype differs from cross-batch recent + already-picked) and `sceneObjectTagFresh` (+1 when sceneObjectTag differs from cross-batch recent + already-picked) binary dims. PLUS +3 `unusedTagBoost` when sceneObjectTag ∈ `unusedSceneObjectTagsLast3`. Note: NO separate +3 archetype boost — the 1:1 `ScriptType → archetype` derivation makes the existing +3 unused-scriptType boost cover archetype rotation indirectly (archetype freshness is already binary-fresh-tracked via `archetypeFresh`), so a second +3 would double-count. **Penalty — `selectionPenalty` (per-pick stacking):** -4 same archetype within batch (heaviest within-axis penalty in the system — archetype is the spec's headline anti-clone signal); -2 same archetypeFamily within batch (stacks with -4 when same archetype, so archetype-collision actually costs -6); -1 same sceneEnvCluster within batch (lighter than family since cluster is broader — `coffee` + `fridge` are both kitchen-cluster but read as more distinct than two ScriptType-cluster collisions); -3 sceneObjectTag ∈ `recentSceneObjectTags` (immediate-prior cross-batch); -2 sceneObjectTag ∈ `frequentSceneObjectTagsLast3` (stacks with -3 to -5 when both fire). All axes saturate per-pick (no double-count), all skip when fields missing. **Batch guards — 4 NEW HARD rules in `batchGuardsPass` at `batch.length >= 2`:** (d) max-1 archetype per batch (blocks two `ill_do_it_later` picks even when scenarioFamily/hook/scriptType differ — e.g. "I'll clean tomorrow" + "I'll text them tomorrow" = two distinct families but one archetype); (e) max-1 archetypeFamily per batch (stricter — two distinct archetypes from the SAME family, e.g. `ill_do_it_later` + `fake_productivity` both in `self_deception`, still read as the same kind of idea); (f) max-1 sceneObjectTag per batch (two `coffee` or two `unread_messages` picks read as the same physical scene shot twice); (g) max-1 sceneEnvCluster per batch (catches cluster collisions across distinct tags — `coffee` + `fridge` are both kitchen-cluster, feels like one kitchen morning split into two videos). All 4 guards skip when fields missing (legacy / fallback safety — counts toward batch but doesn't trip the guard). **Regen rescue — TWO additional exhaustive passes in `selectWithNovelty` (compose with the existing scriptType-fresh rescue):** (i) when `regenerate && recentArchetypeFamilies.size > 0` and current chosen batch has 0 fresh families, run `exhaustiveReselect` with `extraGuard = batchHasNewArchetypeFamily(b, recent) && (recentScriptTypes.size === 0 || batchHasNewScriptType(b, recent))` — composes the scriptType invariant into the new family invariant so this pass can't silently downgrade a freshly-rescued scriptType batch; warn-and-ship-best-effort with `hybrid_ideator.regen_no_new_archetype_family_shipping_best_effort` log if pool can't satisfy. (ii) when `regenerate && recentSceneObjectTags.size > 0` and current chosen batch has 0 fresh tags, run `exhaustiveReselect` with `extraGuard = batchHasNewSceneObjectTag(b, recent) && (recentScriptTypes ⇒ batchHasNewScriptType) && (recentArchetypeFamilies ⇒ batchHasNewArchetypeFamily)` — composes BOTH prior invariants so neither gets silently downgraded; same warn-and-ship-best-effort discipline. **§6 / §7 freshness invariants extended:** the existing §6 "≥2-fresh scriptType soft upgrade" and §7 "≥1-active soft preference" predicates extended to also preserve the snapshotted ≥1-fresh-archetypeFamily AND ≥1-fresh-sceneObjectTag invariants of the currently-chosen batch (snapshotted before the soft pass runs), so neither soft pass can silently downgrade a fresh-batch produced by the new hard rescues. **New helpers exported from hybridIdeator.ts:** `countNewArchetypeFamilies(batch, recent)`, `batchHasNewArchetypeFamily(batch, recent)`, `countNewSceneObjectTags(batch, recent)`, `batchHasNewSceneObjectTag(batch, recent)` — all vacuously true on empty recent set (no-op on cold-start), all skip picks with missing fields (fallback safety). **QA: 70/70 pure-logic asserts** — taxonomy invariants (12 families × 10 archetypes = 120 globally distinct, FAMILY_BY_ARCHETYPE inverse-consistent with ARCHETYPES_BY_FAMILY, all 37 ScriptType→archetype mappings distinct + all 12 families covered, 5 env-clusters × 53 tags total + ENV_CLUSTER_BY_TAG total + TAGS_BY_CLUSTER inverse + 25 family→tag mappings distinct + ≥3 distinct clusters represented), assembleCandidate populates archetype + sceneObjectTag on ≥80% of pattern_variation candidates from a real `generatePatternCandidates({count:16})` run, batchGuardsPass clean-3-distinct PASSES + dup-archetype FAILS + dup-family-distinct-archetypes FAILS + dup-tag FAILS + dup-cluster-distinct-tags FAILS + partial-missing-fields PASSES (3 axes verified independently), selectionPenalty arithmetic (-6 dup archetype = -4 + -2 family stack, -2 dup family-only, -1 dup cluster, -3 cross-batch tag, -3+-2=-5 cross-batch recent+frequent stack, 0 baseline empty), scoreNovelty +3 unused-tag boost exact + within-batch dup-archetype lowers novelty, all 4 helper exports tested (count + has, fresh + recent + vacuous-empty), selectWithNovelty cold-start passes guards + regen produces a batch with ≥1 fresh family AND ≥1 fresh tag AND passes hard guards (recentScriptTypes + recentArchetypes + recentArchetypeFamilies + recentSceneObjectTags all populated). Architect re-review with `includeGitDiff=true`: PASS — composition/order in selectWithNovelty correct (scriptType rescue first → archetypeFamily rescue composes scriptType freshness → sceneObjectTag rescue composes both prior invariants → §6 / §7 preserve all three invariants), missing-field safety consistently handled, penalty arithmetic intentional and non-accidental (no hidden duplicate subtraction), derived-axis plumbing coherent through assembleCandidate + buildNoveltyContext. **HOOK LANGUAGE STYLE layer (final-spec follow-on, layered on top of all of the above):** new 12-value `HookLanguageStyle` axis (`confession`, `observation`, `absurd_claim`, `matter_of_fact`, `question`, `instruction`, `micro_story`, `comparison`, `object_pov`, `time_stamp`, `anti_hook`, `escalation_hook`) becomes the PRIMARY hook diversity axis on the generation Cartesian, replacing the legacy 5-value `HookStyle` on the H axis. Legacy `hookStyle` field stays populated on every candidate via a lossy `HOOK_LANGUAGE_STYLE_TO_LEGACY_HOOK_STYLE` mapping so memory aggregator + cached-batch readers + downstream UI keep working unchanged. ZERO schema changes — derived purely from the JSONB cache envelope, legacy entries pre-dating the axis silently drop to `undefined` and are skipped by recent-set derivation (cold-start / cache-cycle safe). **Generation Cartesian (`patternIdeator.ts`):** H constant switched from 5 → 12, styleOrder built from `HOOK_LANGUAGE_STYLES`, key construction + assembleCandidate signature updated to consume `hookLanguageStyle` (legacy `hookStyle` derived inside `assembleCandidate` via lossy mapping and populated on both `Idea` and `PatternMeta`). `PatternMeta` + `CandidateMeta` fallback union extended with optional `hookLanguageStyle?` field. **NEW catalog `HOOK_PHRASINGS_BY_LANGUAGE_STYLE` (46 entries across 12 styles, ≥3 per style):** rebuilt from scratch around the new axis with build-fns parameterized over `s.actionShort`/`s.realityShort`/`s.topicNoun`/`s.settingDetail`. NONE of the new entries produce text starting with banned prefixes (verified by QA). `pickValidatedLanguagePhrasing(hookLanguageStyle, scenario, tone, seed)` mirrors the existing `pickValidatedPhrasing` but reads from the new catalog AND additionally rejects any variant matching `BANNED_HOOK_PREFIXES`. **Banned-phrasing layer (PART 4 of spec):** new `BANNED_HOOK_PREFIXES: ReadonlyArray<RegExp>` (6 entries, case-insensitive `^\s*`-anchored: "the way i", "why did i", "i really thought", "me saying", "me, ", "what i said vs what i did") + `lookupBannedHookPrefix(hook): boolean` + extended `validateHook` to also reject any matching hook (catches Claude/Llama-fallback hooks that bypass the new catalog). PLUS `GENERIC_FILLER_PHRASES: ReadonlyArray<RegExp>` (5 entries: "this is so me", "we've all been there", "so relatable", "tell me you", "name a more iconic") + `containsGenericFiller(hook): boolean` + same `validateHook` extension. Pure additive — existing rejections preserved. **Novelty context — `NoveltyContext` extension:** added `recentHookLanguageStyles` (immediate-prior batch only — for the -3 cross-batch penalty) + `unusedHookLanguageStylesLast3` (`HOOK_LANGUAGE_STYLES − union(last3Batches)` — for the +2 boost). `buildNoveltyContext` extended to derive both from prev cache + last3 batches by reading `meta.hookLanguageStyle` directly (no derivation needed since it's a first-class field). `toCacheEntries` + `rescueEntries` extended to write `hookLanguageStyle` into the JSONB envelope so next-regen exclusion lists are accurate. **Scoring — `scoreNovelty` (now 11-dim):** added `hookLanguageStyleFresh` binary dim (+1 when hookLanguageStyle differs from cross-batch recent + already-picked) + `unusedHookLanguageStyleBoost` (+2 when hookLanguageStyle ∈ `unusedHookLanguageStylesLast3`). +2 here (not +3 like sceneObjectTag's unused-tag boost) because the H pool is 12 values vs 53 — the boost is sized proportionally to the cycling pressure. **Penalty — `selectionPenalty` (per-pick stacking):** -2 same hookLanguageStyle within batch; -3 hookLanguageStyle ∈ `recentHookLanguageStyles` (immediate-prior cross-batch); -5 when `idea.hook` matches `BANNED_HOOK_PREFIXES` (PART 4 banned-phrasing penalty — second line of defense for fallback hooks that bypass the validator). All axes saturate per-pick (no double-count), all skip when fields missing on legacy/fallback wraps. **Batch guard — NEW HARD rule in `batchGuardsPass`:** SOFTER than the archetype max-1 rule per spec — only HARD-rejects when ALL 3 picks share the same hookLanguageStyle (2-of-same allowed, the soft -2 within-batch penalty in `selectionPenalty` discourages it without rejecting the whole batch). Skip when fields missing on any entry. **Regen rescue — ONE additional exhaustive pass in `selectWithNovelty` (composes with the existing 3-axis rescue chain):** when `regenerate && recentHookLanguageStyles.size > 0` and current chosen batch has 0 fresh hookLanguageStyles, run `exhaustiveReselect` with `extraGuard = batchHasNewHookLanguageStyle(b, recent) && (recentScriptTypes ⇒ batchHasNewScriptType) && (recentArchetypeFamilies ⇒ batchHasNewArchetypeFamily) && (recentSceneObjectTags ⇒ batchHasNewSceneObjectTag)` — composes ALL THREE prior invariants so neither gets silently downgraded; warn-and-ship-best-effort with `hybrid_ideator.regen_no_new_hook_language_style_shipping_best_effort` log if pool can't satisfy. **§6 / §7 freshness invariants extended:** the existing soft-upgrade predicates extended again to also preserve the snapshotted ≥1-fresh-hookLanguageStyle invariant of the currently-chosen batch, so the soft active/scriptType passes can't silently downgrade a fresh-batch produced by the new hard rescue. **New helpers exported from hybridIdeator.ts:** `countNewHookLanguageStyles(batch, recent)` + `batchHasNewHookLanguageStyle(batch, recent)` — vacuously true on empty recent set (no-op on cold-start), skip picks with missing field. **Llama hook-mutator trigger extension (`llamaHookMutator.ts`):** the existing `templated_openers` trigger now ALSO fires when ≥2 candidates in the chosen batch share the same `hookLanguageStyle`; the existing `recent_history_similarity` trigger now ALSO fires when ≥2 batch entries' `hookLanguageStyle` ∈ `ctx.novelty.recentHookLanguageStyles`. Pure additive — existing same-opener / same-family triggers preserved. The mutated hook is re-validated against `validateHook` (which now includes banned-prefix + generic-filler checks) and re-scored; only replaces if better — no regression to existing quality-first override path. **QA: 128/128 pure-logic asserts** — taxonomy enumeration (12 styles, lossy mapping covers all 12 → valid legacy HookStyle), catalog (46 entries, every (style × every stress-scenario) build passes `validateHook` + zero `BANNED`/`FILLER` trips), validateHook extension (rejects all 6 banned prefixes + all 5 filler phrases, accepts clean catalog hooks), batchGuardsPass (HARD rejects 3-identical hookLanguageStyle, ACCEPTS 2-of-same + 1-different + all-3-distinct, SKIPS hard reject when any entry missing the field), selectionPenalty (-5 for banned-prefix candidate, 0 for clean candidate with no peers, -3 for recent-hookLanguageStyle candidate, 0 escape when fresh vs recent), helper exports (`countNewHookLanguageStyles` distinct counting + missing-field skip + cold-start vacuous-true behavior, `batchHasNewHookLanguageStyle` true/false/vacuous semantics), llamaHookMutator (`shouldMutateBatch` returns `templated_openers` for 2-same hookLanguageStyle + fires for cross-batch hookLanguageStyle overlap), regex set sizing (`BANNED_HOOK_PREFIXES.length === 6`, `GENERIC_FILLER_PHRASES.length === 5`). Architect re-review with `includeGitDiff=true`: PASS — composition order in `selectWithNovelty` correct (scriptType → archetypeFamily → sceneObjectTag → hookLanguageStyle rescue chain, each composing all prior invariants → §6 / §7 preserve all four invariants), missing-field safety consistently handled across all four derived axes, Cartesian H index correctly switched 5→12 with key construction + modulo + salt all updated, banned-prefix -5 correctly positioned as second line of defense behind validator (won't double-fire in normal pattern path since validator rejects first), penalty stacking distinct sets (within-batch vs cross-batch — intentional double-charge for both stalling AND cloning), legacy lossy mapping clean and complete (all 12 styles → valid 5-value legacy HookStyle). **VOICE PROFILE layer (final-spec follow-on, layered on top of all of the above):** new 8-value `VoiceProfile` axis (`dry_humor`, `chaotic`, `poetic`, `blunt`, `sarcastic`, `self_aware`, `deadpan`, `soft_confessional`) becomes a STYLE/TONE layer atop the now-frozen HookLanguageStyle pipeline — does NOT touch safety filters, quality threshold, scenario, or archetype selection (purely additive tone overlay). ZERO schema changes — derived purely from creator context (taste calibration / styleHints / visionStyleJson / default rotation), persisted through the JSONB cache envelope. **Selection priority (`selectPrimaryVoiceProfile` in `patternIdeator.ts`):** `tasteCalibration.preferredTone → styleHints.tone → visionStyleJson.deliveryStyle → default rotation [self_aware, dry_humor]` (rotation is deterministic via `rotationSeed` so a given creator's batches stay coherent). `strongPreference: true` ONLY when calibration set (the spec's "user strongly prefers" exception to the never-3-identical rule). **Mapping tables:** `CALIBRATION_TONE_TO_VOICE` (`dry_subtle→dry_humor` / `chaotic→chaotic` / `bold→blunt` / `self_aware→self_aware`), `HINTS_TONE_TO_VOICE` (`dry→dry_humor` / `chaotic→chaotic` / `self_aware→self_aware` / `confident→blunt` / `neutral→null` so the priority chain skips this tier when DerivedStyleHints carries no usable tone signal), `VISION_DELIVERY_TO_VOICE` (`deadpan→deadpan` / `awkward→soft_confessional` / `expressive→chaotic` / `confident→blunt` / `chaotic→chaotic`). **Allowed-voice rotation sets — `ALLOWED_VOICES_BY_PRIMARY: Record<VoiceProfile, VoiceProfile[]>`:** every primary unlocks 3-4 compatible rotation voices (e.g. `self_aware → [self_aware, soft_confessional, dry_humor, deadpan]`, `dry_humor → [dry_humor, deadpan, sarcastic, self_aware]`); `DEFAULT_ALLOWED_VOICES = [self_aware, dry_humor, deadpan, soft_confessional]` is the cold-start neutral coverage set (intentionally excludes the high-energy `chaotic` and high-decoration `poetic` voices — emitted only when positive signal). `pickVoiceForSlot(slotIndex, allowedList)` round-robin through the allowed list (deterministic primary-first bias for reproducibility). **Catalog tagging — `HOOK_PHRASINGS_BY_LANGUAGE_STYLE` extended:** every existing entry tagged with optional `voiceProfiles?: VoiceProfile[]` (undefined = voice-neutral, compatible with all). Entry-level voice tags anchored in entry text (e.g. `confession`/"I just realized..." entries lean self_aware/soft_confessional; `absurd_claim` entries lean chaotic/sarcastic; `matter_of_fact` entries lean blunt/deadpan). Catalog covers every (HLS × `DEFAULT_ALLOWED_VOICES`) cell with ≥1 valid build (verified by QA); voices outside `DEFAULT_ALLOWED_VOICES` rely on the runtime fallback in `pickValidatedLanguagePhrasing` (preferred-voice → voice-neutral → any non-banned), so catalog gaps for them are graceful rather than fatal. **Plumbing — `PatternMeta` / `CandidateMeta` extension:** `PatternMeta` gains optional `voiceProfile?: VoiceProfile` field; `CandidateMeta` fallback union extended (ideaScorer.ts) so Claude/Llama fallback wraps that omit it are type-safe. `assembleCandidate` signature gains `voiceProfile: VoiceProfile` parameter; `generatePatternCandidates` calls `selectPrimaryVoiceProfile` ONCE per generation and `pickVoiceForSlot(k, allowed)` per emitted candidate INSIDE the existing Cartesian inner loop (one voice per emitted candidate, NOT a new outer axis — would explode pool 8x). **Caption transform — `applyVoiceToCaption(caption, voice)`:** light tone tweak per voice (e.g. `chaotic` appends `😭` without separating space to stay within +2-char budget; `blunt`/`deadpan` strip trailing exclamations + collapse run-on dots; `soft_confessional` is idempotent passthrough since the catalog phrasing already reads gentle). Idempotent (`f(f(x)) === f(x)`) and never increases length by more than 2 UTF-16 code units. **Orchestrator wiring — `hybridIdeator.ts`:** between visionDoc analysis and `generatePatternCandidates`, parses tasteCalibration (null-coalesced via `parseTasteCalibration`), majority-mode-aggregates `visionDoc.perVideoSignals[].deliveryStyle` (skips `"unknown"`), then calls `selectPrimaryVoiceProfile` with `hintsTone=null` (DerivedStyleHints lacks tone field — preserved as null sentinel so the priority chain falls through cleanly). `noveltyContext.voiceStrongPreference = voiceSelection.strongPreference` so downstream batch guards see the strongPreference flag, and the full `VoiceProfileSelection` is threaded into `generatePatternCandidates`. **Cross-batch novelty — `NoveltyContext` extension:** added `recentVoiceProfiles` (immediate-prior batch only — for the -2 cross-batch penalty); `unusedVoiceProfilesLast3` not added because the 8-voice pool is small (would over-rotate the +1 boost given the 4-voice cold-start subset). `buildNoveltyContext` reads `meta.voiceProfile` directly from cache envelope (first-class field, no derivation needed); `toCacheEntries` + `rescueEntries` write voiceProfile so next-regen exclusion lists are accurate. **Scoring — `scoreNovelty`:** added `voiceProfileFresh` binary dim (+1 when voiceProfile differs from cross-batch recent + already-picked); legacy entries silently drop to undefined (no penalty / no boost — preserves cold-start safety). **Penalty — `selectionPenalty` (per-pick stacking):** -2 same voiceProfile within batch; -2 voiceProfile ∈ `recentVoiceProfiles` (cross-batch). Distinct sets — intentional double-charge for stalling AND cloning. Saturates per-pick (no double-count when both fire on different axes), skips when field missing. **Batch guard — NEW HARD rule in `batchGuardsPass`:** softer than the archetype max-1 rule per spec — only HARD-rejects when ALL 3 picks share the same voiceProfile UNLESS `ctx.voiceStrongPreference === true` (calibration explicitly picked that voice — user chose this tone, never override). 2-of-same allowed. Skip when fields missing on any entry. **Regen rescue — ONE additional exhaustive pass in `selectWithNovelty` (5th in the rescue chain after scriptType / archetypeFamily / sceneObjectTag / hookLanguageStyle):** when `regenerate && recentVoiceProfiles.size > 0` and current chosen batch has 0 fresh voiceProfiles, run `exhaustiveReselect` with `extraGuard = batchHasNewVoiceProfile(b, recent) && (recentScriptTypes ⇒ batchHasNewScriptType) && (recentArchetypeFamilies ⇒ batchHasNewArchetypeFamily) && (recentSceneObjectTags ⇒ batchHasNewSceneObjectTag) && (recentHookLanguageStyles ⇒ batchHasNewHookLanguageStyle)` — composes ALL FOUR prior invariants so none gets silently downgraded. Warn-and-ship-best-effort with `hybrid_ideator.regen_no_new_voice_profile_shipping_best_effort` log if pool can't satisfy. **§6 / §7 freshness invariants extended (now 5 axes):** the existing soft-upgrade predicates extended to also preserve the snapshotted ≥1-fresh-voiceProfile invariant of the currently-chosen batch, so neither the §6 ≥2-fresh-scriptType nor the §7 ≥1-active soft pass can silently downgrade a fresh-voice batch produced by the new hard rescue. **Voice-violation validator extension — `containsVoiceViolation(hook): boolean` + `VOICE_VIOLATION_PHRASES` regex set:** 4 entries covering the spec's reject criteria — mean (`pathetic|loser|cringe|embarrassing yourself`), motivational/advice-like (`/\byou\b\s+(?:\w+\s+){0,3}(got|deserve|owe yourself|earned|are worthy)\b/i` — the bounded interstitial allows up to 3 modifier words between "you" and the advice verb so spec-flagged hooks like "you absolutely deserve this" trip the guard), motivational-prescriptive (`own your|step into your|main character energy|life update incoming`), theatrical hyperbole stacking (`literally absolutely|absolutely literally|the most.*ever`). Pure additive — `validateHook` extended to also reject any matching hook (catches Claude/Llama-fallback hooks that bypass the new catalog). Existing rejections preserved; verified all 46 existing catalog entries × all stress scenarios still pass. **New helpers exported from hybridIdeator.ts:** `countNewVoiceProfiles(batch, recent)` + `batchHasNewVoiceProfile(batch, recent)` — vacuously true on empty recent set (no-op on cold-start), skip picks with missing field. **QA: 13 sections of pure-logic asserts all green** — 8-value enum exact (every spec voice present), priority chain (calibration / hints / vision / default for all input combinations including all-null + neutral hints sentinel skip + deterministic seed-based default rotation), allowed-voice maps cover all 8 primaries with primary-first bias + no-dupes invariant, hint-map neutral→null sentinel, catalog (every (HLS × `DEFAULT_ALLOWED_VOICES`) cell has ≥1 valid build), voice-violation regex (catches "you absolutely deserve this" + "pathetic loser" + "step into your..." + benign hooks NOT flagged), validateHook composes voice violation, `applyVoiceToCaption` idempotency + ≤+2-char-cap across all 8 voices, `generatePatternCandidates` emits non-empty voiceProfile on every candidate with ≥2 distinct voices per pool, helper exports vacuous-empty behavior (empty recent set → countNew = full count, batchHasNew = true), batchGuardsPass strongPreference monotonicity (3-identical voice REJECTED when !strongPreference; pass(true) ≥ pass(false) for any batch — strongPreference can never make a batch fail that would have passed without it), 2-of-same voice does NOT trip the 3-identical guard (invariance check across strongPreference toggle), `selectionPenalty` stacking (within-batch -2 + cross-batch -2 stacks distinctly when both fire — verified ≥-4 below baseline). Architect re-review with `includeGitDiff=true`: PASS — composition order in `selectWithNovelty` correct (scriptType → archetypeFamily → sceneObjectTag → hookLanguageStyle → voiceProfile rescue chain, each composing all prior invariants → §6 / §7 preserve all five invariants), missing-field safety consistently handled, voice-violation regex correctly catches all 4 spec failure-mode classes without killing the 46 catalog entries, priority chain priority order matches spec exactly, default rotation deterministic + cold-start safe, strongPreference flag plumbed cleanly from calibration through to batch guard. **TrendContext layer (light overlay, NOT a new generator):** spec source `attached_assets/Pasted-TREND-CONTEXT-LAYER-...txt`. ZERO schema changes — purely in-memory + additive JSONB envelope (`CachedBatchEntry.trendId?: string`, tolerantly parsed so legacy entries silently skip the cross-batch lever). New `lib/trendCatalog.ts`: `TrendItem { id, type: object|behavior|phrase, label, freshnessScore ∈ [0.3,1.0], compatibleFamilies[], compatibleArchetypeFamilies[] }`, `TREND_CATALOG` (27 hand-curated items: 12 objects + 10 behaviors + 5 phrases), `TREND_BY_ID` lookup, `MIN_FRESHNESS = 0.3`, `TREND_INJECTION_RATE_PCT = 30`. **Selector** `selectTrendForCandidate({slotIndex, scenarioFamily, archetypeFamily, salt})`: deterministic FNV-1a `hashTriple` → `bucket = h%100`; `bucket >= 30` → null (gate skips ~70% by design); else filter `getActiveTrends()` by strict `trendFitsCandidate` (BOTH family and archetypeFamily must match — null/undefined inputs ALWAYS reject so cold-start / fallback candidates without resolved derivation pipeline can't accidentally receive a graft); empty fit set → null (NO forced trend); else freshness-weighted pick using upper-16-bits of the same hash for reproducibility. **Caption transform** `applyTrendToCaption(caption, trend, scenarioTopicNoun)`: `object` → first-occurrence substitute of `topicNoun` with `trend.label` (no-op when noun absent — caller's validator catches it as "substitution failed silently"); `behavior` → append ` (${trend.label})` if result ≤ 80-char ceiling, else no-op; `phrase` → append ` — ${trend.label}` if result ≤ 80, else no-op. Idempotent (`f(f(x)) === f(x)`) for all 3 types. **Validation** `validateTrendInjection(original, transformed, isCleanCaption)` rejects when transform was a no-op OR caller-supplied predicate fails. In `assembleCandidate`: `isCleanCaption = lookupBannedHookPrefix||containsGenericFiller||containsVoiceViolation` (substring-only validators — `validateHook` requires 3-10 word count and would false-reject most captions). On rejection: caption REVERTS to original and `meta.trendId` stays undefined — candidate STILL ships (soft skip per spec — never a whole-batch reject). `assembleCandidate` signature gained `slotIndex`/`slotSalt` params (defaults 0/0); resolutions hoisted above `idea` literal so trend selection sees them; inner-loop call site passes `(out.length, scenarioSeed)`. `PatternMeta.trendId?: string` + `CandidateMeta` fallback union extended with same. **Cross-batch novelty** (light): `NoveltyContext.recentTrendIds?: ReadonlySet<string>` populated by `buildNoveltyContext` from immediate-prior batch envelope's `trendId` field directly (no derivation step needed). `selectionPenalty`: -2 when `meta.trendId ∈ recentTrendIds`, single-tier ONLY (no frequent-last-3 stack and no unused-last-3 boost — trends are an OPTIONAL overlay not a forced rotation axis); skips silently when `trendId === undefined` (the ~70%-by-design norm — gate skip + soft-skip both leave trendId undefined and the penalty NEVER fires against an absent tag). Same -2 sizing as voice-profile cross-batch lever (also single-tier) — strong enough to break ties, soft enough that a high-quality pattern with a repeated trend can still win. `toCacheEntries` + `rescueEntries` write trendId; `tryParseEntries` tolerantly parses (any non-empty string passes — catalog is curator-managed and grows/decays without code deploy, so no enum whitelist; empty strings drop to undefined so the Set never holds a `""` sentinel). NO batch guard, NO regen rescue — trends are an OPTIONAL overlay per spec; we never force ≥1 trend per batch. **QA harness** (`__qa_trend_context.ts`, deleted post-green): 6 sections — catalog invariants (27 items, all 3 types, freshness ∈ [0.3,1.0], unique ids, archetype IDs resolve to known taxonomy), `trendFitsCandidate` strict AND + null-input rejection, selector reproducibility (50 inputs same → same) + emission rate (1000 samples = 26.3% — under the ≤30% target) + fit-only emission (0 forced trends when archetype unsupported), `applyTrendToCaption` idempotency + length cap + per-type behavior, `validateTrendInjection` no-op/clean-predicate gating, `selectionPenalty` -2 cross-batch repeat + 0 fresh + missing-field skip + undefined-set skip — all green. Architect re-review with `includeGitDiff=true`: PASS — selector gate structurally `<=30%`, strict fit predicate enforced, soft-skip/revert behavior verified in `assembleCandidate`, NO schema/DB writes (additive JSONB only), cross-batch penalty wiring correct + skip on absent trendId, `tryParseEntries` keeps trendId optional + drops empty/non-string to undefined preserving legacy cache compatibility, `applyTrendToCaption` idempotency + 80-char ceiling enforced, no caption mutation when validator rejects.
  - **QA hardening (anti-clone, scene-specific copy, no broken hooks):** Layered on top of the novelty selector to fix the "different ideas that read like the same video filmed three ways" failure mode. (1) **Hook completeness validator** — `validateHook` rejects hooks ending in dangling words (`and`, `vs`, `with`, `to`, `the`, `my`, `me`, etc.), unsubstituted `${...}` leaks, ellipsis truncation, or word-count outside 3–10. The old `clampHookWords` chop-to-10 is gone; instead `pickValidatedPhrasing` walks all variants for the chosen style and `assembleCandidate` returns `null` if none ship — the generator silently skips that (template, scenario, style) triple. The `tryRewrite` rescue path (`ideaScorer.ts`) likewise iterates every other style + every variant and returns `null` if nothing validates, never truncating. (2) **HookOpener taxonomy + within-batch HARD guard** — new `HookOpener` enum (9 values: `i_just`, `i_really`, `me_saying`, `the_way_i`, `why_did_i`, `pov_check`, `tell_me_why`, `what_i_planned_vs`, `the_audacity`) tagged on each `HookPhrasingEntry` and resolvable from any hook string via `lookupHookOpener` regex table; `batchGuardsPass` HARD-caps each opener at 1/batch (so a batch can't ship three "I just realized…" hooks even when hookStyle differs). (3) **Setting expansion + within-batch HARD guard** — scenario pool grew to 25 entries spanning all 8 settings (`bed`/`couch`/`desk`/`bathroom`/`kitchen`/`car`/`outside`/`other`) — added new families (`skincare`, `mirror_pep_talk`, `walk`, `doom_scroll_car`, `closet_pile`); `batchGuardsPass` HARD-caps each `setting` at 1/batch so three kitchen scenes never ship together. (4) **Per-visualAction `howToFilm`** — `HOW_TO_FILM_BY_VISUAL_ACTION` lookup with 13 framing snippets keyed by `visualActionPattern` replaces the v1 boilerplate "Phone propped at chest height". `batchGuardsPass` also HARD-rejects byte-identical `howToFilm` strings within a batch as defense in depth. (5) **Scene-specific `whatToShow`** — new `sceneBeat` field on every `Scenario` (1–2 sentence physical moment, e.g. "You step in front of the mirror in the new outfit, hold the look for one beat, then turn away") drives `buildWhatToShow`, replacing the generic template. (6) **Cross-batch novelty extension** — `NoveltyContext` gained `recentHookOpeners` (derived from cached hook text via `lookupHookOpener`, zero cache-shape change) and `recentSettings` (from `idea.setting`); `scoreNovelty` is now 0–7 (added opener + setting axes); `selectionPenalty` adds -2 same-opener and -2 same-setting. **QA gate (5 regens, deleted post-pass):** every hook validated, no 2+ openers per batch, no 2+ settings per batch, no identical `howToFilm`, all `whatToShow` ≥20 chars + scene-specific, no v1 boilerplate substring, `source: "pattern"` and `usedFallback: false` on all 5 batches.
  - **Trend context layer + archetype pairing system:** Lightweight cultural-relevance overlay so the deterministic engine doesn't sound stuck-in-2023, layered on top of the novelty selector + QA validators without any schema/cache shape changes. (1) **Curated catalog (`trendCatalog.ts`)** — 31 hand-curated `TrendItem`s spanning 4 types: `object` (substitute the scenario topic noun, e.g. "matcha" replacing "coffee"), `behavior` (parenthetical append, e.g. "(stanley cup obsession)"), `phrase` (em-dash append, e.g. " — quiet luxury"), and `format` (parenthetical append describing a video format, e.g. "(voice memo style)"). Each item carries `compatibleFamilies` (positive scenario fit), `compatibleArchetypeFamilies` (positive archetype fit), `freshnessScore ∈ [0.3, 1.0]`, optional `region` (creator-locale gate), and optional `avoidArchetypeFamilies` (negative compat — hard-rejects e.g. matcha + social_observation, stanley_cup + escalation, tiktok_dance_format + low_energy_realism). (2) **Strict-fit selector** — `selectTrendForCandidate` hashes `(slotIndex, scenarioFamily, salt)` into a 100-bucket gate; `bucket >= 30` returns null (caps emission rate at 30%). Surviving candidates are filtered by `trendFitsCandidate` (negative compat hard-reject FIRST, then positive scenario + archetype fit, then optional region match with global-default semantics — either side absent → accept). Final pick is freshness-weighted via the upper-16-bits of the same hash, fully deterministic for cache replay. (3) **Caption injection + soft-skip validator** — `applyTrendToCaption` is pure/idempotent with a tight 80-char ceiling per type; `validateTrendInjection` rejects no-op transforms (`original === transformed`) AND any transform that fails the caller's `isCleanCaption` predicate (banned-prefix / generic-filler / voice-violation). Soft-skip: when validation fails, the candidate STILL ships, just without the trend tag (no whole-batch rejection — trends are an OPTIONAL overlay). **Frequency tuning (Session 3 — spec source `attached_assets/Pasted-TREND-FREQUENCY-TUNING-SAFE-MOVE-TOWARD-20-Goal-Increas_*.txt`):** moved Session 2's freshness-weighted random pick to a 0–10 best-score selector to lift visible emission from ~12% into the 15–25% target band WITHOUT forcing weak pairings. ZERO schema changes — purely additive optional fields on `TrendItem`. (a) **Schema additions** — `secondaryFitArchetypes` / `secondaryFitFamilies` (relaxed positive fit, only consulted when primary pool empty), `avoidFamilies` (negative scenario compat — hard-rejected by predicate alongside existing `avoidArchetypeFamilies`), `niche` flag (-2 score penalty for too-online/reference-locked items like labubu + delulu_planning). (b) **Selector overhaul** — `selectTrendForCandidate` keeps the 30% hash-bucket gate, then builds a PRIMARY pool via `trendFitsCandidate`; if empty, falls back to a SECONDARY pool via the new `trendFitsCandidateSecondary` predicate (union of secondary + primary fit lists, same hard-reject discipline). Every candidate scored via NEW exported `scoreTrendFit(trend, scenarioFamily, archetypeFamily, region?, recentTrendIds?)`: +4 primary archetype, +3 primary family, +2 secondary archetype (only if no primary archetype matched), +1 secondary family (only if no primary family matched), +1 region/global (predicate-pass implicit), +1 freshness ≥ 0.7, -5 avoid (defensive double-defense), -3 weak fit (BOTH primaries missing), -3 decorative phrase/format with no archetype anchor, -2 niche, -2 repeated trend (`recentTrendIds` accepts `Set` OR readonly array, narrowed via `Array.isArray`). Threshold `TREND_FIT_THRESHOLD = 6`; sub-threshold candidates skipped (return null rather than ship a forced graft). Highest-score wins; ties broken deterministically by `(hash >>> 16) % tied.length`. (c) **Catalog tightening** — `girl_dinner` removed `identity` from `compatibleArchetypeFamilies` and added `avoidFamilies: ["productivity","errands","emails"]` per spec ("girl dinner avoids productivity / to-do list / planning / work-task"); secondary tags added to `hot_girl_walk`, `quiet_luxury`, `fake_busy_mode`, `voice_memo_format` so the secondary pool has live coverage; `niche: true` on `labubu` + `delulu_planning`. (d) **Validation** — pure-logic harness `__qa_trend_tuning.ts` (deleted post-pass — 203/203 PASS) covered score-boundary matrix (every bonus/penalty), threshold gating (6 keep / 5 reject), `girl_dinner` full archetype matrix × 3 forbidden families, secondary fallback discipline (only consulted when primary empty, never stacks on top), per-combo simulation across 5000 random samples (18.7% emission — squarely in 15-25% band), all Session 1+2 invariants preserved (idempotency, region matrix, hard cap, format type, ~30% gate, no positive ∩ negative overlap). Runtime QA across 10 batches (mixed regions western/india/philippines/nigeria, count 3+5, regenerate=true, full-catalog label net): **17.5% rate (7/40 ideas)**, zero `girl_dinner` + productivity-flavor pairings, hard cap held in all 10 batches, 4 distinct trends fired (`zara_dress`, `girl_dinner`, `lazy_GRWM`, `doomscroll_arc`), all 4 regions covered. Architect verdict: PASS — no HIGH defects; predicate ↔ score semantic consistency confirmed; tie-break deterministic and modulo-bias-immaterial; `recentTrendIds` union-narrowing correct. (4) **Within-batch HARD CAP (`enforceTrendCap` in `hybridIdeator.ts`)** — finalization pass enforces ≤ N-1 trend-injected candidates per N-pick batch (so a 3-pick batch can never ship 3-of-3 trended). When the cap fires, the LOWEST-priority trend-injected candidate has `meta.trendId` cleared AND its caption reverted from `meta.originalCaption` (a paired snapshot captured by `assembleCandidate` at injection time) — the candidate STILL ships, just untagged. Priority semantics: main path uses `score.total` (drops lowest-scoring), rescue path uses `-i` (drops last in slice). Cap formula `Math.max(1, n-1)` allows single-pick batches to carry 1 trend. (5) **Cross-batch novelty lever** — `meta.trendId` persisted on the JSONB cache envelope alongside `voiceProfile` / `hookLanguageStyle`; `buildNoveltyContext` reads `recentTrendIds` from the immediate-prior batch and `selectionPenalty` applies -2 demotion so the same trend doesn't dominate back-to-back generations. **QA harness (`__qa_trend_pairing.ts`, deleted post-pass — 39/39 PASS):** format type produces parenthetical + idempotent + length-capped; avoidArchetypeFamilies hard-rejects (predicate-only); region matrix (US/US accepts, US/EU rejects, none/EU accepts, US/none accepts, none/none accepts); selector emission rate ≈ 30% over 1000 samples within ±5pp band; catalog invariants (≥28 items, all 4 types present, no positive ∩ negative archetype overlap, freshness ∈ [0.3, 1.0], TREND_BY_ID coverage); enforceTrendCap (3-of-3 → drops exactly 1, 2-of-3 no-op, 0-trend no-op, 1-pick allows 1, rescue drops LAST); idempotency for all 4 types + validator soft-skip rejects/accepts. **Architect verdict:** PASS — no medium/severe defects; pairing semantics sound; trendId ↔ originalCaption pairing preserved across `tryRewrite` / `maybeMutateBatch` (both spread `meta`); `final` const→let reassignment correctly propagates capped state to cache write + source counters.
- Users provide "Yes / Maybe / No" feedback on ideas, influencing future generation and trending boards.
- AI-generated ideas are the sole primary entry into the filming/create flow.
- The import stage supports adding 1-2 clips with "Film" or "Upload" options.
- The "Ready to post?" stage confirms video readiness with hook + clip + caption and offers "Export video" or "Make another version."
- Film-screen UX includes a "Why this works" block derived from idea metadata.
- Camera permission denial is handled gracefully, allowing users to upload instead.
- Four fixed timing templates are deterministically selected based on the Ideator's `templateHint`.
- **Enhancement Brain:** Provides up to 3 typed suggestions (caption, hook, start hint, manual) for video improvement, reusing style derivation and viral pattern memory.
- **Semi-auto Suggestion Apply:** Allows applying suggestions with immediate visual feedback in the Before/After frame, triggering an `applied_enhancement` signal.
- **Semi-auto Edit (stitch + trim):** Offers `stitch_clips` and `trim_start` actions with visual confirmation in the Before/After frame. No on-device video processing; changes are intent-only for future processing.
- **QA-mode onboarding bypass:** Allows skipping onboarding for e2e tests.
- **Visible adaptation flow (Quick Tune):** A 3-step auto-advancing flow (format → tone → hook) that biases idea generation based on user preferences, with visual feedback and a "Better match?" prompt.
- **Quick Tune onboarding trigger:** Triggers the Quick Tune flow based on client-side state after a certain number of ideas are viewed or scrolled.
- **Daily-habit copy lock:** Home screen H1 and sub are locked to "3 ideas for today" / "Made for your style." Daily return messages vary based on user history.

# External Dependencies

- **pnpm workspaces:** Monorepo management.
- **TypeScript:** Programming language.
- **Expo (React Native):** Mobile application framework.
- **Express:** Web application framework.
- **Replit Postgres:** Database hosting.
- **Drizzle ORM:** Object-Relational Mapper.
- **Clerk:** Authentication service.
- **Claude Haiku 4.5 (via Replit AI Integrations):** Large Language Model.
- **Zod:** Schema declaration and validation.
- **Orval:** OpenAPI client code generator.
- **esbuild:** JavaScript bundler.
- **pino:** Structured logging library.