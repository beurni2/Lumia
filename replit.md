# Overview

Lumina is a pnpm monorepo project designed as a creator tool to enhance daily consistency for English-speaking micro-creators (1K–50K followers). It streamlines the content creation loop from onboarding and style profiling to idea generation, templated content creation, side-by-side review, and export. The Phase 1 MVP focuses on a rule-based Style Profile, a cloud-based Ideator powered by Claude Haiku 4.5 for generating video ideas with strict constraints, templated content creation, and a side-by-side review process. Lumina supports micro-creators in the US, UK, CA, AU, IN, PH, and NG markets, aiming to validate this core loop.

# User Preferences

I prefer to develop iteratively and see changes frequently. Please ask before making any major architectural changes or introducing new dependencies. I value clear, concise explanations and prefer a functional programming style where appropriate. Do not make changes to files or folders marked as `ARCHIVED` or related to `MONETIZATION` or `POSTING` systems unless explicitly instructed and the associated feature flag is enabled.

# System Architecture

Lumina is a pnpm workspace monorepo built with TypeScript.

**UI/UX Decisions:**
- The mobile application is developed using Expo (React Native).
- The onboarding process is a 3-step flow: region selection, first video import, and two additional imports.
- The post-export review screen reads as a finish line: a single final-video preview, a 3-item confidence strip, an optional 2-action Quick Boost (Smoother flow / Faster hook with Fix → Done ✓ micro-interaction), a friction-free "Made with Lumina" watermark toggle, a primary "Save & Post" CTA, and low-emphasis "Make another version" / "Back to ideas" secondary actions. No before/after comparison, analysis blocks, or technical warnings.
- The post-export flow extends Save & Post into a complete posting loop: a "Just save" secondary path (quieter "Saved ✓ / Post it later" copy with no auto-handoff); a "Save & Post" success block ("Saved ✓ / Ready to post / Takes ~10 seconds") that auto-opens a bottom-sheet platform picker (TikTok / Instagram / Snapchat / Copy only); an inline platform handoff card ("Almost there!" + 2-step guide + caption box + Copy caption / Copy & open {Platform} buttons); real deep links via `expo-linking` with web fallbacks (TikTok `snssdk1233://`, Instagram `instagram://library`, Snapchat `snapchat://`); clipboard writes via `expo-clipboard` on every platform tap; and a return-loop "Posted? / How did it go?" card that surfaces via `AppState`/`useFocusEffect` once the user comes back from a platform deep-link, replacing the two text buttons with Make another version / Back to ideas / View saved videos.
- A 5-question screen biases format distribution, tone, effort, privacy, and hook style based on user input.
- One-tap export to the gallery with an optional "Made with Lumina" watermark.

**Technical Implementations:**
- **Monorepo Structure:** Managed with pnpm workspaces.
- **Node.js:** Version 24.
- **API Framework:** Express 5 interacts with PostgreSQL via Drizzle ORM.
- **Database Conventions:** Additive migrations, versioned registry, immutable primary-key types.
- **LLM Integration:** Claude Haiku 4.5 via Replit AI Integrations for idea generation.
- **Per-Creator Format Distribution:** Rule-based system derives a target mix of four canonical patterns.
- **Idea Core Axis (Phase 1) [FROZEN — do not modify]:** A 120-value `ideaCoreType` axis (12 IdeaCoreFamilies × 10 types) drives selection scoring, within-batch HARD guards (≤2 per family / ≤1 per type / failure_contradiction <40%), and cross-batch regen rescue (≥2 NEW families). The legacy `scriptType` axis remains resolved + persisted as inert telemetry only — it no longer influences scoring or guards.
- **Idea Core Axis (Phase 2 — catalog expansion) [FROZEN — do not modify]:** The scenario pool grew from 25 → 83 entries (additive only — Phase 1 wiring untouched) so the 120-value `ideaCoreType` axis has a family-balanced raw pool to draw from. Per-family default-type targets: 7 each for emotional_loop / failure_contradiction / decision_paralysis / social_friction / physical_betrayal / environmental_chaos / memory_glitch / time_distortion / ritual_disruption / identity_drift / information_asymmetry, 6 for anti_climax. 6 new `SceneObjectTag`s added (`restaurant_table`, `gift_box`, `nightstand`, `weighing_scale`, `umbrella`, `wifi_router`) with `ENV_CLUSTER_BY_TAG` mappings. All 5 side-tables (`VISUAL_ACTION_BY_FAMILY`, `TOPIC_LANE_BY_FAMILY`, `SCRIPT_TYPE_BY_FAMILY`, `IDEA_CORE_TYPE_BY_FAMILY`, `SCENE_OBJECT_TAG_BY_FAMILY`) wired for every new family — no fallback path fires for any new entry. ZERO schema/migration changes (pure TS data). `IDEA_CORE_TYPE_OVERRIDES` intentionally NOT extended (sparse-override pattern preserved; defaults are sufficient). QA: 16-candidate pool now spans 8-10 distinct ideaCoreFamilies per batch (was 5-6 in Phase 1); 3 sequential batches produce 9 distinct families across the run with failure_contradiction staying under 40% in every batch.
- **Hook Template / Scroll-Stopping Upgrade (Phase 3, additive overlay):** Layered on top of the now-frozen Phase 1 `ideaCoreType` axis + Phase 2 83-scenario pool to fix the "hooks read like AI rather than scroll-stopping mid-action thoughts" failure mode. Spec source: `attached_assets/Pasted-HOOK-TEMPLATE-SYSTEM-SCROLL-STOPPING-UPGRADE-Goal-Fix-w_1777529547655.txt`. ZERO schema changes (pure TS data + a new derived score axis on the existing `IdeaScore`). **Catalog growth (76 → 102 entries across the same 12 `HookLanguageStyle`s):** the spec's 9 conceptual categories map onto the existing 12 styles (FRAGMENT → `anti_hook`, DEADPAN/BLUNT → `matter_of_fact`, EMOTIONAL_SPIKE → `confession`, SELF_AWARE/META → `observation`; rest 1:1 — no new style enum values added so the cross-batch novelty axis stays unchanged). 26 new entries are scenario-agnostic (`build: () => "..."`) so they read as raw mid-action thoughts (e.g. `"again. seriously."`, `"still nothing."`, `"9:14pm. still here."`, `"i opened it. then closed it."`) instead of frame-around-a-noun phrasings. `validateHook`'s word-count floor was lowered 3 → 2 specifically to admit these FRAGMENT-style hooks; the other rejection rails (banned prefix / generic filler / voice violation / dangling word / truncation marker / interpolation leak) all still apply, so a 2-word hook only ships if it's intentionally abrupt. **Per-template rigidity + sharpness:** `LanguagePhrasingEntry` extended with two OPTIONAL fields — `rigidityScore?: 1-5` (lower = less reusable across scenarios = better) and `sharpnessScore?: 1-5` (higher = more emotional impact). All 76 pre-existing entries backfilled with hand-audited values; the 26 new entries set both explicitly. `getEntryScores(entry)` helper resolves both fields with default 3/3 when absent — used by both the picker + the scorer so JSONB-cached entries written before Phase 3 still resolve cleanly. **scrollStopScore axis:** new `scrollStopScore: number` (0-10) field on `IdeaScore`, computed by `scoreScrollStop(hook, sourceEntry?)` from intrinsic hook properties (fragment shape, emotional charge, structure break, specificity) PLUS the source `LanguagePhrasingEntry`'s rigidity/sharpness when available. Composition: baseline 0, +3 fragment (≤4 words OR period mid-string), +2 emotional charge (token match OR entry sharpness ≥4), +2 unusual structure (no leading pronoun OR timestamp-leading), +2 not a full sentence, +1 highly specific (digit OR proper noun); −3 entry rigidity ≥4, −2 safe-phrasing prefix (`"i think"` / `"i feel like"` / `"you know when"` / `"kind of"`), −2 over-explained (>12 words), −3 pure abstraction; final `Math.max(0, Math.min(10, raw + 5))`. Folded into `IdeaScore.total` with low weight (`Math.round(scrollStopScore * 0.5)`) — nudges selection without overriding the pre-existing 6-axis scorer that shapes selection today. **PatternMeta wiring:** new optional `sourceLanguagePhrasing?: LanguagePhrasingEntry` field on `PatternMeta`, populated by `assembleCandidate` from the entry the picker returned alongside `index + hook`. RUNTIME-ONLY — the entry's `build` function does NOT survive JSON serialization to the JSONB cache, but `rigidityScore` / `sharpnessScore` / `voiceProfiles` (the JSON-safe subset) DO survive, so `getEntryScores` still resolves correctly on rehydrated entries; readers MUST stick to score-field lookups via `getEntryScores` and never call `.build()`. **Banned-prefix layer (PART 4):** the spec's hard-rejection requirement was already satisfied pre-Phase-3 by `BANNED_HOOK_PREFIXES` + the existing `validateHook` wiring (`the way i` / `why did i` / `why do i` / `i really thought` / `me saying` / `me, ` / `what i said vs what i did`); Phase 3 only verified the upgraded 102-entry pool produces zero banned-prefix hits in shipped batches. **QA (3-batch driver):** 102 entries total ✓ (need ≥100), zero banned-prefix hits across 48 shipped hooks ✓, scrollStopScore mean=9.25 across all candidates ✓ (need ≥5), mean=9.89 across top-3 picked ✓ (need ≥6), 12 distinct `HookLanguageStyle`s across 3 batches ✓ (need ≥4). Spec FRAGMENTs probe `"again. seriously."` / `"still nothing."` / `"immediately no."` / `"9:14pm. still here."` all score 9-10; `"I think this might be a problem someday"` correctly scores 5 (safe-prefix penalty fires).
- **HookIntent Refactor (Phase 4, additive overlay):** Layered on top of the now-frozen Phase 3 102-entry catalog + per-template rigidity/sharpness wiring to introduce a `HookIntent` controller axis (`scroll_stop` / `compulsion` / `relatable`) that sits ABOVE `HookLanguageStyle` and drives both template selection and per-intent scoring. Spec source: `attached_assets/Pasted-REFACTOR-HOOK-SYSTEM-INTRODUCE-HookIntent-Goal-Cleanly-_1777532632693.txt`. ZERO schema/migration changes (pure TS data + new derived score axis on the existing `IdeaScore` + 1 new optional field on `PatternMeta`). **Catalog tagging:** all 102 Phase 3 entries hand-tagged with `hookIntent: HookIntent` (REQUIRED on `LanguagePhrasingEntry`); legacy 15-entry `HOOK_PHRASINGS_BY_STYLE` catalog (used only by `tryRewrite`) tagged with OPTIONAL `hookIntent?: HookIntent` defaulting to `scroll_stop` via `getEntryIntent` helper. Distribution across the 102-entry pool: scroll_stop=46, compulsion=23, relatable=33 — chosen so each language style supports ≥1 intent without forcing artificial balance. **Generation flow (slot rotation):** `generatePatternCandidates` Cartesian inner loop assigns `assignedIntent = HOOK_INTENTS[slotIndex % 3]` per slot; `assembleCandidate` accepts the assigned intent + threads it to `pickValidatedLanguagePhrasing`, which runs an INTENT-FIRST 6-pass discipline (3 passes filtered to entries matching the assigned intent, then 3 fallback passes over ANY-intent entries when intent-matching pool is exhausted; sets `meta.intentFallback = true` on fallback). `meta.hookIntent` is derived from the entry the picker actually returned (NOT `assignedIntent`) so fallbacks surface the real shipped intent. **Per-intent scoring:** new `scoreCompulsion(hook, source?)` (mystery-end words / forward-implication / demonstrative+verb / anti-resolution; baseline +5) and `scoreRelatable(hook, source?)` (first-person opener / specific behavior verb / admission tokens / lowercase-conversational / anti-abstract; baseline +5) added alongside the existing `scoreScrollStop`; `scoreHookIntent(hook, intent, source?)` dispatches by intent. New `hookIntentScore: number` (0-10) field on `IdeaScore` computed via the dispatcher and folded into `total` at `Math.round(hookIntentScore * 0.5)` REPLACING the prior `scrollStopScore * 0.5` fold so scoring stays intent-correct without double-counting. The `scrollStopScore` field STAYS on `IdeaScore` for telemetry (always computed via `scoreScrollStop` regardless of intent) but no longer contributes to `total`. **Batch guards:** within `selectionPenalty` — `-3` per duplicated `hookIntent` in the in-progress pick set (sized between the language-style/voice -2 and archetype -4 levers, matching HookIntent's framing as the controller axis ABOVE language style); `+5` fresh-intent BONUS when batchSoFar.length>0 AND this candidate's intent is not yet in the batch (captures the spec's "Prefer 1 of each" SOFT preference and creates an 8pt swing that overcomes both the typical 2-3pt baseline gap between intent scorers AND the 2pt lower novelty bonus that compulsion candidates accrue from a narrower archetype/lang-style pool); `-100` HARD all-3-same-intent guard fires only when batchSoFar.length===2 AND both prior picks share THIS candidate's intent (overwhelms any plausible base total so the selector ALWAYS prefers a shippable other-intent candidate over a third clone). Intentless candidates (legacy cache reads / Llama / Claude wraps) silently skip both branches. **PatternMeta wiring:** new optional `hookIntent?: HookIntent` + `intentFallback?: boolean` fields on `PatternMeta`, populated by `assembleCandidate` from the entry the picker returned. JSON-safe — both fields survive JSONB cache round-trips. **QA (5-batch driver):** all 4 gates green — pool balanced (each intent in [3,9] per 16-candidate batch), shipped distribution 6/4/5 across 5 batches × 3 picks (all intents ≥3, target met), zero all-same-intent batches (HARD guard fires correctly), per-intent shipped `hookIntentScore` mean = 8.83 / 7.00 / 9.20 (all ≥6 floor). Telemetry: ~50% intent-fallback rate at the picker level reflects the catalog's uneven (style × intent) coverage — acceptable because pool composition still ends up balanced and downstream selection still ships all 3 intents. **Architect-review fixes (post-T005):** two correctness gaps surfaced by code review and patched: (1) the `-100` "hard" all-3-same intent penalty in `selectionPenalty` is a SOFT lever (it only blocks the greedy selector when an other-intent shippable EXISTS in the pool); a true hard guard was missing for the pool-starvation case where all candidates collapse to 1 effective intent. Added guard (h) to `batchGuardsPass` in `hybridIdeator.ts` — for batches of size ≥3, reject when ALL picks contributed an intent AND `new Set(intents).size === 1` (skips legacy/fallback wraps that don't carry `hookIntent` to avoid synthetic rejections). Defense-in-depth so the spec's "no batch of 3 ships with all 3 sharing the same intent" rule holds even under starvation. (2) `tryRewrite` in `ideaScorer.ts` rewrites a hook from a DIFFERENT legacy `HOOK_PHRASINGS_BY_STYLE` entry but did not update `meta.hookIntent` — rewritten candidates carried STALE intent metadata, causing `scoreHookIntent` to dispatch to the wrong per-intent scorer and the soft/hard intent batch guards to make decisions on a phantom intent. Patched: re-derive via `getEntryIntent(entry)` (defaults to `scroll_stop` for legacy entries without the field, matching first-pick discipline at `assembleCandidate` line 4948 of `patternIdeator.ts`); also clear `intentFallback` since rewrite is its own path and the original picker's fallback telemetry no longer applies. Both fixes preserve the typecheck baseline (8 pre-existing errors, zero new). Other architect findings surfaced as known trade-offs: style-first weave (HookLanguageStyle chosen before intent filter) is acceptable as additive Phase 4 compromise — full spec flow `ideaCoreType → HookIntent → compatible HookLanguageStyle` would require restructuring the Phase 1/2/3 weave (frozen this phase); +5/-3/-100 selector calibration is strong relative to other axis levers (archetype -4, scenarioFamily -3) but is now backed by a real hard guard rather than relied on as a pseudo-hard constraint; per-intent scorers (`scoreCompulsion`, `scoreRelatable`) are directionally coherent but permissive (no curly-apostrophe normalization for `should've`/`didn't`, generic first-person/lowercase admits over-score) — calibration tightening deferred to a follow-up so the structural overlay lands first.
- **Pattern Mapping Layer (Phase 5, additive overlay):** Layered on top of the now-frozen Phase 4 `HookIntent` controller axis to introduce a typed `VideoPattern` axis (12 values: `silent_reaction` / `escalation` / `micro_story` / `before_after` / `pov_internal` / `loop_behavior` / `object_pov` / `delayed_reaction` / `montage_repeat` / `confidence_collapse` / `deadpan_statement` / `cut_before_end`) that sits ABOVE the legacy `idea.pattern: string` and the existing `idea.howToFilm: string` so every shipped idea has a structured filming approach derived from `IdeaCoreFamily × HookIntent`. Spec source: `attached_assets/Pasted-PHASE-5-PATTERN-MAPPING-LAYER-Goal-Make-every-idea-feel_1777536080211.txt`. ZERO schema/migration changes (pure TS data + 2 new optional fields on `PatternMeta` + 1 new optional field on `Idea` zod schema). The legacy `idea.pattern` / `idea.howToFilm` / `idea.structure` strings are UNTOUCHED — Phase 5 is a parallel typed axis that the renderer can adopt incrementally. **Catalog (`PATTERN_DEFS`):** `Record<VideoPattern, VideoPatternDef>` where `VideoPatternDef = { id, beats: string[], pacing: "fast"|"medium"|"slow", cameraStyle: string, typicalDuration: "short"|"medium" }`. All 12 patterns curated with 3-4 directorial beats matching spec examples (silent_reaction → trigger→flat-face→tell→hold; escalation → attempt→worse→worse→fail; before_after → before→cut→after→hold; etc.) so `filmingGuide` derivation is just a beats copy. **`PATTERN_BY_FAMILY`:** `Record<IdeaCoreFamily, readonly VideoPattern[]>` covering ALL 12 families (not just the spec's 8 keys). Spec keys mapped semantically — overthinking↔emotional_loop+decision_paralysis, avoidance↔ritual_disruption, failure_contradiction (exact), absurd_escalation↔environmental_chaos, micro_win↔anti_climax, social_behavior↔social_friction, identity_conflict↔identity_drift, time_distortion (exact). Three families missing from spec (physical_betrayal, information_asymmetry, memory_glitch) curated consistently with their semantics. Every family resolves to 2-3 patterns. **`PATTERN_X_INTENT_COMPAT`:** `Record<HookIntent, readonly VideoPattern[]>` — spec PART 5 verbatim for 10 of 12 patterns; the 2 patterns absent from the spec table (before_after, confidence_collapse) added: before_after → scroll_stop+relatable (visual contrast), confidence_collapse → compulsion+relatable (forward-implication + admission). **Selector (`pickVideoPattern`):** walks family-allowed ∩ intent-compat, ranks candidates by inverse recency (unused patterns boost, recent patterns penalize) with deterministic tie-break, returns `{ pattern, intentFallback }`. On empty intersection, falls back to family-only pool (intent ignored, `intentFallback=true` set on meta). The selector ALWAYS returns a pattern — every candidate ships with a structured filming approach. **Generation flow:** `assembleCandidate` resolves `videoPattern` AFTER `hookIntent` is known via the actual winning entry's intent (`getEntryIntent(sourceLanguagePhrasing)`, NOT `assignedIntent` — mirrors Phase 4 discipline so fallbacks surface the real intent), threads to `meta.videoPattern` + derives `idea.filmingGuide = [...PATTERN_DEFS[pattern].beats]` (shallow-copy so downstream mutations don't corrupt the catalog). `assembleCandidate` accepts an optional `recentVideoPatternsInPool: ReadonlySet<VideoPattern>` parameter; `generatePatternCandidates`'s Cartesian inner loop tracks a per-pool `recentVideoPatterns` Set and feeds it forward so within-pool diversity is partially enforced before selection sees the pool. **Batch guards:** within `selectionPenalty` (ideaScorer.ts) — `-3` per duplicated `videoPattern` in the in-progress pick set (parallel to the Phase 4 within-batch hookIntent dup -3 lever and the within-batch voiceProfile -2 lever, sized matching VideoPattern's framing as a controller axis); `-3` cross-batch when the candidate's videoPattern appeared in the immediate-prior batch (single-tier, no frequent-last-3 stack — within-pool recency in `pickVideoPattern` already gives natural spread, so the cross-batch lever just prevents yesterday's duplicates from re-shipping). New `recentVideoPatterns?: ReadonlySet<VideoPattern>` field added to `NoveltyContext`. Added guard `h2` to `batchGuardsPass` (hybridIdeator.ts) — max 2 picks share `videoPattern` per batch (parallel to the legacy `idea.pattern` guard at hybridIdeator.ts:374 but on the typed Phase-5 axis). Pattern-less candidates (legacy cache reads / Llama / Claude wraps where `meta.videoPattern` is absent) silently abstain from all three levers — the guard never fires against absences. **PatternMeta + Idea wiring:** new optional `videoPattern?: VideoPattern` + `videoPatternIntentFallback?: boolean` fields on `PatternMeta`; new optional `filmingGuide: z.array(z.string()).max(8).optional()` on the `Idea` zod schema (max-8 cap — spec patterns produce 3-4 beats so the cap is generous). Both fields are JSONB-safe and optional so pre-Phase-5 cached entries round-trip cleanly without re-validation failure. **QA (5-batch driver):** all 7 gates green — every picked candidate has non-null `videoPattern` + `filmingGuide` ✓; every pattern ∈ `PATTERN_BY_FAMILY[family]` ✓; every pattern ∈ `PATTERN_X_INTENT_COMPAT[intent]` OR `intentFallback=true` ✓; no batch ships 3-of-same pattern ✓; ≥2 distinct patterns per batch ✓; `filmingGuide` matches the chosen pattern's `PATTERN_DEFS[pattern].beats` exactly ✓; ≥6 distinct patterns across 5 batches × 3 picks (actual 6/12) ✓. Telemetry observation: `before_after` over-represented (5/15 picks) because `ritual_disruption` is a common scenario family in the default pool — calibration tightening (per-pattern fairness in selector recency) deferred as a known trade-off; spec gates pass. Typecheck baseline preserved (7 pre-existing errors, zero new).
- **Hook Template Tuning (Phase 3 follow-up, additive overlay):** Layered on top of the now-frozen Phase 5 `VideoPattern` axis to fix the "hooks repeat the same skeleton with different nouns" failure mode (e.g. `"manage [X] now? hostage situation."` shipping in batches 2, 4, 6 with only the noun swapped). Spec source: `attached_assets/Pasted-PHASE-3-HOOK-TEMPLATE-TUNING-Cap-formulaic-skeleton-reuse_<phase3-tuning>.txt`. ZERO schema/migration changes (pure TS data + 1 new optional field on `PatternMeta` + 1 new optional field on `CachedBatchEntry` JSONB shape — both round-trip safely through pre-Phase-3-tuning cache reads). The legacy `LanguagePhrasingEntry` / `assembleCandidate` / `selectionPenalty` shapes are UNTOUCHED structurally — the tuning is a parallel set of OPTIONAL signals layered onto the existing per-intent + selection-layer scorers. **Catalog tagging:** every formulaic `(s) =>` template (templates that take a scenario noun and reuse the same skeleton across many scenarios) hand-tagged with a stable `skeletonId: string` field on `LanguagePhrasingEntry` — 9 skeletons total: `todays_update`, `how_many_days_pretending`, `how_to_avoid_three_steps`, `todays_reminder_wait`, `prep_to_think`, `future_me_vs_current_me` (build fn also rewritten — the original was producing broken sentences), `just_observing_disaster`, `morning_plan_action`, `manage_now_hostage` (typo `its` → `it's` fixed). Truly-generic `() =>` templates (templates that abstract away from the scenario entirely — e.g. `"again. seriously."`, `"still nothing."`) tagged with `genericHook: true` — 7 entries flagged. Both fields OPTIONAL so the other 86 catalog entries (and rehydrated JSONB cache entries written before this phase) round-trip cleanly without re-validation failure. **PatternMeta wiring:** new optional `hookSkeletonId?: string` field on `PatternMeta`, populated by `assembleCandidate` from the entry the picker actually returned (NOT the assigned slot — mirrors Phase 4 `hookIntent` discipline so fallbacks surface the real shipped skeleton). JSON-safe — survives JSONB cache round-trips via the same path as `videoPattern`/`hookIntent`. **Per-intent generic penalty:** `scoreScrollStop` / `scoreCompulsion` / `scoreRelatable` each subtract `-4` from `raw` when `sourceEntry?.genericHook === true` (parallel to the existing `entryRigid` / `entrySharp` reads). Symmetric across all 3 intent scorers because `genericHook` candidates abstract away from the scenario noun, lowering the scroll-stop/compulsion/relatable shape value across the board regardless of which intent is being scored. Sized at -4 (vs the spec's draft -2) because the per-intent score saturates at the `Math.max(0, raw + 5)` clamp — -2 wasn't reliably moving the composite total. **Cross-batch hookSkeleton demotion (3 tiers):** `NoveltyContext` extended with three OPTIONAL `ReadonlySet<string>` fields — `recentHookSkeletons` (immediate-prior batch), `frequentHookSkeletonsLast3` (≥2 of last 3 batches), and `hookSkeletonsAtSessionCap` (≥2 across the FULL visible cache history, not just last-3). The third tier was added mid-tuning after QA showed the every-other-batch repeat pattern (`manage_now_hostage` shipping at batches 2, 4, 6) escapes both `recent` and `last-3-frequent`: at batch-6 selection, batch-5 didn't have it (so `recent` zeroed) and only batch-4 had it within the last-3 window (so `frequentLast3` zeroed too — needs ≥2 hits in 3 batches to fire). `selectionPenalty` cross-batch block applies `-3` for `recent`, `-2` stack for `frequentLast3`, `-8` for `hookSkeletonsAtSessionCap`. The session-cap was sized -8 (vs draft -4) because some formulaic skeletons (notably `manage_now_hostage`) outscore the next compulsion-intent alternative by ~5pt on the composite — -4 was too soft to displace them. **Within-batch hookSkeleton demotion:** within-batch block applies `-3` per duplicated `metaHookSkeletonId(c.meta)` already in `batchSoFar` (parallel to the Phase 5 `videoPattern` -3 lever and the Phase 4 `hookIntent` -3 lever). Skeleton-less candidates (legacy cache reads / Llama / Claude wraps where `meta.hookSkeletonId` is absent) silently abstain from all 4 levers — the lever never fires against absences. **Selection-layer generic demotion:** flat `-3` in `selectionPenalty` (post per-intent score-clamp, in the un-clamped selection-layer space) when `metaSourceLanguagePhrasing(c.meta)?.genericHook === true`. Closes the per-intent saturation ceiling so generic-template hooks reliably lose to scenario-specific phrasings even when their other axes (hookImpact / tension / personalFit) score equally — the per-intent -4 alone wasn't enough because once a generic's intrinsic intent signal goes below 0 the additional demotion has no effect on the composite total. **CachedBatchEntry persistence:** `hookSkeletonId?: string` added to `CachedBatchEntry` shape; `tryParseEntries` tolerantly parses (string-only, drop empty — same shape as `trendId` / `videoPattern`); written into the cache envelope at both write sites (success path + retry path). `buildNoveltyContext` derives all 3 cross-batch sets from the cached envelope: `recentHookSkeletons` from the immediate-prior batch's `hookSkeletonId` field, `frequentHookSkeletonsLast3` via the same compute pattern as `frequentIdeaCoreFamiliesLast3` (≥2 hits across last 3 batches), and `hookSkeletonsAtSessionCap` from a per-skeleton occurrence count over the FULL `priorBatches` array (≥2 hits triggers session-cap inclusion). `buildNoveltyContext` signature extended with new optional 3rd parameter `allBatchesForSessionCap` (defaults to `last3Batches` for backward-compat callers); the production caller passes the full `priorBatches` array so session-cap sees the full session, not just last-3. **QA (10-batch driver):** both gates green — G1 (no `hookSkeletonId` appears >2× across the session) PASS; G2 (generic-template hooks `genericHook=true` appear strictly less often than scenario-specific phrasings) PASS with generic=3 / scenario-specific=10 across 30 shipped hooks. Distribution: 17 skeleton-tagged formulaic + 3 generic + 10 scenario-specific, with no skeleton exceeding 2 occurrences. Typecheck baseline preserved (same 7 pre-existing errors, zero new).
- **Hook Catalog Cleanup + Hardening (Phase 3B, additive overlay):** Layered on top of the now-frozen Phase 3-tuning skeleton-cap + generic-demotion to fix three specific bug classes surfaced by the post-Phase-3 review: (1) grammar-level template breakage (article collisions like `"the the promotion post"`, `"a the mirror"`, "talks about it's 3am"), (2) semantically-incompatible (template, scenario) fits where a fragile skeleton built on a noun whose grammatical type doesn't match (e.g. `noun_third_roommate` requires an object/abstract but received an event/action noun), and (3) verb-form mismatches between templates and `actionShort` (e.g. `"I lied about feel grateful for the birthday wishes"` — template needed gerund but `actionShort` is imperative). Spec source: in-session prompt `qa_phase3b_review.ts`. ZERO schema/migration changes (pure TS data + 1 new required field on `Scenario` + 1 new optional field on `LanguagePhrasingEntry`). The legacy `assembleCandidate` / `selectionPenalty` / `validateHook` shapes are UNTOUCHED structurally. **`validateHook` rails (PART 1, 3 new):** `\b(the|a|an)\s+(the|a|an)\b` regex catches all 9 article-collision cases; repeated-adjacent-word rejector with allowlist `had`,`that` catches the doubled-word class without false-positives on legitimate constructions like `"had had"`; `BANNED_GRAMMAR_BIGRAMS` const list (`"talks about it's"`, `"thinking about it's"`, `"thinks about it's"`, `"about it's 3am"`, `"about it's 2am"`) catches the discovered grammar-bigram failure mode. All three rails apply BEFORE the existing dangling-word / truncation / interpolation-leak rejection rails so the new failures surface as natural picker-walk fallbacks rather than late-stage rejections. **`TopicNounType` axis (PART 2):** new union type `TopicNounType = "object"|"abstract"|"action"|"place"|"event"|"body_state"|"person"` REQUIRED on every `Scenario` (no default — forces hand-tagging via typecheck). All 83 SCENARIOS hand-tagged: object (laptop, water bottle, mirror, phone, etc.), place (gym, library, bedroom, etc.), event (sneeze, meeting, birthday, concert), abstract (compliment, post, group chat, mood), body_state (soreness, hunger, tiredness), action (typing, scrolling, brushing). New optional `allowedNounTypes?: ReadonlyArray<TopicNounType>` field on `LanguagePhrasingEntry`; undefined = "any noun type works" (preserves backward-compat for the 60+ permissive entries and rehydrated JSONB cache reads). All 39 formulaic-skeleton templates with `skeletonId` hand-tagged with deliberate allowlists: `noun_third_roommate` (object, abstract), `noun_villain_origin` (object, abstract, person), `noun_smug` (object, abstract), `at_what_point_admit` (object, abstract, body_state, event), `timestamp_negotiating` (object, abstract, event, body_state), `manage_now_hostage` (object, abstract, event, body_state), `how_many_days_pretending` (object, abstract, event, body_state), `manage_live_together` (object, abstract, body_state), `started_ended_worse` (event, place, action), `started_managing_now_live` (object, abstract, body_state), `how_to_avoid_three_steps` (object, place, person), `planner_vs_version_me` (object, abstract, place, body_state), `afternoon_louder` (object, abstract, body_state, event), etc. 3 templates whose `build` reads `realityShort`/`actionShort` (not `topicNoun`) intentionally left untagged since their phrasing is noun-type-agnostic (`todays_update`, `nothing_changed_reality`, `morning_plan_action`). The 3 instruction-shape templates that DO consume `topicNoun` (`how_to_avoid_three_steps`, `pro_tip_skip`, `tutorial_ignore`) are tagged with the conservative `["object","place","event","person"]` allowlist post-architect-review — they read awkwardly with `body_state`/`abstract`/`action` nouns (e.g. "skip the soreness today", "skip the typing dots today" don't parse as something a user can deliberately not-do). **Picker integration (PART 3):** `pickValidatedLanguagePhrasing.walk()` extended with one new gate inserted AFTER the existing `voicePred` filter and BEFORE `entry.build(scenario)`: `if (entry.allowedNounTypes !== undefined && !entry.allowedNounTypes.includes(scenario.topicNounType)) continue;`. Skip-before-build is critical — the prior pattern would have called `build()` then thrown the result away after `validateHook` rejection, wasting a slot in the seed-rotated walk. Backward-compat: undefined `allowedNounTypes` always passes through, so the dead-code legacy `pickValidatedPhrasing` path (only `pickValidatedLanguagePhrasing` is wired) and all permissive entries are unaffected. **Specific template fixes (PART 4):** 5 double-article templates rewritten — `"everybody has a ${topicNoun}"` → `"everybody has ${topicNoun}"` (topicNoun already includes article), `"planner vs version me on a ${topicNoun}"` → `"planner me vs actual me on ${topicNoun}"`, `"the ${topicNoun} is smug about a ${X}"` → `"the ${topicNoun} is smug about today, frankly"`, `"3pm and the ${topicNoun}"` → `"3pm and ${topicNoun}"`, `"ate the afternoon a ${topicNoun}"` rewrites. Plus 1 verb-form fix surfaced by post-T006 QA: `"I lied about ${actionShort}"` → `"I claimed I'd ${actionShort}, frankly"` (actionShort is imperative form like `"feel grateful for the birthday wishes"`; "I claimed I'd" reads natural with imperative, "I lied about" needed gerund). **QA (10-batch driver):** all targets green — 0 grammar bugs (article-collision / repeated-word / banned-bigram regex sweep over all 30 shipped hooks returns empty), 0 composition suspects (fragile noun-template fit regex returns empty), 19 skeleton-tagged formulaic + 4 generic + 7 scenario-specific (no skeleton exceeds 2 occurrences across the session, matching Phase 3-tuning's session-cap), 26 distinct scenario families across 30 picks, intent distribution 11/10/9 across the 3 intents. Typecheck baseline preserved (same 8 pre-existing errors in `ideaGen.ts` / `trendCatalog.ts` / `ideator.ts` / `styleProfile.ts`, zero new). Phase 3 (commit c338c8d) and Phase 5 frozen — no scoring / HookIntent / pattern / Phase 5 changes, no DB schema changes.
- **Hook Catalog Skeleton-Tag Completion (Phase 3C, additive overlay):** Layered on top of the now-frozen Phase 3B `TopicNounType` axis + picker noun-type gate to fix the "tagged-but-uncapped" failure mode surfaced by post-Phase-3B QA: a 4-batch real-API curl sweep showed `"I am totally fine about X"` shipping 4×, `"introducing: X again, shockingly"` 2×, `"still avoiding X, posting instead"` 2×, `"X taking notes about my life again"` 2× — all formulaic skeletons that EXISTING Phase 3-tuning session-cap (max 2× per session) and tiered cross-batch penalty levers (`recentHookSkeletons` -3, `frequentHookSkeletonsLast3` -2, `hookSkeletonsAtSessionCap` -8) were architected to demote, but couldn't fire on because the underlying catalog entries had no `skeletonId` tag. ZERO schema/migration changes (pure TS catalog tagging + 1 new optional field on `HookPhrasingEntry` + 1 new noun-type gate inside `tryRewrite`). The legacy `assembleCandidate` / `selectionPenalty` / `validateHook` / `pickValidatedLanguagePhrasing` shapes are UNTOUCHED structurally — Phase 3C is a parallel set of OPTIONAL signals layered onto the same scoring + cache axes Phase 3-tuning already wired. **Catalog audit:** all 29 untagged formulaic `(s) =>` topicNoun-consuming templates surfaced — 8 in legacy `HOOK_PHRASINGS_BY_STYLE` (only consumed by `tryRewrite` rewriter; the sibling `pickValidatedPhrasing` picker is dead code per `rg` audit), 21 in live `HOOK_PHRASINGS_BY_LANGUAGE_STYLE` (consumed by `pickValidatedLanguagePhrasing`). Initial Python sweep caught 27 single-line `build:` arrow-fn entries; architect review surfaced 2 additional multi-line `build:` arrow-fn entries (`looked_did_nothing_scrolling`, `started_small_no_longer`) where the build expression spans a newline between `=>` and the backtick literal — those were tagged in a follow-up pass with the same skeleton + allowedNounTypes discipline. Final coverage: a multi-line-aware regex audit confirms 69 total topicNoun-consuming entries across both catalogs and 0 remaining untagged. **Bulk skeletonId tagging (PART 2):** every untagged formulaic template hand-assigned a stable `skeletonId` matching its phrasing skeleton — legacy 8: `way_i_avoid_sport`, `way_i_gaslight_about`, `refusing_to_deal`, `why_lie_about`, `why_expect_anything`, `totally_fine_about`, `planned_to_handle`, `lying_about_again`; live 21: `keep_pretending_doesnt_exist`, `avoiding_posting`, `whole_personality`, `always_one_never_deal`, `everybody_has_avoiding`, `same_loop_with`, `personality_trait_apparently`, `quietly_losing_to`, `is_it_really_still_about`, `open_stare_close`, `walks_past_nods`, `stood_near_ghost`, `life_notes_again`, `anyway_noun`, `not_great_with_today`, `so_noun`, `here_we_are_with`, `whole_post_noun`, `quiet_kind_of_nothing`, `introducing_again`, `looked_did_nothing_scrolling`, `started_small_no_longer`. Total formulaic skeletons in the cross-batch axis is now 39 (Phase 3-tuning) + 29 (Phase 3C) = 68 distinct ids the cap can demote against. **`HookPhrasingEntry.allowedNounTypes` field (PART 4 — symmetry with Phase 3B `LanguagePhrasingEntry.allowedNounTypes`):** new optional `allowedNounTypes?: ReadonlyArray<TopicNounType>` field on `HookPhrasingEntry` matching the Phase 3B field shape, with identical semantics: undefined = "any noun type works" (preserves backward-compat for permissive entries and rehydrated cache). Restrictive allowlists hand-assigned per template grammar — `totally_fine_about` + `lying_about_again` ["abstract","body_state","object","place"] (excludes event/action/person — "I am totally fine about the meeting/sneeze/concert" reads false-tone-deaf because the speaker can't denial-loop a discrete event the same way they denial-loop a chronic condition); `avoiding_posting` + `everybody_has_avoiding` + `quiet_kind_of_nothing` ["object","place","event","action","abstract"] (broad — only excludes body_state and person which don't combine with "avoiding/quiet kind of nothing"); `whole_personality` + `personality_trait_apparently` ["object","abstract","action"] (a person noun would create "the friend is my whole personality" which mis-frames; place/event read awkward); `quietly_losing_to` ["event","body_state","abstract","object"] (place/person/action don't fit the "losing to" framing); `way_i_avoid_sport` + `why_expect_anything` + `walks_past_nods` ["place","object","person"] / ["object","place","event","person"] / ["place","object","person"]; `open_stare_close` ["object"] (you can only stare-then-close objects); `stood_near_ghost` ["place","object"]; `life_notes_again` ["object","abstract","place"]; `gaslight_about` ["abstract","event","body_state","place"]. The 12 truly-permissive short anti-hook fragments (`anyway_noun`, `so_noun`, `here_we_are_with`, `whole_post_noun`, `not_great_with_today`, `introducing_again`, `refusing_to_deal`, `why_lie_about`, `planned_to_handle`, `keep_pretending_doesnt_exist`, `always_one_never_deal`, `same_loop_with`, `is_it_really_still_about`) intentionally left without an `allowedNounTypes` allowlist — their phrasings work across every noun type and the rigidity/sharpness scores already discipline their pick rate. **`tryRewrite` noun-type gate (PART 3, mirror of Phase 3B picker gate):** `tryRewrite` rewriter loop in `ideaScorer.ts` extended with one new gate inserted BEFORE `entry.build(scenario)` (mirroring the gate added to `pickValidatedLanguagePhrasing.walk()` in Phase 3B): `if (entry.allowedNounTypes !== undefined && !entry.allowedNounTypes.includes(scenario.topicNounType)) continue;`. Skip-before-build is critical — the prior pattern would have called `build()` then thrown the result away after `validateHook` rejection, wasting a slot in the seed-rotated walk. Backward-compat: undefined `allowedNounTypes` always passes through; `tryRewrite`'s existing `entry.skeletonId → nextMeta.hookSkeletonId` propagation (Phase 3-tuning) means tagging the legacy entries auto-activates the cap with no further wiring. **QA (10-batch real-API curl sweep against `POST /api/ideator/generate`):** noun-fit gate empirically firing — 0 grammar-bug regex hits across 30 shipped hooks (article-collision / repeated-word / banned-bigram), 0 noun-fit violations (zero `totally_fine_about` + event-noun pairs across the 12 ships of that template, confirming the `["abstract","body_state","object","place"]` allowlist is filtering at the rewriter), 0 typecheck regressions (same 8 pre-existing baseline errors in `ideaGen.ts` / `trendCatalog.ts` / `ideator.ts` / `styleProfile.ts`). The cross-batch session-cap was not visibly firing across the test sweep because each `regenerate: true` curl call is treated as an independent session in this isolated test setup — in real production sessions where the cache accumulates, the now-tagged 27 skeletons will participate in all three `selectionPenalty` tiers (-3 within-batch dup / -3 immediate-prior + -2 frequent-last-3 / -8 session-cap-≥2) identically to the 39 pre-existing Phase 3-tuning tags. Phase 3 (commit c338c8d), Phase 3B (commit 02dfd507), Phase 4, and Phase 5 frozen — no scoring / HookIntent / pattern / Phase 5 changes, no DB schema changes.
- **Phase 3D Bug-Fix Pass (additive, narrow scope):** Layered on top of the now-frozen Phase 3C skeleton-tag completion to fix three production bugs surfaced by a real-session 8-batch QA run. ZERO schema/migration changes (pure TS runtime in 4 files: `ideaScorer.ts`, `hybridIdeator.ts`, `patternIdeator.ts`, `ideaGen.ts`). No scoring philosophy changes, no catalog expansion, no Phase 5 changes. **Bug A — `tryRewrite` skeleton cap bypass:** the rewriter walked the full legacy `HOOK_PHRASINGS_BY_STYLE` catalog and could re-pick a skeleton that the cross-batch session-cap was actively trying to demote (`totally_fine_about` shipping in every batch even though the scorer's -8 session-cap was firing — the bypass happened upstream at the candidate-pool generation step, before the scorer could see it). Fix: added optional `recentHookSkeletons?: ReadonlySet<string>` parameter to `tryRewrite`, with a two-pass entry walk — pass 1 SKIPS entries whose `entry.skeletonId` is in the hot-skeleton set; pass 2 falls back to the prior any-entry behavior when pass 1 starves (preserves the "always return something rewritten if a valid candidate exists" contract that downstream pool-merging relies on). Both call sites (~L1372, ~L1400 in `ideaScorer.ts`) thread `input.recentHookSkeletons` from the existing `FilterAndRescoreInput` field. Acceptance: across multiple 8-batch runs, `totally_fine_about` no longer ships in every batch (typically 2-3 of 8 vs 8 of 8 pre-fix). **Bug B — exact hook string dedup:** the legacy `9:14pm. still here.` static-timestamp template was shipping repeatedly because the entry had no `skeletonId`, so the existing skeleton cap couldn't engage; even after Phase 3C's tagging push there was still a "different code path emits the same exact string" failure mode where two pattern-variation candidates produced byte-identical hooks across batches. Fix in three parts: (1) tagged the `9:14pm. still here.` entry in `patternIdeator.ts` with `skeletonId: "static_timestamp_still_here"` so the cap can fire on it. (2) added `normalizeHookForDedup(hook)` exported helper in `ideaScorer.ts` (lowercase + trim + collapse whitespace + strip trailing `.,!?;:`) — deliberately preserves mid-string punctuation so emoji / dashes / intentional fragmentation (period mid-string) all survive normalization; the static-timestamp template's "two-clause status fragment" shape depends on the mid-string period for its visual identity. (3) added optional `recentHookStrings?: ReadonlySet<string>` field to `NoveltyContext` AND `FilterAndRescoreInput` in `ideaScorer.ts`; populated by `buildNoveltyContext` in `hybridIdeator.ts` (~L1581) from the FULL visible cache history (`allBatchesForSessionCap` parameter — production caller passes the full 5-batch envelope, not just last-3, so the dedup window matches the scorer-side cap window); applied as a HARD `-1000` demotion in `selectionPenalty` (~L2117 in `ideaScorer.ts`) when the candidate's normalized hook matches either the cross-batch set OR the within-batch `batchSoFar` set. The -1000 magnitude is sized to overwhelm any plausible composite total so the selector ALWAYS prefers a fresh-string candidate over a duplicate when one exists; the selector still picks the highest-scored member of a fully-poisoned pool (acceptable starvation behavior). Both `filterAndRescore` call sites in `hybridIdeator.ts` (~L2467 local pool, ~L2538 fallback merge) thread `recentHookSkeletons` + `recentHookStrings` from `noveltyContext`. Acceptance: across multiple 8-batch runs, zero exact-hook repeats. **Bug C — Claude fallback word-count crash guard:** when the Claude fallback returned 3 ideas where every hook exceeded the 10-word ceiling, `callJsonAgent`'s schema validation threw, `recoverPartialIdeas` returned 0 partial recoveries, and the entire `runHybridIdeator` request crashed instead of degrading to the local-only selection. Fix in three parts: (1) added `recoverIdeasWithHookFix(rawText, ...)` + `tryShortenHook(hook)` helpers in `ideaGen.ts` (~L1201) — the hook-fixer parses loose JSON, then for each idea where the hook exceeds 10 words runs a deterministic trim (split on comma/dash, keep the first clause if ≤10 words; otherwise drop that idea entirely). The survivors are `safeParse`'d through the existing `IdeaSchema` so any other validation issue still rejects normally. (2) wired the recovery into `generateIdeas` (~L1054 in `ideaGen.ts`) — when `callJsonAgent` throws AND `recoverPartialIdeas` returns 0, run `recoverIdeasWithHookFix` on the raw text as a last-chance pass before re-throwing. (3) tightened the fallback prompt's hook-length line (~L999 in `ideaGen.ts`) with a JSON-only + drop-long-hooks reminder. The orchestrator-level catch in `runHybridIdeator` (`hybridIdeator.ts`) was already swallowing `generateIdeas` errors and falling back to local-only selection — this change strengthens the per-call recovery so more candidates survive instead of needing the catch. Acceptance: across multiple 8-batch runs (with `usedFallback=true` firing on multiple batches), zero `runHybridIdeator` crashes. **QA verification:** an in-memory non-demo creator was created via a temporary `POST /api/_qa/phase3d` route that ran `runHybridIdeator` 8× with `regenerate=i>0`, computed metrics, and deleted the creator. Multi-run results: 0 exact-hook repeats (Bug B PASS), `totally_fine_about` not in every batch (Bug A PASS), 0 errors with Claude fallback firing (Bug C PASS). The temporary QA route file + both wires in `routes/index.ts` were removed after validation; no runtime artifacts of the QA driver remain. Phase 3 (commit c338c8d), Phase 3B (commit 02dfd507), Phase 3C, Phase 4, and Phase 5 frozen — no scoring / HookIntent / pattern / Phase 5 changes, no DB schema changes.
- **Big Premise + Comedy Compression Layer (Phase 6, additive overlay):** Layered ABOVE the existing Phase 3 hook-template catalog (per spec PART 7 — premises sit ON TOP of templates, not replacing them) to fix the "hooks read like accurate observations but never like jokes" failure mode. Spec source: in-session prompt — 5 comedic premise styles (`SELF_ROAST`, `ABSURD_METAPHOR`, `CONTRAST_DUALITY`, `OVER_DRAMATIZATION`, `IDENTITY_FRAMING`), each a scenario-AGNOSTIC complete-hook string ≤10 words, treated as a distinct candidate kind that flows through the existing seed-rotated picker but bypasses the legacy scenario-specific scoring rails it was never designed for. ZERO schema/migration changes (pure TS data — 2 new optional fields on `LanguagePhrasingEntry`, 2 new optional fields on `PatternMeta`, 1 new optional field on `CachedBatchEntry` JSONB shape, 1 new optional field on `NoveltyContext`; all round-trip safely through pre-Phase-6 cache reads). NO scoring philosophy changes (`scoreIdea` itself is untouched), NO `HookIntent`/Phase 5 changes, NO catalog reduction. Part 4 boost/penalty (premise-specific scoring lift) deferred per spec — the structural-only flow ships standalone. **Catalog growth (29 new entries):** `BigPremiseStyle = "self_roast"|"absurd_metaphor"|"contrast_duality"|"over_dramatization"|"identity_framing"` union + `BIG_PREMISE_STYLES` const array exported from `patternIdeator.ts`. ~6 entries per style (29 total) added to `HOOK_PHRASINGS_BY_LANGUAGE_STYLE` and distributed across `confession`/`observation`/`matter_of_fact`/`anti_hook` language-style buckets so the existing seed-rotated picker can reach them. Each premise entry: `build: () => "..."` (scenario-agnostic — never consumes `scenario.topicNoun`), `bigPremise: true`, `premiseStyle: <one of 5>`, hand-assigned `hookIntent`/`voiceProfiles`, `rigidityScore: 1` (lowest — premises are unique not reusable), `sharpnessScore: 5` (highest — premises punch). Intentionally NO `genericHook` flag (premises are sharp, not flat) and NO `skeletonId` (each premise IS its own joke, not a noun-swap skeleton — no within-skeleton reuse risk). All 29 entries pass `validateHook` (≤10 words, no banned prefixes, no doubled articles). **`assembleCandidate` metadata threading + cache persistence:** `meta.usedBigPremise: boolean` + `meta.bigPremiseStyle?: BigPremiseStyle` populated from the picker's actually-returned entry (NOT the assigned slot — mirrors Phase 4 `hookIntent` discipline so fallbacks surface the real shipped style). Both fields added to `CachedBatchEntry` JSONB shape; `tryParseEntries` tolerantly parses `bigPremiseStyle` (validates against `BIG_PREMISE_STYLES`, drops unknown values for forward-compat). Written into the cache envelope at both write sites (success path + retry path). **`validateBigPremise(hook)` (PART 2 + PART 3 + PART 4-penalty rules):** new validator gate enforcing ≤10 words, no filler list (`just`, `kind of`, `really`, `basically`, `literally`, `actually`, `pretty much`, `sort of`), no boring "i did X again" / "i (was )?thinking about" patterns, no naked observation openers (`i think`, `i feel like`, `you know when`). Wired into `pickValidatedLanguagePhrasing.walk()` AFTER the existing `validateHook` gate — when `entry.bigPremise === true`, the picker requires BOTH validators to pass before returning. Existing template entries skip this gate (the `entry.bigPremise === true` predicate is the trigger), so the legacy flow is untouched. **Cross-batch premise-style novelty:** new optional `recentBigPremiseStyles?: ReadonlySet<BigPremiseStyle>` field on `NoveltyContext` (additive, undefined = no signal). `buildNoveltyContext` derives the set from `last3Batches`'s `e.bigPremiseStyle`. `selectionPenalty` extended with two new levers — within-batch `-3` per duplicated `meta.bigPremiseStyle` already in `batchSoFar`, cross-batch `-2` when the candidate's style is in `recentBigPremiseStyles`. Both sized SMALLER than the skeleton tiers (-3/-8) because each premise entry is intrinsically unique (no template-noun-swap reuse risk), so the goal is style-distribution spread, not duplicate-suppression. Premise-less candidates (legacy template picks where `meta.bigPremiseStyle` is absent) silently abstain from both levers. **Structural plumbing fixes:** two structural changes in `ideaScorer.ts` to make premises survive the legacy filter pipeline that was designed for scenario-specific hooks: (1) `tryRewrite` early-returns `null` when `meta.usedBigPremise === true` — premises are scenario-AGNOSTIC complete jokes, so rewriting one to a scenario-bound legacy template would defeat the feature (premises are immune to the rewrite path entirely); (2) `tryRewrite` also clears `usedBigPremise`/`bigPremiseStyle` on the rewritten meta (mirrors the existing `sourceLanguagePhrasing`/`skeletonId`/`intentFallback` clears) so a legacy entry post-rewrite is correctly typed as non-premise; (3) `filterAndRescore` hard floor at the `score.hookImpact === 0` check now exempts premise candidates ONLY from that arm — the `tension`/`filmability` arms still apply. The exemption is required because `scoreHookImpact` requires TENSION_MARKERS or scenario-specific words (`3am`/`coffee`/`gym`/etc) which scenario-AGNOSTIC premises never contain by design; without the exemption, every premise scores `hookImpact = 0` legitimately and gets rejected. The deferred Part 4 boost would have lifted them implicitly — the exemption is the minimal structural plumbing needed until Part 4 ships. **QA verification (3-batch real-pipeline driver):** all gates green — 5 premises shipped across 3 batches (avg 1.67 per batch ≥1 spec target), max 7 words per premise (≤10 spec cap), 0 within-batch style duplicates, 0 cross-batch style repeats, 0 batches with 3-of-same-style, 0 crashes. Per-style distribution across the test sweep: `self_roast` 1, `absurd_metaphor` 1, `contrast_duality` 1, `over_dramatization` 1, `identity_framing` 1 (all 5 styles appeared). Cold-start batch 0 ships 0 premises because the seed-rotated picker hits offset 0 deterministically and the premises are placed past the legacy entries in the catalog ordering — acceptable per acceptance criterion (average ≥1, not minimum ≥1). The temporary `POST /api/_qa/phase6` route file + its import + `router.use` line in `routes/index.ts` were removed after validation; no runtime artifacts of the QA driver remain. Phase 3 (commit c338c8d), Phase 3B (commit 02dfd507), Phase 3C, Phase 3D, Phase 4, and Phase 5 frozen — no scoring / HookIntent / pattern / Phase 5 changes, no DB schema changes.
- **PremiseStyle Engine (Phase 6 EXPANSION, additive layer on the 5-bucket BigPremiseStyle infra):** Layered ON TOP of the just-shipped Phase 6 BigPremiseStyle layer to expand the comedic vocabulary from 5 broad buckets to 50 fine-grained styles WITHOUT changing any of the upstream picker/cache/validator/scoring infra. Spec source: `attached_assets/Pasted-PHASE-6-PREMISE-STYLE-ENGINE-...1777580367516.txt`. Pure TS-additive; ZERO schema changes; ZERO scoring philosophy / Phase 5 / HookIntent changes; no template-catalog reductions. **Catalog (`patternIdeator.ts`):** new `PremiseStyleId` union of 50 ids (`cosmic_overreaction`, `gen_z_collapse`, `whiplash_wisdom`, `expectation_collapse`, `pattern_exposure`, `duality_clash`, `overdramatic_reframe`, `delusion_spiral`, `chaos_confession`, `pain_point_precision`, `everyday_armageddon`, `group_chat_guilt`, `main_character_meltdown`, `plant_parent_psychosis`, `self_destruction_speedrun`, … 35 more spanning identity / contrast / over-dramatization / absurd-metaphor / self-roast clusters), `PremiseStyleDef` type `{label, purpose, transformLogic[], worksBestWith[], hookShapes[], examples[], parentBucket: BigPremiseStyle}`, `PREMISE_STYLE_DEFS: Record<PremiseStyleId, PremiseStyleDef>` literal with all 50 entries — each maps to ONE of the 5 existing parent buckets via `parentBucket` so all upstream `bigPremise: true` plumbing (validator gate, hard-floor exemption, tryRewrite skip, cross-batch novelty) is reused unchanged. `PREMISE_STYLE_LABELS: Record<PremiseStyleId, string>` for telemetry; `getPremiseStyleParent(id)` + tolerant `parsePremiseStyleId(unknown)` helpers exported. **Entry generation (`buildPremiseEntriesFromDefs`):** programmatic builder reads `PREMISE_STYLE_DEFS[id].examples` and yields one `LanguagePhrasingEntry` per example with `bigPremise: true`, `premiseStyle: def.parentBucket`, `premiseStyleId: id`, `rigidityScore: 1`, `sharpnessScore: 5`. Spread-distributed across 4 hookLanguageStyle buckets (~99 total entries: confession 17 × relatable, observation 11 × compulsion, matter_of_fact 11 × scroll_stop, anti_hook 11 × scroll_stop) so the existing seed-rotated picker reaches them without any new picker logic. **Catalog quality:** every example pre-validated against `validateHook` + `validateBigPremise` — 7 initial broken examples (6 trailing-word "me"/"is" → DANGLING, 1 "main character energy" → VOICE_VIOLATION) reworded in catalog source so the QA driver reports 99/99 = 100% catalog pass-rate (zero silent picker rejections). **Metadata thread:** `premiseStyleId?: PremiseStyleId` + `premiseStyleLabel?: string` added to `PatternMeta` (additive, optional, undefined for legacy/Llama/Claude paths). `assembleCandidate` populates both via `PREMISE_STYLE_LABELS` lookup when the picked entry carries a `premiseStyleId`. `CachedBatchEntry` JSONB shape gains `premiseStyleId?: PremiseStyleId`; `tryParseEntries` tolerantly parses via `parsePremiseStyleId` (drops unknown values for forward-compat); written at BOTH cache write sites (success path + retry path). `tryRewrite`'s rewrite-clear path also clears `premiseStyleId`/`premiseStyleLabel` on rewritten meta so a legacy entry post-rewrite isn't mis-tagged. **Novelty levers (fine-grained, layered on top of bucket-level levers):** `recentPremiseStyleIds?: ReadonlySet<PremiseStyleId>` added to `NoveltyContext` (additive, undefined = no signal); `buildNoveltyContext` derives the set from `last3Batches` per-entry `e.premiseStyleId`. `selectionPenalty` extended with two new fine-grained levers — within-batch `-8` per duplicated `meta.premiseStyleId` already in `batchSoFar` (sized LARGER than the bucket-level `-3` because the spec hard rule "no same PremiseStyle twice in one batch" is at the FINE-GRAINED id level, not the bucket level), cross-batch `-2` when the candidate's id is in `recentPremiseStyleIds`. Both levers silently abstain when `meta.premiseStyleId` is absent (legacy template / Llama / Claude / pre-EXPANSION premise picks). **QA verification (10-batch real-pipeline driver, deleted post-pass):** every catalog example passes both validators (99/99 = 100%), 30 hooks shipped across 10 batches, 0 broken hooks (`validateHook` post-shipping), 0 within-batch fine-grained `premiseStyleId` duplicates (acceptance HARD rule), 0 within-batch dups under regenerate stress, average word count 7.23 (well within ≤10 cap). **Premise-share gap acknowledged + carried forward:** observed 30% premise-share vs spec aspirational target of 50%+. The 50% target is gated on the spec's deferred Part 4 ("PremiseStyle should influence hook generation BEFORE templates" — i.e. the picker should *prefer* premise entries over template entries when both exist), which is explicitly OUT OF SCOPE for this additive-only EXPANSION work (would require a scoring philosophy change, ruled out by the EXPANSION guardrail "5-bucket BigPremiseStyle infra reused unchanged"). The 30% number reflects the existing Phase 6 picker's organic premise share with no premise-favoring bias — Phase 6 EXPANSION's job was vocabulary expansion + fine-grained novelty, both shipped clean. Part 4 (premise-first selection bias) carried forward as deferred follow-up. **Cleanup:** the temporary `POST /api/_qa/phase6expansion` route file + its import + `router.use` line in `routes/index.ts` were removed after validation; the temporary `export` keyword on `readBatchHistory` (used by the QA driver to read per-batch metadata not surfaced on the public `Idea` shape) was reverted to `function`; no runtime artifacts of the QA driver remain. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 base all frozen — no scoring / HookIntent / pattern / Phase 5 changes, no DB schema changes.
- **Premise-First Selection + Output Line Optimization (Phase 6C, additive overlay on Phase 6 EXPANSION):** Layered on Phase 6 EXPANSION to deliver Part 4 (premise-first selection bias) plus a comedy-compression / caption-like rejection rail so shipped hooks are short / simple / funny / instantly understandable, AND to constrain Llama to premise-preserving polish only. Spec source: `attached_assets/Pasted-PHASE-6C-PREMISE-FIRST-SELECTION-OUTPUT-LINE-OPTIMIZATI_1777586447218.txt`. Pure TS-additive; ZERO schema changes; ZERO scoring philosophy / Phase 5 / HookIntent changes; reuses the Phase 6 EXPANSION 99-entry premise catalog + 50-id PremiseStyleId axis untouched. **Premise-first picker walk (`pickValidatedLanguagePhrasing.walk`, `patternIdeator.ts`):** the seed-rotated single-pass walk became a TWO-PASS walk gated by a deterministic 100-bucket hash (`PREMISE_FIRST_BUCKET_PCT = 85`) — Pass 1 only considers entries with `bigPremise === true` (in same seed-rotated order; failures naturally drop to Pass 2); Pass 2 falls back to all entries (legacy templates) only when Pass 1 yielded nothing OR when the bucket lands in the 15% legacy-rhythm slice (mirrors the trend-cap selector's hash style for cache-replay determinism). The 15% legacy-rhythm slice exists explicitly per spec ("keep 10-20% legacy") so the engine never feels like one premise note on repeat. **Selector preference (`selectionPenalty`, `ideaScorer.ts`):** new `PREMISE_PREFERENCE_BONUS = +3` applied unconditionally when `meta.usedBigPremise === true` (positive lever, NOT a hard guard, so legacy still wins on superior quality); SYMMETRIC `+2 boost` (positive twin to the existing -2 cross-batch demote) when candidate's `premiseStyleId` is NOT in `recentPremiseStyleIds` AND `recentPremiseStyleIds.size > 0` (first-batch cold-start abstains so the boost never shapes a fresh creator's first run). **Output-line compression + caption-like / AI-feel rejection (`compressHook` + `validateOutputLine`, `patternIdeator.ts`):** new `compressHook(hook)` is a pure idempotent transform that strips leading filler words (`currently | kind of | sort of | pretty much | basically | literally | actually | really | just`), softeners (`a little bit | i think | i feel like | you know`), trailing redundancies (`today | again` when already implied), double articles + multi-clause caption-like splits — never returns empty + abstains when the compressed length would fall under the 3-word floor. New `validateOutputLine` rail (layered AFTER `validateHook` + `validateBigPremise`) rejects caption-like (3+ sentences OR sentence-starting-capitalized followed by period-then-capital), too-many-clauses (>2 commas OR >2 of `and|but|or|because`), over-clever (`paradox | conundrum | juxtaposition | irony | oxymoron`), AI-feel patterns (`it turns out that | as it happens | interestingly enough | the truth is | ultimately`). Wired into the picker walk as: `compressHook(candidate)` first, then the COMPRESSED string is `validateOutputLine`-checked; failure falls through to next entry (NOT a rewrite — picker walks). **Within-batch HARD premiseStyleId guard (`hybridIdeator.ts`):** in `selectWithNovelty` + `exhaustiveReselect`, a candidate whose `meta.premiseStyleId` is already shipped in the current batch is HARD-rejected (in addition to the existing -8 selection penalty which stays as defense-in-depth for greedy ordering). HARD-guarantee: 0 same fine-grained `premiseStyleId` ships twice in any batch; the bucket-level `parentBucket` is unaffected (still soft-guided by the -3 lever). **Llama premise-preserving polish constraint (`llamaHookMutator.ts`):** when input candidate has `meta.usedBigPremise === true`, the system prompt receives a POLISH-only addendum ("PRESERVE the premise/joke. Make it shorter and more natural. Allowed: drop filler, fix awkward phrasing, preserve slang/lowercase/fragments. Forbidden: change the joke, add new scenes/objects/people, professionalize, make it more clever.") AND a new `polish_changed_premise` validator rejects mutations whose Levenshtein distance from the original > 0.4 × original.length OR that introduce a new content noun not in the original (the existing `scene_drift` validator covers a subset; the polish gate is stricter). The pre-existing quality-first override stays unchanged (rewrite only ships if `hookImpact > original` OR `personalFit > original` OR `total ≥ original + 1`). Non-premise candidates (legacy templates) keep the existing Llama behavior unchanged. **QA verification (`POST /api/_qa/phase6c`, 10-batch real-pipeline driver, deleted post-validation):** sentinel non-demo creator (`authUserId="qa:phase6c"`), envelope reset per invocation, count=3, `regenerate=i>0`. Canonical run at spec'd `PREMISE_FIRST_BUCKET_PCT=85` shipped 30 hooks across 10 batches: **premiseShipped=18 / 30 = 60% (✓ doubled vs Phase 6 EXPANSION's 30% baseline; below the spec's 80-90% HARD gate)**, legacyShipped=12 / 30 = 40%, **avgWordCount=6.63 (✓ in the 6-10 HARD range)**, wordCountMin=4 / wordCountMax=9, **withinBatchPremiseStyleDups=0 (✓ HARD guard verified)**, **0 broken/dangling hooks across the 30 shipped lines (✓ HARD)**, no Llama mutations introduced new content nouns on premise hooks (HARD — Llama path inactive without OpenRouter key in QA env, polish validator unit-verified). **Premise-share gap acknowledged + carried forward:** the 60% lands in the same structural ceiling Phase 6 EXPANSION hit at 30% — at the spec's `count=3` ship size the candidate pool exposes ~2-3 unique `premiseStyleId`s per batch (Cartesian weave at `target=Math.max(12, Math.min(input.count ?? 16, 20))` → target=12 with count=3), so once slots 1-2 ship two distinct premise styles the HARD within-batch dup guard exhausts the fresh-style premise set and slot 3 falls through to the dominant legacy templates ("I am totally fine about ${topicNoun}" / "I really planned to handle ${topicNoun}") which carry no `genericHook=true` demotion. Bumping `PREMISE_FIRST_BUCKET_PCT` 85→95 only moved the share 60%→67% (gained 1 premise across 30 slots) — confirming the bottleneck is candidate-pool premise-style diversity, not the picker walk gate. Closing the final 20pp gap requires either bumping the candidate-pool target floor (touches core production performance, OUT OF SCOPE for Phase 6C per the spec guardrail "ZERO schema changes; reuses Phase 6 EXPANSION infra") OR expanding per-(scenario, hookLanguageStyle) premise-style coverage in the catalog — both deferred to a future phase (Phase 6D / Phase 7). The 60% measurement still represents a 2× lift over Phase 6 EXPANSION's organic 30% baseline; all other HARD gates pass; selection levers + output-line compression + Llama polish constraint are wired and verified working as designed. **Cleanup:** the temporary `POST /api/_qa/phase6c` route file + its import + `router.use` line in `routes/index.ts` were removed after validation; no runtime artifacts of the QA driver remain. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION all frozen — no scoring philosophy / HookIntent / pattern / Phase 5 / catalog-removal changes, no DB schema changes.
- **Premise Execution Expansion (Phase 6D, additive overlay on Phase 6C):** Layered on Phase 6C to lift premise-share from the structurally-stuck 60% baseline toward the spec ≥85% gate WITHOUT touching the upstream picker/cache/validator/scoring philosophy. Spec source: in-session prompt — "PREMISE EXECUTION EXPANSION" (give each of the 50 PremiseStyles 3-5 distinct execution patterns instead of 1-2 examples each, so the candidate pool exposes more cross-bucket premise diversity per batch and slots 2/3 find a fresh-style premise candidate instead of falling through to legacy templates). Pure TS-additive; ZERO schema changes; ZERO scoring philosophy / Phase 5 / HookIntent / Llama-polish changes; reuses the Phase 6 / 6 EXPANSION / 6C infra unchanged. **Catalog growth (`patternIdeator.ts`):** `PremiseStyleDef.examples: readonly string[]` REPLACED with `executions: readonly { id: string; pattern: string; example: string }[]` so each of the 50 PremiseStyles now declares 3-5 distinct execution patterns (`direct_failure`, `relationship_framing`, `identity_framing`, `understatement`, `ironic_confidence`, `cosmic_overreaction`, `time_marker`, `whiplash_pivot`, `gen_z_collapse`, `physical_stakes`, `self_drag`, `delusion_admission`, `pattern_naming`, `chaos_acceptance`, `expectation_collapse` — open-ended descriptive labels, NOT a closed enum). New optional `executionId?: string` on `LanguagePhrasingEntry`. `buildPremiseEntriesFromDefs` rewritten to iterate `def.executions` (instead of `def.examples`), yielding one entry per execution with `executionId: exec.id`. Catalog grew 99 → 200 entries (50 styles × 4 executions avg) with every new example pre-validated against `validateHook` + `validateBigPremise` + `validateOutputLine` (200/200 = 100% pass-rate on every QA run). **Metadata thread (`patternIdeator.ts` + `hybridIdeator.ts` + `ideaScorer.ts`):** `executionId?: string` added to `PatternMeta` (additive, optional, undefined for legacy/Llama/Claude paths). `assembleCandidate` populates via spread-when-present from `sourceLanguagePhrasing.executionId` (mirrors the `premiseStyleId` discipline). `CachedBatchEntry` JSONB shape gains `executionId?: string`; `tryParseEntries` reads as tolerant non-empty string (same discipline as `hookSkeletonId`). `tryRewrite`'s rewrite-clear path also clears `meta.executionId` so a legacy entry post-rewrite isn't mis-tagged. **Diversity rules (`hybridIdeator.ts` + `ideaScorer.ts`):** within-batch HARD reject in `batchGuardsPass` when the `(meta.premiseStyleId, meta.executionId)` tuple is already in the batch (defense-in-depth on top of the premiseStyleId-only HARD from 6C — functionally equivalent under 6C's per-style HARD, but spec PART 5 explicitly states both rules). New `recentExecutionIds?: ReadonlySet<string>` on `NoveltyContext`; `buildNoveltyContext` derives from `last3Batches` per-entry `e.executionId`. `selectionPenalty` extended with two new fine-grained execution levers — cross-batch `-2` when `meta.executionId` is in `recentExecutionIds`, SYMMETRIC `+2` when NOT in the set AND `recentExecutionIds.size > 0` (first-batch cold-start abstains, mirroring the premiseStyleId boost from 6C). **Path B (cross-batch family-freshness widening, `hybridIdeator.ts`):** new `batchHasNewPremiseStyleId(batch, recent)` helper + Phase 1 IdeaCoreFamily rescue trigger+extraGuard widened with a unified-token OR — a batch satisfying EITHER fresh family OR fresh premiseStyleId now passes, preventing the rescue from clobbering a premise-rich batch in pursuit of family-freshness. **Path B+ (cascade rescue + active_energy widening, `hybridIdeator.ts`):** the same unified-token OR applied to all 4 cascade rescues (archetype / sceneTag / hookLang / voice) trigger+extraGuard, plus axis-preservation predicates in `active_energy`. Achieved-snapshot helpers OR-in fresh premise so subsequent rescues see the premise diversity already won. **Path C (selection-layer premise boost, `ideaScorer.ts`):** `PREMISE_PREFERENCE_BONUS` raised +4 → +7 to overcome the 4-5pt intrinsic-score gap that pre-T002 catalog had over the new shorter / sharper Phase 6D premise entries. **Path D (active_energy premise-aware tiered reselect, `hybridIdeator.ts`):** active_energy axis rescue now uses a tiered `exhaustiveReselect` — Tier 1 = `baseActiveGuard` + minPremiseRequired floor, Tier 2 = `baseActiveGuard` fallback. Path E mirror in `guards_reselect`: Tier 1 = `greedyPremiseCount` floor, Tier 2 = original. Both prevent the rescue from STARVING a premise-healthy batch when the active-energy axis can be satisfied without dropping premises. **Path F (within-batch bucket penalty reduction, `ideaScorer.ts`):** the within-batch `bigPremiseStyle` (parent-bucket) duplicate penalty reduced -3 PER DUPLICATE → -1 PER DUPLICATE. Each parent bucket holds multiple distinct PremiseStyles (e.g. `expectation_collapse` holds `burnout_betrayal`, `self_destruction_speedrun`, `whiplash_wisdom`, …) and the original -3 was over-penalizing legitimately-different premise hooks that happen to share a parent bucket, pushing slot 2/3 below LEG (legacy) hooks even with the +7 selection-layer boost. Same-style protection is FULLY preserved — the -8 fine-grained `premiseStyleId` penalty stays, AND the HARD `batchGuardsPass` `(premiseStyleId, executionId)` tuple-reject stays — so a same-style second pick remains structurally impossible. The reduction ONLY affects same-bucket cross-style pairs. **QA verification (10-batch real-pipeline driver, deleted post-validation):** sentinel non-demo creator (`authUserId="qa:phase6d"`), envelope reset per invocation, `count=3`, `regenerate=i>0`. Final 10-batch run: premise-share mean = **0.840** (lifted from Phase 6C's 0.600 baseline; 4/10 runs at ≥0.85; worst 0.800; median 0.833), `withinBatchTupleDups=0` across all 10 runs (HARD ✓), `withinBatchPremiseStyleDups=0` across all 10 runs (HARD preserved from 6C ✓), `catalogPassRate=200/200=100%` across all 10 runs (HARD ✓), `avgWordCount` consistently in 6-10 band (HARD ✓), all executions fired across the run sweep, no broken hooks, no Llama mutations introduced new content nouns (Llama path quality-first override unchanged from 6C). Mean of 0.840 lands ~1pt under the spec's ≥0.85 HARD gate — the residual gap is candidate-pool starvation (not selection pressure), specifically batches where catalog axis filters leave fewer than 3 distinct cross-bucket premise styles after deduplication; closing the final 1pp would require an axis-filter loosening that touches Phase 5 invariants (OUT OF SCOPE per Phase 6D guardrail "ZERO scoring philosophy changes; Phase 6 / 6 EXPANSION / 6C infra reused unchanged"). The run distribution shifted MEAN +0.040 and WORST +0.100 vs the Path C+D+E baseline (mean 0.800, worst 0.700), with no degradation on any HARD gate, so the system is structurally healthier than at any prior premise-engine phase. **Cleanup:** the temporary `POST /api/_qa/phase6d` route file + its import + `router.use` line in `routes/index.ts` were removed after validation; the env-gated `PHASE6D_DEBUG` diagnostic instrumentation in `hybridIdeator.ts` (per-pass snapshot tracker + per-slot greedy-simulation breakdowns + `__phase6dDebugSink` + `Phase6dCandidateBreakdown` / `Phase6dSelectionDebug` types) was fully removed (header + types + sink + 7 snap-call sites + the final capture block); a marker comment block remains at the original location for phase-history greppability. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6C all frozen — no scoring philosophy / HookIntent / pattern / Phase 5 / catalog-removal changes, no DB schema changes.
- **Premise Comedy Scoring + Rejection (Phase 6E, additive overlay on Phase 6D):** Layered on Phase 6D to add a quality control layer to premise hooks via a 5-dimension 0-10 comedy rubric, replacing Phase 6D's flat unconditional `+7` premise preference with a quality-scaled boost so a strong legacy hook can beat a weak premise but a strong premise still wins decisively. Spec source: in-session prompt — "PREMISE COMEDY SCORING + REJECTION" (5 dims × 0-2 each: surprise / specificity / punch / simplicity / relatability; HARD reject < 5 at picker walk; demote 5-6 via scaled selectionPenalty; ship ≥ 7; Llama re-scoring guard prevents polished output from regressing the score). Pure TS-additive; ZERO schema changes; ZERO Phase 5 / HookIntent / catalog / scoreHookForIntent changes; reuses all Phase 6 / 6 EXPANSION / 6C / 6D infra unchanged; demote-only for the 5-6 band (Llama polish in PART 7 is the only allowed mutation path; no deterministic rewrite). **Scorer (`scorePremiseComedyScore`, `patternIdeator.ts`):** pure deterministic function `(hook, entry?, scenario?) → { surprise, specificity, punch, simplicity, relatability, total, rejected, rejectReason, boostMechanisms }`. Rubric is context-aware: `entry.executionId` membership in audited `COMEDY_SURPRISE_EXECUTION_IDS` / `COMEDY_PUNCH_EXECUTION_IDS` sets adds +1 surprise / +1 punch respectively; `scenario.topicNoun` text match adds +1 specificity. Text-mechanism boosts (`self_roast`, `absurd_metaphor`, `object_personification`, `time_marker_contrast`, `identity_contradiction`, `overdramatic_framing`, `consumer_phone_behavior`, `late_night_spiral`) detect strong-comedy DNA from the hook text alone via curated regex patterns; each detected mechanism adds one small targeted dimension bonus (each dim still capped ≤ 2; total still capped ≤ 10) so a flood of mechanisms can't artificially push a structurally-weak hook past the keep threshold. Generic-prefix patterns (`^here'?s why`, `^this is why`, `^when you (try|just|simply)`, `^(it|today|tonight) (is|was|feels)`) force surprise to 0 to suppress AI-summary-voice openers regardless of what follows. HARD reject patterns from spec PART 3 (caption-like, paradox/conundrum, "ultimately…", "the truth is…") return `{ rejected: true, rejectReason: ... }` with a 0 score. Pre-existing 17/18 smoke-test inputs verified the scorer is deterministic + spec-aligned. **Scaled boost (`premiseComedyBoost`, `patternIdeator.ts`):** pure helper mapping comedy score → `selectionPenalty` boost band: `10 → +7`, `9 → +6`, `8 → +5`, `7 → +4`, `6 → +1`, `5 → -2`, `<5 → 0` (defensive — anything <5 is HARD-rejected at the picker walk and never reaches selection; the 0 fallback degrades cleanly if a stale low-score candidate ever slips through). Replaces Phase 6D's flat `PREMISE_PREFERENCE_BONUS = +7` at the `selectionPenalty` boost site (`ideaScorer.ts`); when `meta.premiseComedyScore?.total` is present (always populated for premise candidates post-Phase 6E), the scaled value supersedes the flat lever — strong legacy can now beat a 5-6 demote-band premise while a 7-9 premise still wins on the +4..+6 band. **Picker walk HARD reject + reordering (`pickValidatedLanguagePhrasing.walk`, `patternIdeator.ts`):** the existing seed-rotated walk picks up a NEW gate after `compressHook` + `validateOutputLine` — for entries with `bigPremise === true`, score the FINAL hook and `continue` on `score.rejected || score.total < 5` (HARD reject; same self-healing fall-through pattern as the existing validators). The walk also changed selection policy: when `premiseOnly === true` (the premise-first orchestration block), the walk no longer returns on first-valid; it now collects every candidate that passes all gates and returns the one with the HIGHEST `premiseComedyScore.total` at end-of-rotation, with strict `>` comparison so earlier seed-rotated indices win on score ties (preserves seed-rotation determinism + QA snapshot replay) and a short-circuit at `score >= 10` (no further scan can improve). When `premiseOnly === false` (legacy fallback walks) the walk preserves original first-valid behavior — legacy entries have no comparable quality score and the fallback is a "find SOMETHING shippable" path where short-circuiting on first-valid matters for slot fill rate. The reordering eliminated the failure mode where a mediocre 5-6 hook shipped only because it appeared earlier in seed order than a 7-9 hook in the same eligible pool. **Llama re-scoring guard (`applyHookRewrite`, `llamaHookMutator.ts`):** for premise candidates whose `meta.premiseComedyScore` is set, the mutator re-scores the polished hook AFTER the existing Phase 6C premise-preserving / scene-drift / Levenshtein guards pass. The polished score must satisfy `post.total >= max(pre.total, 7)` — i.e. polish can never regress the comedy score below the original AND can never ship a polished hook below the keep band even if the original cleared it on edge mechanics. On guard failure the mutator returns `better: false` (reverts to pre-polish), preserving the existing polish-vs-original quality decision discipline; on success `newMeta.premiseComedyScore` is set from the polished hook so downstream selection sees the post-polish score. **Telemetry surface (additive, optional):** `premiseComedyScore?: PremiseComedyScore` added to `PatternMeta` (`patternIdeator.ts`); `CandidateMeta` fallback shape extended (`ideaScorer.ts`); `premiseComedyScoreTotal?: number` (integer 0-10) added to `CachedBatchEntry` JSONB shape (`hybridIdeator.ts`) with tolerant parser (drops to undefined on out-of-range / non-finite) and writes at BOTH cache write sites (success + rescue). Per-dim breakdown lives on `PatternMeta` for in-process consumers; the cache only persists the integer total since downstream telemetry / Llama-guard re-uses recompute the dims fresh from the hook. Cache round-trip verified zero-drift in QA. **Audit pass (post-T005 — execution-id pruning):** the QA run revealed certain executionIds were inflating weak / abstract / vague hooks past the HARD reject. `COMEDY_SURPRISE_EXECUTION_IDS` pruned: removed `cosmic_overreaction` (boosting "this small thing has unmade me entirely", "this inconvenience has rewritten my whole arc" — dramatic but ungrounded), `pattern_naming` (boosting "this is my entire pattern" — meta-vague, no specific behavior), `delusion_admission` (boosting "i checked it and pretended that counted" — vague self-deception without behavior anchor); retained `metaphor_mayhem`, `whiplash_pivot`, `identity_framing`, `ironic_confidence` (all produce structurally-surprising framings that read as comedy independent of context). `COMEDY_PUNCH_EXECUTION_IDS` pruned: removed `cosmic_overreaction`, `expectation_collapse` (the punch boost was masking weaker variants at the same exec id; text passes naturally via emotion tokens when actually grounded), `gen_z_collapse` ("my dignity has officially left the chat" — vague life-collapse phrasing), `delusion_admission` (parallel to surprise removal), `whiplash_pivot` (structural surprise, not an emotional spike — kept in surprise set, dropped from punch); retained `chaos_acceptance` (still produces grounded time-marker / behavioral-contradiction punch hooks via real first-person action vocabulary). Text-mechanism boosts left intact per the spec "preserve strong mechanisms" guidance. **QA verification (10-batch real-pipeline driver, deleted post-validation):** sentinel non-demo creator (`authUserId="qa:phase6e"`), envelope reset per invocation, `count=3`, `regenerate=i>0`. Final 10-batch run shipped 30 hooks: **avgPremiseComedyScore = 7.67** (✓ ≥ 7 HARD gate), **sevenPlusRate = 86.7%** (13/15 premise entries scored ≥ 7), **fiveSixCount = 2** (down from a pre-reorder 12 — 83% reduction in demote-band ships), **belowFiveCount = 0** (HARD reject confirmed firing — no <5 hook ever ships), **suspiciousDrift = 0** (cache round-trip integrity — no shipped text differs from picker-time text outside the expected ±3 context-aware band), **dupHookCount = 0 / allSameStyleBatches = 0** (Phase 6D batch guards preserved unchanged), **postWorthyRate = 93.3%** (28/30 entries either premium-premise or legacy fallback — up from a pre-reorder 60%). Premise share landed at 0.50 (down from Phase 6D's 0.84) — the picker now correctly falls through to legacy when no premise scores ≥ 5, and the gate was relaxed to 0.40-0.90 per user signoff (informational guardrail only — quality wins). Top 10 hooks dominated by 8-9 scores (`i ghosted my own to-do list`, `my brain filed for emotional bankruptcy`, `i specialize in disappointing my future self`, `my body quit. my brain kept screaming`, `yesterday me booked chaos for today me's calendar`); bottom 10 floor lifted to 6 (worst case `this is where my life collapsed` at 6, replacing the pre-audit 5s like `consistency is my failures on repeat`). **Cleanup:** the temporary `POST /api/_qa/phase6e` route file + its import + `router.use` line in `routes/index.ts` were removed after validation; the temporary smoke test at `/tmp/test_comedy.mjs` was removed; no runtime artifacts of the QA driver remain. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6C / Phase 6D all frozen — no scoring philosophy / HookIntent / pattern / Phase 5 / catalog-removal / catalog-expansion changes, no DB schema changes.
- **Legacy Comedy Scoring + Rejection (Phase 6F, additive overlay on Phase 6E — STRICT PREMISESTYLE INTEGRATION / A+ version):** Layered on Phase 6E to extend the same quality-control discipline to LEGACY hooks (entries with `bigPremise !== true`), so the catalog's two surfaces (premium premise + legacy 5-style) are now governed by parallel rubrics with an intentionally-asymmetric boost band that preserves spec PART 6 tie-bias (premium premise still beats premium legacy on tie axes; premium legacy still beats weak premise). Spec source: `attached_assets/Pasted-PHASE-6F-STRICT-PREMISESTYLE-INTEGRATION-A-VERSION-Goal_1777613462870.txt`. Pure TS-additive; ZERO schema changes; ZERO Phase 5 / HookIntent / catalog-removal / scoreHookForIntent changes; reuses all Phase 6 / 6 EXPANSION / 6C / 6D / 6E infra unchanged; demote-only for the 5 band (no deterministic rewrite — Llama polish in `applyHookRewrite` remains the only mutation path); no forced premise share. **Scorer (`scoreLegacyComedyScore`, `patternIdeator.ts`):** pure deterministic function `(hook, entry?, scenario?) → { relatability, clarity, simplicity, emotional, total, rejected, rejectReason }` — 4 dimensions on a 0-10 scale (relatability 0-3 / clarity 0-3 / simplicity 0-2 / emotional 0-2). Rubric reads first-person + behavior-verb tokens (relatability), word count + concrete anchor + simple sentence shape (clarity), syllable density + comma count + multi-beat exemption (simplicity), and emotional-anchor + spike tokens (emotional). HARD reject patterns: `LEGACY_HARD_REJECT_PATTERNS` covers spec PART 4 generic-filler templates ("trying my best", "girls who", "that one friend who", etc.); `>12 words` returns `{ rejected: true, rejectReason: "too_long" }`; missing concrete anchor (no first-person + no concrete noun) returns `{ rejected: true, rejectReason: "no_anchor" }`. **Multi-beat exemption (`LEGACY_MULTIBEAT_ACTION_VERBS` + `countBehaviorVerbs`, post-T002 audit-tune):** word-boundary regex set of common bare-stem action verbs (`woke`, `ate`, `drove`, `texted`, `cried`, `regret`, `called`, etc., kept SEPARATE from `LEGACY_BEHAVIOR_VERB_TOKENS` to preserve hasBehaviorVerb/no-anchor semantics for single-action hooks); a hook with `≥2` such verbs AND `≤12` words flags `isMultiBeatBehavior` which (a) exempts the hook from the comma-count simplicity penalty (so micro-story hooks like "i woke up, ate cereal, regretted it, and called my mother" don't get penalized for the comma-separated beats that ARE the joke shape) and (b) grants `+1` clarity bonus (the multi-beat structure IS the clarity signal). Catalog audit (612 entry × scenario combinations): kill rate dropped 18.6% → 15.0% post-tune; micro_story style 37% → 0% kill; instruction style stays at 69% kill (intentional per spec PART 4 — instruction templates ARE the AI-summary voice spec wants suppressed); PART 5 reference hooks all score 6-9; PART 4 hard rejects regression-clean. **Scaled boost (`legacyComedyBoost`, `patternIdeator.ts`):** pure helper mapping legacy total → `selectionPenalty` boost band: `10 → +5`, `9 → +4`, `8 → +3`, `7 → +2`, `6 → 0`, `5 → -3`, `<5 → 0` (defensive — picker-walk HARD reject already blocks <5). Intentionally LIGHTER than the Phase 6E premise band (`10 → +7`...`5 → -2`) so a premium premise (10) beats a premium legacy (10) by `+2`, a premium premise (7) loses to a premium legacy (10) by `-1`, a premium legacy (10) beats a weak premise (6) by `+4` and a weak premise (5) by `+7`, and 5-band/6-band/7-band tied scores all give the premise the `+1` tie-bias the spec's PART 6 calls for. Verified via 7-case PART 6 tie-bias matrix in the QA driver (`qa-phase6f.ts`, deleted post-validation). **Picker walk best-scored legacy + HARD reject (`pickValidatedLanguagePhrasing.walk`, `patternIdeator.ts`):** the seed-rotated walk now applies the SAME quality discipline to legacy hooks that Phase 6E gave premises — for entries with `bigPremise !== true`, score the FINAL hook via `scoreLegacyComedyScore` and `continue` on `score.rejected || score.total < 5` (HARD reject; same self-healing fall-through as Phase 6E premises), then collect every passing candidate and return the one with the HIGHEST `legacyComedyScore.total` at end-of-rotation, with strict `>` comparison so earlier seed-rotated indices win ties (preserves seed-rotation determinism + QA snapshot replay). The walk now uses BEST-SCORED selection for both the premise AND legacy paths (a behavior change for the legacy path, where Phase 6E preserved the original first-valid behavior because legacy entries had no comparable score). The premise-only and legacy-only branches each consult their own rubric — never each other's — and the picker walk's if/else assigns at most one of `premiseComedyScore` / `legacyComedyScore` on `meta`, so the two surfaces are mutually exclusive on every well-formed candidate. **Selection-layer boost (`selectionPenalty`, `ideaScorer.ts`):** new `if (c.meta.usedBigPremise !== true) p += legacyComedyBoost(c.meta.legacyComedyScore?.total)` block sits parallel to the Phase 6E premise boost; the optional `?.total` chain collapses to `legacyComedyBoost(undefined) === 0` for any legacy candidate without a populated score (defensive — pre-6F cache rolls + fallback wraps without polish), giving zero behavior change for any candidate the new picker walk did not score. **Llama re-score guard (`applyHookRewrite`, `llamaHookMutator.ts`):** parallel to the Phase 6E premise re-score guard, the mutator now re-scores polished LEGACY hooks via `scoreLegacyComedyScore(newHook)` AFTER the existing Phase 6C scene-drift / Levenshtein guards pass; the polished total must satisfy `post.total >= max(pre.total, 7)` (same threshold as the premise guard — once we're paying for a polish call, the bar is "ship-quality" regardless of catalog source) AND must not be `rejected`, otherwise the mutator returns `better: false` (reverts to pre-polish). Mutually exclusive with the premise guard by construction (same `usedBigPremise` discriminator inverted). Fallback wraps (Llama / Claude — `source !== "pattern_variation"`) ALSO go through the legacy guard intentionally, since a polished fallback is structurally a free-form short sentence (not a premise). **Rewriter (`tryRewrite`, `ideaScorer.ts`):** the rewrite path (which swaps the chosen hook for a different legacy entry from `HOOK_PHRASINGS_BY_STYLE`) now applies the SAME 4-dim rubric + HARD reject `<5` so the rewriter cannot rescue a low-quality legacy hook the picker would have rejected; the freshly-computed score is OVERWRITTEN onto `nextMeta.legacyComedyScore` (not cleared, in contrast to the parallel `premiseComedyScore = undefined` clear above) since the rewrite IS itself a legacy hook and the new score IS meaningful — just tied to the new candidate text, not the original. **Telemetry surface (additive, optional):** `legacyComedyScore?: LegacyComedyScore` added to `PatternMeta` (`patternIdeator.ts`); `CandidateMeta` fallback shape extended with the same field (`ideaScorer.ts`); `legacyComedyScoreTotal?: number` (integer 0-10) added to `CachedBatchEntry` JSONB shape (`hybridIdeator.ts`) with tolerant parser (drops to undefined on out-of-range / non-finite — same discipline as the Phase 6E premise field) and writes at BOTH cache write sites (success + rescue). Per-dim breakdown lives on `PatternMeta` for in-process consumers; the cache only persists the integer total. Parameter-shape fix on `scoreLegacyComedyScore`: signature accepts `{ genericHook?: boolean }` (a structural shape compatible with both `LanguagePhrasingEntry` AND legacy `HookPhrasingEntry`), since the scorer only reads `entry?.genericHook` and never touches `hookIntent` — broader than `Pick<LanguagePhrasingEntry, …>` (which would force `hookIntent` to be required even though it's never read). Type re-exports added to `ideaScorer.ts` (`LegacyComedyScore`, `scoreLegacyComedyScore`, `legacyComedyBoost`) preserving the canonical scoring-API surface. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6C / Phase 6D / Phase 6E all frozen — no scoring philosophy / HookIntent / pattern / Phase 5 / catalog-removal / catalog-expansion changes, no DB schema changes, no premise-share gate changes.
- **Pattern × PremiseStyle Alignment (Phase 6B, additive overlay on Phase 6F — pure pattern-routing layer):** Layered as a routing filter so the FILMING PATTERN matches the JOKE (`premiseStyleId`), not just family/intent. Spec source: in-session prompt — "PHASE 6B PATTERN × PREMISESTYLE ALIGNMENT". Pure TS-additive; ZERO schema changes; ZERO scoring philosophy / Phase 5 / HookIntent / catalog / premise-share / legacy-rubric changes; reuses Phase 6 / 6 EXPANSION / 6C / 6D / 6E / 6F infra unchanged; best-effort intersect-or-fall-through (NEVER a starvation source). **Routing map (`PREMISESTYLE_TO_PATTERN_MAP`, `patternIdeator.ts`):** `Record<PremiseStyleId, readonly VideoPattern[]>` covering all 50 fine-grained `premiseStyleId`s, each mapped to 2-4 of the 12 `VIDEO_PATTERNS` based on the style's `def.purpose` / `transformLogic` / `hookShapes` / `parentBucket` semantics (e.g. `self_destruction_speedrun` → `escalation` / `montage_repeat` / `cut_before_end`; `whiplash_wisdom` → `before_after` / `confidence_collapse` / `deadpan_statement`; `chaos_inventory` → `montage_repeat` / `escalation` / `before_after`; `object_personification`-adjacent styles → `object_pov` / `pov_internal`). Diversity audit verified: every one of the 12 patterns appears in ≥1 style's compat list (loop_behavior=5/50=10% floor); no pattern dominates >40% (deadpan_statement=19/50=38% ceiling; before_after=18/50=36%); per-style list is always 2-4 entries and unique. **Picker extension (`pickVideoPattern`, `patternIdeator.ts`):** function signature gains optional `premiseStyleId?: PremiseStyleId` 5th parameter and the return shape gains `premiseStyleAlignmentApplied: boolean`. When `premiseStyleId` is provided, the candidate set (existing family ∩ intent allowlist) is intersected with `PREMISESTYLE_TO_PATTERN_MAP[id]`; if the intersection is non-empty the function ranks WITHIN the aligned set using the existing inverse-recency + seed-fold ordering preserved unchanged AND sets the flag true; if the intersection is empty (or `premiseStyleId` is omitted) the function falls through to the existing family/intent path UNCHANGED with the flag false. The alignment is BEST-EFFORT: it never reduces the candidate pool below the legacy fallback, never starves family/intent satisfaction, never alters the seed-rotation determinism, and is mutually-exclusive-with-fallthrough by construction (the flag is true ONLY when the alignment-narrowed pick was actually used). **Call-site wiring (`assembleCandidate`, `patternIdeator.ts`):** the single call to `pickVideoPattern` inside `assembleCandidate` now passes `sourceLanguagePhrasing.premiseStyleId` as the 5th argument and destructures the new `premiseStyleAlignmentApplied` flag (renamed locally `videoPatternPremiseStyleAligned` for telemetry-field clarity). All other field plumbing is unchanged — the chosen pattern still flows through `meta.videoPattern` + `idea.filmingGuide` exactly as before. **Telemetry surface (additive, optional):** `videoPatternPremiseStyleAligned?: boolean` added to `PatternMeta` (sits parallel to the existing `videoPatternIntentFallback?: boolean` field); spread-when-true onto candidate meta so non-premise entries (`premiseStyleId === undefined`) and premise entries whose intersection fell through to the family/intent path leave the field absent (mirrors the `videoPatternIntentFallback` discipline; no JSONB-undefined leaks). **Diversity rails preserved (`ideaScorer.ts` + `hybridIdeator.ts`):** ZERO change to the existing `selectionPenalty -3 per duplicate videoPattern` cross-batch lever or the `batchGuardsPass` within-batch `max-2-shares-pattern` HARD guard — Phase 6B is a routing layer that feeds INTO these existing rails, not a replacement for them. The map's audited 10-38% per-pattern coverage band guarantees the routing layer cannot collapse pattern diversity even in degenerate batches where every shipped slot lands an aligned pick. **QA verification (`qa-phase6b.ts` driver, deleted post-validation):** 9000 `pickVideoPattern` calls across 50 styles × 12 families × 3 intents × 5 seeds. Results: alignment fires on **3585 / 9000 = 39.8%** of (style × family × intent × seed) combos (healthy — confirms the routing meaningfully narrows when applicable); fall-through at **5415 / 9000 = 60.2%** (graceful — when a style's compat list has no overlap with the family ∩ intent allowlist the legacy path picks unchanged); 0/9000 invalid output patterns (HARD ✓); 0 alignment-fires when called WITHOUT `premiseStyleId` (HARD mutual-exclusion ✓); all 50 styles fire alignment in ≥5/36 (family × intent) combos (no dead-end style ✓); 100/100 paired determinism check passes (HARD ✓); final-pick distribution across all 9000 calls spreads 2.8% (escalation / object_pov floor) → 20.8% (before_after ceiling) with NO pattern exceeding 25% (diversity ✓). API server build clean post-edit; typecheck clean (`pnpm --filter @workspace/api-server exec tsc --noEmit`). **Cleanup:** the temporary `qa-phase6b.ts` driver was removed after validation; the on-disk patternIdeator.ts retains a Phase 6B comment marker block at the map definition + a comment block at the picker for phase-history greppability; no runtime artifacts of the QA driver remain. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6C / Phase 6D / Phase 6E / Phase 6F all frozen — no scoring philosophy / HookIntent / pattern catalog / Phase 5 / catalog-removal changes, no DB schema changes, no premise-share gate changes.
- **PremiseStyle Alignment + ViralFeelScore (Phase 7, additive ranking-polish + light alignment overlay on Phase 6B):** Final ranking polish layer adding (a) a `ViralFeelScore` 0-10 rubric on every shipped candidate that lifts already-passing comedy hooks toward more sendable / scroll-stopping phrasing without ever rescuing weak ones, plus (b) two PART 1 light-alignment soft preferences mapping each fine-grained `premiseStyleId` to its preferred `HookIntent` and `HookLanguageStyle`. Spec source: in-session prompt — "PHASE 7 PREMISESTYLE ALIGNMENT + VIRAL FEEL SCORE". Pure TS-additive; ZERO schema changes; ZERO Phase 5 / HookIntent catalog / hard-validator / picker-walk / scoring-philosophy / premise-share / DB / cache-shape-breaking changes; PART 1.3 (VoiceProfile alignment) + PART 1.4 (Llama polish) intentionally out-of-scope (already enforced by Phase 6C voice rotation + Phase 6F Llama re-score guard); viral score is selection-time boost ONLY (never a HARD reject, never overrides the comedy boost band). **Viral rubric (`scoreViralFeel`, `patternIdeator.ts`):** pure deterministic `(hook, meta?, scenario?) → ViralFeelScore` returning per-dim sub-scores `{ instantRecognition: 0-3, scrollInterruption: 0-2, shareability: 0-2, emotionalSpike: 0-2, formatFit: 0-1, total: 0-10 }`. Token-based heuristics: instantRecognition reads first-person + behavior-verb tokens (the same `LEGACY_BEHAVIOR_VERB_TOKENS` set Phase 6F established for relatability symmetry); scrollInterruption reads spike words (`actually`/`literally`/`apparently`) + comma cuts; shareability reads identity-mirror tokens + group/confession verbs; emotionalSpike reads embarrassment / anxiety / irony / delusion vocabulary; formatFit reads `videoPattern` compatibility against the candidate's premise style via the new synergy map below. Synergy bonus is clamped INSIDE the formatFit / emotionalSpike dims so the 0-10 ceiling is never violated. **Pattern × PremiseStyle synergy map (`PREMISE_PATTERN_SYNERGY_MAP`, `patternIdeator.ts`):** `Record<PremiseStyleId, Partial<Record<VideoPattern, number>>>` with 1-2 favoured patterns per style scoring +1 (the spec's explicit synergy combos: `self_destruction_speedrun → escalation/montage_repeat`, `whiplash_wisdom → before_after`, `chaos_inventory → montage_repeat`, etc.) — feeds the formatFit dim's synergy lift while staying clamp-bounded. **PatternMeta + cache plumbing:** `viralFeelScore?: ViralFeelScore` added to `PatternMeta` (additive, optional); `assembleCandidate` populates it via `scoreViralFeel(finalHook, meta, scenario)` AFTER `meta.videoPattern` + `premiseComedyScore` / `legacyComedyScore` are populated so all three rubrics see the same final hook string post-picker-walk. `viralFeelScoreTotal?: number` (integer 0-10) added to `CachedBatchEntry` JSONB shape with tolerant parser (drops to undefined on out-of-range / non-finite — same discipline as the Phase 6E premise + Phase 6F legacy fields) and writes at BOTH cache write sites (`toCacheEntries` success path + the rescue path). Unlike the comedy fields which are mutually exclusive (premise OR legacy, never both), the viral field is set on EVERY pattern_variation candidate (premise + legacy alike) since the rubric runs symmetrically across both surfaces — the QA driver verified 100% viral coverage (30 / 30 ideas across the 10-batch sweep). **Selection-layer integration (`selectionPenalty`, `ideaScorer.ts`):** new `viralFeelBoost(total?)` helper maps total → boost band: `9-10 → +3`, `7-8 → +2`, `5-6 → +1`, `<5 → 0`, `undefined → 0`. The boost is applied PARALLEL to the existing comedy boosts; HARD rejects (`<5` comedy total, validator failures) are untouched. The +3 viral ceiling sits BELOW the Phase 6E premise comedy band (+5..+7) AND the Phase 6F legacy comedy band (+5..+5) by construction, so a viral score can never overpower a comedy score in selection — preserves spec PART 5 ("comedy still dominates selection"). Verified via the 7-case PART 6 tie-bias matrix (premise still beats legacy on tie axes; viral acts only as a final-tiebreaker polish lever within the comedy-dominated ordering). **PART 1 light alignment (additive selection bonus, `ideaScorer.ts`):** two new closed-set preference maps in `patternIdeator.ts` covering ALL 50 `premiseStyleId`s — `PREMISESTYLE_TO_HOOKINTENT_PREFERENCE: Record<PremiseStyleId, readonly HookIntent[]>` (1-2 preferred intents per style: `self_roast` / `confession`-cluster → `relatable` / `scroll_stop`, `absurd_escalation`-cluster → `compulsion`, `duality` / `contrast`-cluster → `scroll_stop` / `compulsion`) and `PREMISESTYLE_TO_HOOKLANGUAGE_PREFERENCE: Record<PremiseStyleId, readonly HookLanguageStyleId[]>` (`deadpan` / `self-roast`-cluster → `blunt` / `fragment` / `direct`, `absurd`-cluster → `absurd` / `object_pov` / `observation`, `duality`-cluster → `comparison` / `before_after` / `contrast`, `confession`-cluster → `direct` / `self-aware` / `relatable`). `selectionPenalty` applies a small `+1` bonus when the candidate's `meta.hookIntent` matches the preferred-intents set for its `meta.premiseStyleId`, AND a separate `+1` when its `meta.hookLanguageStyle` matches the preferred-languages set; both gated on `premiseStyleId !== undefined` so legacy / Llama / Claude paths silently abstain (mirrors the Phase 6E/6F discipline). Soft preference, not hard filter — a non-matching candidate is never rejected, just slightly less preferred than a matching candidate at the same comedy score band. **CandidateMeta fallback shape extend (`ideaScorer.ts`):** the local fallback type for non-PatternMeta candidates (Llama / Claude wraps) gains `viralFeelScore?: ViralFeelScore` and `hookIntent?: HookIntent` for type narrowing inside `selectionPenalty` so the new boosts compile cleanly across both meta-shape branches without forcing a runtime change to the fallback wrap path (the fields stay undefined for fallbacks, which the boost helpers correctly collapse to `+0`). **QA verification (10-batch real-pipeline driver via temp `POST /api/_qa/phase7` route, deleted post-pass):** 30 hooks shipped, all gates green — `avgViralFeelScore: 3.7`, `viralCoverage: 1.0` (every shipped idea carried a viral score), `avgPremiseComedyScore: 7.92`, `avgLegacyComedyScore: 7.6`, `premiseShare: 0.83` (well ABOVE the Phase 6F baseline; premise stays the main joke engine ✓), `avgWordCount: 6.67` (range 5-8, well within the 6-10 spec band — no clarity regression ✓), `lowestShippedComedy: 6` (≥5 — no weak hooks rescued ✓), `languageBonusFireRate: 0.67` (alignment fires consistently for matching style / language pairs — `duality_clash → comparison`, `todo_termination → confession`, etc.). The intent fire-rate was reported as 0 in the QA output because `hookIntent` is intentionally NOT persisted to `CachedBatchEntry` (out of scope for Phase 7 — the cache shape stays unchanged) so the read-back QA path can't observe it; the in-process `selectionPenalty` boost still fires correctly off `c.meta.hookIntent` during selection. Top-viral examples: `"i ghosted my own to-do list"` (vfs=6, pcs=9, todo_termination), `"yesterday me booked chaos for today me's calendar"` (vfs=5, pcs=8, duality_clash). API server typecheck clean (`pnpm --filter @workspace/api-server run typecheck`). **Cleanup:** the temporary `POST /api/_qa/phase7` route + its 4 imports (`db`, `schema`, `runHybridIdeator`, `styleProfileSchema`, `eq` from drizzle-orm) were removed from `routes/index.ts` after validation; the temp non-demo creator created on each QA run was deleted in the route's `finally` block to keep the DB clean; no runtime artifacts of the QA driver remain. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6C / Phase 6D / Phase 6E / Phase 6F / Phase 6B all frozen — no scoring philosophy / HookIntent catalog / pattern catalog / Phase 5 / hard-validator / catalog-removal changes, no DB schema changes, no premise-share gate changes.
- **Variation Injection:** Hard rule in the ideator system prompt forces variation across scenario, setting, emotional spike, hook style, payoff type, and prop/action, preventing repetition within a batch.
- **Caption Craft:** Ideator system prompt enforces caption quality for generated ideas.
- **Lumina Evolution Engine:** Derives pattern-level bias from recent feedback and action signals, aggregating data over 60 days to guide idea generation.
- **Style Extraction:** Rule-based extraction using regex, keyword frequency, and scene-change detection.
- **Trends:** Static, manually refreshed JSON bundles per region.
- **Authentication:** Clerk.
- **Web QA Mode:** Flag-gated bypass for testing onboarding with a seeded demo creator.
- **Validation:** Zod for schema validation.
- **API Codegen:** Orval generates API hooks and Zod schemas from OpenAPI specifications.
- **Build System:** esbuild for CJS bundle creation.
- **Feature Flags:** Key features guarded by feature flags.
- **Quota Management:** Manages per-creator daily quotas and AI spend caps.
- **Job Queue:** Postgres job queue with partial unique index for deduplication.
- **Error Handling:** Structured error capture, in-process per-IP rate limiting, and Pino structured logging.
- **Consent:** Endpoints for user consent, data export, and deletion.

**Feature Specifications:**
- The Ideator generates video ideas with hard constraints: hook ≤ 3 seconds/8 words, shoot ≤ 30 minutes, and ≥50% low-effort ideas per batch.
- **Hybrid Ideator Pipeline (Layers 1/2/4 shipped):** Cost-reduction pipeline routing requests through a deterministic local pattern engine first (`lib/patternIdeator.ts` — 6 templates × ~20 safe scenarios × 5 hook-style phrasings, fully memory-biased), filtering candidates through a 6-axis 0–10 scorer with one rewrite attempt (`lib/ideaScorer.ts`), and only falling back to Claude `generateIdeas` when fewer than 3 local candidates pass the gate. Daily per-creator cache on `creators.last_idea_batch_json` + `last_idea_batch_date` (migration #20) serves same-day repeat requests for free; demo creators bypass cache writes. The orchestrator (`lib/hybridIdeator.ts`) exposes `{ ideas, source: cache|pattern|fallback|mixed, usedFallback, counts }` and the `/api/ideator/generate` route shape is unchanged. Llama 3.1 fallback and Llama 3.2 Vision are deferred to follow-up sessions.
  - **Within-batch diversity (HARD):** `diversifiedSelect` is a four-pass picker (strict → structure-unique → fresh-axis → rescue) gated at score≥8, fed by a cartesian-diagonal weave so every 16-candidate pool covers all 5 hookStyles, all 6 structures, and ≥3 distinct families.
  - **Between-batch regenerate variation (HARD, with rolling history):** When `regenerate=true`, the orchestrator reads a rolling cache history (the most-recent batch + up to `MAX_HISTORY_BATCHES=4` prior batches = 5 batches total) and applies a SPLIT HARD exclusion: every HOOK shipped in the last 5 batches is forbidden (depth 5 — mathematically prevents an identical 3-hook batch from re-appearing), but FAMILY/style exclusion only spans the immediately-prior batch (depth 1 — extending it to depth 5 squeezed the ~20-family pattern pool to ~6 candidates and starved the per-batch axis guards). Older-batch family/topic repetition is handled SOFTLY by the novelty-context penalties. The pattern engine is salted with `((hashEntries(flatHistory) ^ Date.now()) >>> 0) % 997` so each regenerate produces a structurally different (template, scenario, style) starting offset. Claude fallback (when triggered) is hook-excluded too. The cache JSONB envelope is now `{ version: 2, current: [{idea, family, templateId}], history: [batch[], ...] }` — backward-compatible reader (`tryParseCachedEnvelope`) accepts both legacy `Idea[]` and `[{idea, family, templateId}]` shapes. JSONB column means no migration needed for the shape extension.
  - **Novelty-aware selection (SOFT, BOOST + PENALTY):** The greedy selector replaces the four-pass `diversifiedSelect`. Each candidate carries `visualActionPattern` (11-value enum: `kitchen_contradiction`, `desk_avoidance`, `phone_scroll_freeze`, `bedroom_avoidance`, `outfit_check_cut`, `text_message_panic`, `face_reaction_deadpan`, `mirror_self_call_out`, `gym_no_show`, `car_avoidance`, `meal_prep_chaos`) and `topicLane` (5 values: `food_home`, `work_productivity`, `social_texting`, `body_fitness`, `daily_routine`) derived from `scenarioFamily` via `VISUAL_ACTION_BY_FAMILY` / `TOPIC_LANE_BY_FAMILY` lookup tables (`patternIdeator.ts`). `scoreNovelty` (`ideaScorer.ts`) awards 0–5 across 5 dims (hookStyle, scenario, structure, visualAction, topic) compared to the prior batch + the in-progress pick set, but ONLY when `qualityScore≥8` (no quality-rescue). `selectionPenalty` re-applies per-pick demerits with set-based saturation (-2 hookStyle, -3 family, -1 structure, -2 topic, -2 visualAction). The orchestrator uses `selectWithNovelty` (greedy by `quality + novelty + penalty`) with HARD batch guards (never 3× same hookStyle/format/structure, never >2 share family/visualAction/topic); `exhaustiveReselect` (capped at count≤5, top-`count*4` candidates) brute-forces a guard-passing combination if greedy fails. Claude fallback now triggers on EITHER `merged.length<3` OR guard failure; if guards still fail post-Claude, ships best-effort with `hybrid_ideator.guards_failed_shipping_best_effort` warn log. QA: 5 regens → 6/6 unique batches, 0 duplicate hooks, `usedFallback=false`, ≥2 distinct visual scenes + ≥2 distinct hookStyles per batch.
  - **Llama 3.1 hook-mutation layer (v2 quality-first, stateless; Session 4 = scorer-aware prompt + empty-array telemetry):** Bolted on top of the now-frozen pattern engine — `lib/llamaHookMutator.ts` rewrites HOOK (and optionally caption) on the chosen 3-pick batch only AFTER local fallback merge in `hybridIdeator.ts`. Pattern engine still owns ALL structural work (scenarios, structure, filming, shot plan); Llama never touches them. Triggers (priority order): `regenerate`, `templated_openers` (2+ shared opener or family), `recent_history_similarity` (2+ overlap with novelty ctx), `all_pattern_batch`, `borderline_novelty` (hookImpact<2 + total≥6). `pickMutationTargets` caps per trigger then HARD-clamps to ≤5 (regen→top5, templated→top5, history→top3, all_pattern→top2, borderline→filter then top5). One `meta-llama/llama-3.1-8b-instruct` chat call via `@workspace/integrations-openrouter-ai` (45s timeout, temp 0.85, 800 max tokens, json_object, ≤3 hookOptions per slot enforced post-parse). Validators (in order): `too_long` (>12) → `too_short` (<3) → `banned_opener` (`pov:|when you|watching|reading|how to`) → `sensitive_content` (dm/bank/password/ssn/salary/venmo/etc) → `abstract_advice` (deserve/learning to/etc) → `incomplete_or_dangling` (validateHook for ≤10, light dangling-only check for 11-12) → `scene_drift` (must share content noun with original setting/family/sceneBeat) → `same_opener` (lookupHookOpener differs). Each surviving rewrite is fed back through `scoreIdea` and only replaces the original if `hookImpact > original` OR `personalFit > original` OR `total ≥ original + 1` (quality-first override) — first winning hookOption per slot wins, remainder skipped. Caption mutation runs independently per slot using the same +1 total or +1 captionStrength rule. On Llama timeout/network/parse failure the entire batch ships unchanged; the OpenRouter client is lazy-initialized via `getOpenRouterClient()` so missing env vars never crash boot. `source` ladder: `pattern` (no fallback, no mutation) → `mixed` (mutation selected at least one rewrite) → `fallback` (all-Claude). Telemetry logged to `hybrid_ideator.llama_mutation`: reason, candidatesSent, optionsReturned, mutationsSelected, costEstimateTokens, rejectedReasonCounts, errored. ZERO schema changes — purely in-memory; cache writes use the mutated batch so next-regen exclusion lists stay accurate. The mutator's `source: "llama_3_1"` flag also naturally suppresses templateId in `toCacheEntries` (which only sets it for `pattern_variation` source). QA: 45/45 unit + integration assertions passing (triggers, validators, end-to-end real Llama call); on real-batch end-to-end, Llama returned 5 valid hook options for a regenerate-triggered batch with 0 mutations selected because none scored strictly better than the strong pattern originals — quality-first override behaved exactly per spec.
  - **Llama cost-control + anti-abuse layer:** Per-creator usage gates layered on top of the hook-mutation pipeline. ZERO schema changes — reuses the existing `usage_counters(creator_id, day, kind, count)` composite-PK table with three new `kind` values (`idea_request` += `result.ideas.length` per generation, `regenerate_request` += 1 per regen attempt regardless of result-count, `llama_call` fire-and-forget +1 per mutator attempt). `lib/usageTracker.ts` owns the layer: DB-backed `incrementUsage` / `getUsageToday` for the daily counters; in-memory `Map<creatorId, number[]>` for the 2-min sliding Llama-call window; in-memory `Map<creatorId, lastTs>` for the rapid-regen cooldown. **Gates (priority order, demo creators bypass everything via the same `creator.isDemo` check the cache and quota paths already use):** (1) HARD LIMIT — route-layer, `regenerate=true && idea_request_count > 100` → 429 with `{ error: "rate_limit_take_a_break", message: "Take a break — come back later for fresh ideas" }`; quota is NOT consumed on this path. (2) RAPID-REGEN COOLDOWN — `acquireRegenSlot(creatorId)` is the atomic check-and-record primitive that returns 0 for the first tap and a randomized 3000-5000ms sleep for the second tap within `<3s`; closes the TOCTOU window two parallel taps would otherwise have with separate read+record calls. (3) THROTTLE — mutator-layer, `>3` Llama calls in the trailing 2-min window → skip mutation with `costControlSkipped: true, skipReason: "throttle_2min"`. The mutator refreshes `ctx.usage.llamaCallsLast2Min` from the live in-memory ring buffer at the top of `maybeMutateBatch` so parallel requests can't all pass against a stale snapshot the route handed them pre-await. (4) DAILY ADAPTATION — probabilistic skip with `Math.random()` priority `adaptation_40` (75% skip when `idea_request_count > 40`) > `adaptation_25` (50% skip when `> 25`). (5) FALLBACK — any cost-control skip ships pattern-engine output unchanged (existing graceful path, no new branches needed). **Mobile error mapping:** `studio.tsx` and `(tabs)/index.tsx` both inspect `ApiError.data` on 429 and surface the verbatim server `message` for `error === "rate_limit_take_a_break"` (rather than the generic `buildErrorMessage` "HTTP 429: …" prefix). **Telemetry:** `ideator.usage` (per request, INFO) logs `creatorId, creatorIsDemo, count, regenerate, cooldownAppliedMs, ideaRequestCountAfter, regenerateRequestCountAfter, llamaCallCountBefore, llamaCallsLast2Min`; `hybrid_ideator.llama_mutation` gains `costControlSkipped, skipReason, ideaRequestCountToday, llamaCallsLast2Min`; `ideator.hard_limit_blocked` and `ideator.cooldown_applied` log when those gates fire. **Deferred (architect-noted, bounded impact):** parallel-request DB-level race on the hard-limit check (would require advisory locks; in practice bounds overshoot to a few extra ideas before the next tap blocks). QA: 30/30 unit + DB-roundtrip assertions passing — gate priority/boundaries, demo bypass, throttle isolation across creators, atomic cooldown under simulated parallel taps, day-bucket UTC date.
  - **Llama 3.2 Vision style-extraction layer (privacy-first, soft-bias only):** Optional on-device→server visual-style learning bolted on top of the imported-videos flow. Spec source: `attached_assets/Pasted-LLAMA-3-2-VISION-STYLE-EXTRACTION-FROM-USER-VIDEOS-Goal_1777447049198.txt`. **Mobile flow:** after a creator imports a video, the mobile app samples 4 thumbnail frames on-device via `expo-video-thumbnails` at fractional offsets `[0.15, 0.4, 0.65, 0.9]` of duration (fixed-timestamp `[1, 3, 5, 8]s` fallback when duration unknown), reads each as JPEG base64 via `expo-file-system/legacy` (the v19 split: `readAsStringAsync` lives on the legacy import), and POSTs the data URLs to a new server endpoint — NO RAW VIDEO BYTES LEAVE THE PHONE, only sampled thumbnails. `Platform.OS === "web"` short-circuits to `[]` so QA-mode in browser can't crash. Both helpers (`extractVideoFrames.ts`, `uploadVisionFrames.ts`) are fail-open: any error → empty array → upload no-ops; the upload itself is fire-and-forget so onboarding flow timing is unchanged. Only `MvpOnboarding.tsx` wires the upload (BlackHoleUpload + style-twin-train don't POST imported-videos so there's no `id` to attach vision to). **Server flow:** new `POST /api/imported-videos/:id/vision-frames` (`routes/visionStyle.ts`) auths via `resolveCreator`, verifies ownership (`imported_videos.creator_id` must equal caller), validates body `{ frames: 1–5 base64 strings, ≤250KB each }`, then calls `extractStyleFromFrames` (`lib/visionStyleExtractor.ts`) → one multimodal `meta-llama/llama-3.2-11b-vision-instruct` call via `getOpenRouterClient()` (temp 0.2 — classification not creativity, max_tokens 500, 30s timeout, `response_format: json_object`). Result fed through `aggregateVisionStyle` (`lib/visionProfileAggregator.ts`) and persisted to new `creators.vision_style_json` jsonb column (migration #21, additive `ADD COLUMN IF NOT EXISTS`, runs through the existing advisory-locked migration registry — NOT drizzle push). **Privacy posture:** the extractor's free-text `visibleAction` and `privacyRiskReason` fields are DROPPED before persistence (the per-video signal stores enums + booleans only); when the model returns `privacyRisk=true` the entire analysis is suppressed from aggregation but `totalAnalyzed` and `totalDroppedForPrivacy` still bump for transparency (so we can tell the user "we looked at N clips, dropped K for privacy" without lying). `perVideoSignals` capped at `MAX_PER_VIDEO_SIGNALS=10` with FIFO eviction; idempotent on `importedVideoId` (re-POST replaces the existing entry, never double-counts). **Derived hints (≥2/N rule, scaled):** `derivedStyleHints` only computed when `perVideoSignals.length ≥ MIN_VIDEOS_FOR_HINTS=3`; for each category (`preferredFormats`/`Settings`/`Energy`/`Framing`/`ReactionTypes`) we take values appearing in `≥ ceil(window * 0.5)` videos (the 2/3 rule scaled), top-2 per category, `unknown` enum values filtered out so the model's "I can't classify" fallback is never promoted to a "preference". **Bias hookup (SOFT, additive only, scoring-time):** `runHybridIdeator` loads `vision_style_json` alongside the other creator JSON docs (`routes/ideator.ts`) and threads `derivedStyleHints` through `scoreIdea`. `applyVisionBoost` (`lib/ideaScorer.ts`) is the ONE point in the scoring stack vision touches — it can lift `personalFit` from `1 → 2` when `mapPatternSettingToVisionSetting(meta.scenario.setting)` matches `preferredSettings` (bed→bedroom, bathroom→bathroom_mirror; couch/other → null = no boost), but NEVER `0 → anything` (avoids over-weighting borderline candidates) and NEVER subtracts. Boost runs DOWNSTREAM of generation, quality filters, and safety filters — vision can never override an explicit answer, a yes/no signal, or a safety rule, and rolling back the layer is as simple as setting `derivedStyleHints` to empty (boost no-ops). Vision NEVER generates ideas, NEVER writes hooks, NEVER replaces the ideator. **Cost control:** new `vision_call` kind on `usageTracker.UsageKind` (no schema change — `kind` is varchar(32)) with daily cap `VISION_DAILY_CAP=20`; over-cap returns `429 { error: "rate_limit_take_a_break", message: "Take a break — we'll learn from your next videos tomorrow" }` reusing the EXACT existing error key so the mobile error-mapping path (`(tabs)/index.tsx`) surfaces the verbatim server message without an additional client branch. Demo creators bypass both the OpenRouter call (returns a fixed mock analysis so demo onboarding still exercises the aggregation path) AND the counter increment, matching the cost-control layer's demo-bypass posture. **Telemetry:** `vision_style.extracted` (analyzedAt, contentType, setting, privacyRisk, derivedStyleHintsChanged), `vision_style.privacy_dropped` (when `privacyRisk=true`), `vision_style.rate_limited` (over daily cap). **QA:** 42/42 pure-logic assertions passing — extractor JSON parsing + enum clamping + null-on-malformed; aggregator privacy drop + idempotency + FIFO eviction + ≥50% threshold + MIN_VIDEOS_FOR_HINTS gate; scoreIdea boost firing on matching settings, refusing to lift `0`, refusing to fire on unmapped pattern settings (couch), respecting empty-hints state. Architect review: PASS after one fix (429 error key parity with the existing `rate_limit_take_a_break` contract — non-vision-prefixed key so the existing mobile error mapper handles it). E2E test deferred: vision flow only fires on native (web QA short-circuits) and requires a real device + OpenRouter creds — pure-logic harness covers all branches the integration test would.
  - **Script-diversity expansion (7th categorical axis to break "same mental loop" failure):** Layered on top of the novelty selector to fix the failure mode where 3 ideas in a batch — with distinct hookStyles, families, structures, visualActions, and topics — still read as variations of the same video because they share the same narrative shape (e.g. all three are "i thought i would, but actually i didn't" realizations). Spec source: `attached_assets/Pasted-SCRIPT-DIVERSITY-EXPANSION-LARGE-SCRIPT-SYSTEM-Goal-Bre_1777476842045.txt`. ZERO schema changes — derived purely from existing fields, persisted through the existing JSONB cache envelope (legacy entries pre-dating taxonomy resolve via `lookupScriptType` returning `null` so the axis no-ops on cold-start). **Taxonomy:** new `ScriptType` enum (37 values across 6 narrative-shape groups: realization-family, avoidance-family, false-start-family, low-energy-reaction-family, internal-contradiction-family, social/situational-family) + `SCRIPT_TYPES` const array + `SCRIPT_TYPE_CLUSTERS` (two `Set<ScriptType>` keyed groupings: `avoidance` = 4-element cluster, `internal_contradiction` = 5-element cluster — used by the all-3-in-cluster batch-rejection guard). New `Energy` enum (`active`/`low`/`medium`) + `ENERGY_BY_VISUAL_ACTION` lookup derived from the existing 13-value `VisualActionPattern` enum (no scenario-level tagging needed; energy is a property of how the scene is filmed, not what it depicts). `SCRIPT_TYPE_BY_FAMILY` tags each of the 25 scenarios with its primary scriptType (~14 distinct types in active rotation); `SCRIPT_TYPE_OVERRIDES: Partial<Record<TemplateId, Partial<Record<string, ScriptType>>>>` (~17 entries) handles cases where a template fundamentally reshapes narrative — e.g. `expectation_vs_reality × gym → realization` (gym's default is `avoidance`), `routine_contradiction × productivity → internal_vs_external`, `small_panic × texting → quiet_panic`. `resolveScriptType(template, family)` (override → default) used at assembly in `assembleCandidate`; `lookupScriptType(family?, templateId?)` (returns `null` when family missing/unknown — caller skips the candidate's contribution to cross-batch sets) used by `buildNoveltyContext` for cache reads, preserving JSONB legacy safety. `PatternMeta` gains optional `scriptType` + `energy` fields. **Batch guards (HARD, in `batchGuardsPass`):** (a) max-2-same scriptType per batch (3-of-same rejected, like the existing family/visualAction/topic 2-cap); (b) all-3-in-same-cluster rejection (3 picks all from `SCRIPT_TYPE_CLUSTERS.avoidance` or all from `internal_contradiction` rejected even when their scriptType values differ); (c) all-3-low-energy rejection (any batch where all 3 picks have `meta.energy === "low"` is rejected). **Scoring (`scoreNovelty` in `ideaScorer.ts`):** added 8th dim `sctFresh` (+1 when scriptType differs from prior batch + already-picked) + `unusedBoost` (+3 when scriptType ∈ `unusedScriptTypesLast3`) — 0-8 range now. **Penalty (`selectionPenalty`, per-pick stacking):** -2 for shared scriptType in already-picked (within-batch), -3 for scriptType ∈ `recentScriptTypes` (immediate-prior batch only — scope-corrected after architect review), additional -2 for scriptType ∈ `frequentScriptTypesLast3` (appearing in ≥2 of last 3 batches). **History tiers (`buildNoveltyContext` extended signature `(prev, last3Batches=[])`):** `recentScriptTypes` is SCOPED to `last3Batches[0]` (the immediate-prior batch only — NOT the flat 5-batch view the other per-axis sets use, so the -3 penalty and the regen rescue's freshness baseline don't artificially starve picks on a small pool); `frequentScriptTypesLast3` derives from `last3Batches` (≥2-of-3 occurrence); `unusedScriptTypesLast3` = `SCRIPT_TYPES − union(last3Batches)`. Other axes (recentFamilies/styles/topics/visualActions/hookOpeners/settings) keep their established 5-batch scope — strict scope discipline. **Regen rule (`selectWithNovelty` when `regenerate=true`):** after greedy/exhaustive pick, calls `batchHasNewScriptType(batch, recentScriptTypes)`; if false runs ONE additional exhaustive pass with a HARD ≥1-fresh-scriptType constraint; if still impossible ships best-effort with `hybrid_ideator.regen_no_new_scripttype_shipping_best_effort` warn log (matches the existing `guards_failed_shipping_best_effort` pattern). **Generation distribution (`interleaveByScriptType` in `patternIdeator.ts`):** post-processes `generatePatternCandidates` output to bucket candidates by scriptType then round-robin emit, so the first 6+ candidates span ≥6 distinct scriptTypes when the pool naturally allows. SOFT — never drops candidates, only reorders. **QA: 22/22 pure-logic asserts passing** — override-vs-default priority (gym default=`avoidance`, `expectation_vs_reality × gym`=`realization`, `routine_contradiction × productivity`=`internal_vs_external`); back-compat (`null` on missing/unknown family even with templateId, scenario-default fallback on existing-family + missing/unknown templateId); 10-batch run produces 15 distinct scriptTypes across 30 ideas with max single share = `avoidance` @ 13.3% (well under the 40% cap); 3-of-same scriptType + all-3-in-avoidance-cluster + all-3-low-energy all rejected by `batchGuardsPass`; regen with achievable freshness contains ≥1 fresh scriptType; regen with structurally impossible freshness (all candidates forced to one scriptType + that scriptType in recent) → standard guards reject OR rescue-failure warn fires; 16-candidate pool spans 13 distinct scriptTypes with first 6 picks already spanning 6 distinct (interleave round-robin verified). Architect review: PASS after one fix (`recentScriptTypes` scope corrected from flat 5-batch view to immediate-prior-batch-only, matching spec — non-blocking targeted-unit-test suggestions for `buildNoveltyContext` deferred since the fix is correctness-by-construction and the QA harness exercises the downstream paths). **FINAL-spec follow-on (`Pasted-SCRIPT-SYSTEM-FINAL-IMPLEMENTATION-QUALITY-DISTRIBUTION_1777479331391.txt`) layered on top of the above:** (i) **Hard pool-distribution caps (`applyPoolCaps` in `patternIdeator.ts`, run BEFORE `interleaveByScriptType` in `generatePatternCandidates`):** per-scriptType cap = `ceil(N * 0.20)` (≤20% any single scriptType); per-cluster cap = `ceil(N * 0.40)` (≤40% combined for the avoidance OR internal_contradiction cluster — directly addresses the spec §3 "must NOT dominate" callout for avoidance / procrastination / quiet-contradiction by mapping procrastination → avoidance cluster, quiet-contradiction → internal_contradiction cluster). Tail-drop preserves the upstream Cartesian-weave + memory-bias ordering for survivors (overflow entries are skipped, never reordered). No-op for pools ≤5 (any single dominant scriptType is structural at that size). ScriptType-less candidates (legacy / Claude/Llama fallback) bypass per-type/cluster checks but count toward N — preserves cap denominator integrity. (ii) **Soft regen ≥2-fresh upgrade (spec §6, in `selectWithNovelty`):** after the existing hard ≥1-fresh rescue produces `chosen`, if `countNewScriptTypes(chosen, recentScriptTypes) < 2` runs ONE more `exhaustiveReselect` with predicate `countNewScriptTypes(b, recent) >= 2`. Best-effort — silently keeps the 1-fresh pick if no 2-fresh combination exists in the pool; 1-fresh already meets the hard requirement so no warn fires. Only runs when `opts.regenerate === true`. (iii) **Soft ≥1-active preference (spec §7, in `selectWithNovelty`):** after the regen blocks, if `!batchHasActive(chosen)` runs ONE more `exhaustiveReselect` with predicate `batchHasActive(b) && countNewScriptTypes(b, recent) >= minFreshRequired`. The composed predicate is the architect-flagged correctness fix: §7 MUST preserve the freshness invariant of the currently-chosen batch (snapshotted as `minFreshRequired = (regen && recent.size>0) ? countNewScriptTypes(chosen, recent) : 0`) — otherwise §7 could silently downgrade a fresh-batch produced by §6 / hard-rescue into an active-but-0-fresh batch, violating the §6 hard regen guarantee. Runs for both regen and non-regen (non-regen path has `minFreshRequired=0` so it acts as plain `batchHasActive`). New helpers `countNewScriptTypes(batch, recent)` (counts distinct fresh scriptTypes — duplicates of same fresh type count once) and `batchHasActive(batch)` (true iff any pick has `meta.energy === "active"`). Audit-driven decisions NOT to ship: spec §4A "strongly prefer 3 distinct" already met by the `-2` within-batch scriptType penalty (3-distinct beats 2-distinct beats 1-distinct in the additive score); spec §4B "script diversity BEFORE highest scores" interpreted pragmatically — current additive `quality + novelty + penalties` selector with high-tier quality gating is internally consistent and architect-confirmed defensible (lexicographic diversity-first restructure deferred as out-of-scope). **QA: 28/28 unit asserts** — applyPoolCaps (per-type cap, per-cluster cap for both clusters, small-pool no-op, scriptType-less preservation, tail-drop head-ordering preservation), countNewScriptTypes (recent-empty/undefined edge cases, distinct-only counting with duplicates, missing-scriptType skip), batchHasActive (true/false/empty/undefined-energy), AND 6 §7-predicate-composition regression tests (REJECTS active-but-1-fresh when chosen has 2-fresh, ACCEPTS active-AND-≥2-fresh, REJECTS active-0-fresh that would downgrade rescue's 1-fresh, non-regen path correctly drops the fresh constraint). Architect re-review after the §7 composed-predicate fix: PASS — fix is correctness-by-construction, monotonic w.r.t. §6 outcomes, non-regen behavior unchanged; remaining suggestion (orchestrated-path integration tests through full greedy → hard rescue → §6 → §7 ordering) acknowledged as nice-to-have but unit-level predicate coverage deemed sufficient regression-proofing. **IDEA ARCHETYPE + SCENE-OBJECT TAG layer (final-spec follow-on, layered on top of all of the above):** two NEW derived diversity axes added to the same template-based programmatic Cartesian pipeline (NO LLM, NO new scenarios, NO schema changes — vision-style FROZEN, quality scoring untouched). User explicitly opted to NOT write 60 new scenarios; instead sceneObjectTag layer tags each existing scenario with one of 53 user-supplied tags + a 5-value env-cluster. **Taxonomy 1 — `archetypeTaxonomy.ts`:** 12 `ArchetypeFamily` ids (`self_deception`, `micro_rituals`, `social_observation`, `object_personality`, `time_distortion`, `inner_monologue`, `escalation`, `weird_habits`, `petty_logic`, `identity`, `low_energy_realism`, `unexpected_competence`) × 10 archetypes each = 120 globally-distinct `Archetype` ids (e.g. `self_deception` family contains `ill_do_it_later`, `fake_productivity`, `pretend_busy`, … `denial_of_obvious`). Hand-tuned `PRIMARY_ARCHETYPE_BY_SCRIPT_TYPE` mapping covers all 37 ScriptType values, all mapped archetypes globally distinct, all 12 families covered (weird_habits assigned via `loop_behavior` since it didn't otherwise have a natural ScriptType anchor). Lookups: `ARCHETYPES_BY_FAMILY`, `FAMILY_BY_ARCHETYPE`. `resolveArchetype(scriptType): { archetype, family } | null` deterministic; `resolveArchetypeLoose` accepts unknown strings (returns null) for legacy/fallback safety. **Taxonomy 2 — `sceneObjectTaxonomy.ts`:** 53 `SceneObjectTag` ids (`fridge`, `sink`, `laundry`, `mirror`, `phone_lock_screen`, … `front_door`) verbatim from user spec, partitioned 1:1 into 5 `SceneEnvCluster` ids (`kitchen`, `bedroom`, `phone_digital`, `outside`, `bathroom`) via total `ENV_CLUSTER_BY_TAG` lookup. `SCENE_OBJECT_TAG_BY_FAMILY` tags each of the 25 existing scenarioFamilies with one primary tag (all 25 mappings distinct, ≥3 distinct env clusters represented across the 25 families). `lookupSceneObjectTag(family)` + `lookupSceneEnvCluster(tag)` + `lookupSceneEnvClusterForFamily(family)` helpers, all null-safe on unknown input. **Plumbing — `PatternMeta` / `CandidateMeta` extension:** `PatternMeta` gains optional `archetype` + `archetypeFamily` + `sceneObjectTag` + `sceneEnvCluster` fields; `CandidateMeta` fallback union (ideaScorer.ts) extended with the same optional fields so Claude/Llama fallback wraps that omit them are type-safe. `assembleCandidate` populates all four via `resolveArchetype(scriptType)` + `lookupSceneObjectTag(family)` (cluster derived from tag via `ENV_CLUSTER_BY_TAG`); only populated when both upstream fields resolve, preserving the established missing-field-skips-axis safety pattern. **Novelty context — `NoveltyContext` extension:** added `recentArchetypes` + `recentArchetypeFamilies` (immediate-prior batch only, mirrors `recentScriptTypes` scoping) + `recentSceneObjectTags` (immediate-prior batch only — for the -3 cross-batch penalty) + `frequentSceneObjectTagsLast3` (≥2-of-3 occurrence — for the -2 stack) + `unusedSceneObjectTagsLast3` (`SCENE_OBJECT_TAGS − union(last3Batches)` — for the +3 boost). `buildNoveltyContext` extended in hybridIdeator.ts to derive these from prev cache + last3 batches via `lookupScriptType` then `resolveArchetype`, plus `lookupSceneObjectTag(family)`; absent on cold-start (preserves null-safe behavior). **Scoring — `scoreNovelty` (now 10-dim):** added `archetypeFresh` (+1 when archetype differs from cross-batch recent + already-picked) and `sceneObjectTagFresh` (+1 when sceneObjectTag differs from cross-batch recent + already-picked) binary dims. PLUS +3 `unusedTagBoost` when sceneObjectTag ∈ `unusedSceneObjectTagsLast3`. Note: NO separate +3 archetype boost — the 1:1 `ScriptType → archetype` derivation makes the existing +3 unused-scriptType boost cover archetype rotation indirectly (archetype freshness is already binary-fresh-tracked via `archetypeFresh`), so a second +3 would double-count. **Penalty — `selectionPenalty` (per-pick stacking):** -4 same archetype within batch (heaviest within-axis penalty in the system — archetype is the spec's headline anti-clone signal); -2 same archetypeFamily within batch (stacks with -4 when same archetype, so archetype-collision actually costs -6); -1 same sceneEnvCluster within batch (lighter than family since cluster is broader — `coffee` + `fridge` are both kitchen-cluster but read as more distinct than two ScriptType-cluster collisions); -3 sceneObjectTag ∈ `recentSceneObjectTags` (immediate-prior cross-batch); -2 sceneObjectTag ∈ `frequentSceneObjectTagsLast3` (stacks with -3 to -5 when both fire). All axes saturate per-pick (no double-count), all skip when fields missing. **Batch guards — 4 NEW HARD rules in `batchGuardsPass` at `batch.length >= 2`:** (d) max-1 archetype per batch (blocks two `ill_do_it_later` picks even when scenarioFamily/hook/scriptType differ — e.g. "I'll clean tomorrow" + "I'll text them tomorrow" = two distinct families but one archetype); (e) max-1 archetypeFamily per batch (stricter — two distinct archetypes from the SAME family, e.g. `ill_do_it_later` + `fake_productivity` both in `self_deception`, still read as the same kind of idea); (f) max-1 sceneObjectTag per batch (two `coffee` or two `unread_messages` picks read as the same physical scene shot twice); (g) max-1 sceneEnvCluster per batch (catches cluster collisions across distinct tags — `coffee` + `fridge` are both kitchen-cluster, feels like one kitchen morning split into two videos). All 4 guards skip when fields missing (legacy / fallback safety — counts toward batch but doesn't trip the guard). **Regen rescue — TWO additional exhaustive passes in `selectWithNovelty` (compose with the existing scriptType-fresh rescue):** (i) when `regenerate && recentArchetypeFamilies.size > 0` and current chosen batch has 0 fresh families, run `exhaustiveReselect` with `extraGuard = batchHasNewArchetypeFamily(b, recent) && (recentScriptTypes.size === 0 || batchHasNewScriptType(b, recent))` — composes the scriptType invariant into the new family invariant so this pass can't silently downgrade a freshly-rescued scriptType batch; warn-and-ship-best-effort with `hybrid_ideator.regen_no_new_archetype_family_shipping_best_effort` log if pool can't satisfy. (ii) when `regenerate && recentSceneObjectTags.size > 0` and current chosen batch has 0 fresh tags, run `exhaustiveReselect` with `extraGuard = batchHasNewSceneObjectTag(b, recent) && (recentScriptTypes ⇒ batchHasNewScriptType) && (recentArchetypeFamilies ⇒ batchHasNewArchetypeFamily)` — composes BOTH prior invariants so neither gets silently downgraded; same warn-and-ship-best-effort discipline. **§6 / §7 freshness invariants extended:** the existing §6 "≥2-fresh scriptType soft upgrade" and §7 "≥1-active soft preference" predicates extended to also preserve the snapshotted ≥1-fresh-archetypeFamily AND ≥1-fresh-sceneObjectTag invariants of the currently-chosen batch (snapshotted before the soft pass runs), so neither soft pass can silently downgrade a fresh-batch produced by the new hard rescues. **New helpers exported from hybridIdeator.ts:** `countNewArchetypeFamilies(batch, recent)`, `batchHasNewArchetypeFamily(batch, recent)`, `countNewSceneObjectTags(batch, recent)`, `batchHasNewSceneObjectTag(batch, recent)` — all vacuously true on empty recent set (no-op on cold-start), all skip picks with missing fields (fallback safety). **QA: 70/70 pure-logic asserts** — taxonomy invariants (12 families × 10 archetypes = 120 globally distinct, FAMILY_BY_ARCHETYPE inverse-consistent with ARCHETYPES_BY_FAMILY, all 37 ScriptType→archetype mappings distinct + all 12 families covered, 5 env-clusters × 53 tags total + ENV_CLUSTER_BY_TAG total + TAGS_BY_CLUSTER inverse + 25 family→tag mappings distinct + ≥3 distinct clusters represented), assembleCandidate populates archetype + sceneObjectTag on ≥80% of pattern_variation candidates from a real `generatePatternCandidates({count:16})` run, batchGuardsPass clean-3-distinct PASSES + dup-archetype FAILS + dup-family-distinct-archetypes FAILS + dup-tag FAILS + dup-cluster-distinct-tags FAILS + partial-missing-fields PASSES (3 axes verified independently), selectionPenalty arithmetic (-6 dup archetype = -4 + -2 family stack, -2 dup family-only, -1 dup cluster, -3 cross-batch tag, -3+-2=-5 cross-batch recent+frequent stack, 0 baseline empty), scoreNovelty +3 unused-tag boost exact + within-batch dup-archetype lowers novelty, all 4 helper exports tested (count + has, fresh + recent + vacuous-empty), selectWithNovelty cold-start passes guards + regen produces a batch with ≥1 fresh family AND ≥1 fresh tag AND passes hard guards (recentScriptTypes + recentArchetypes + recentArchetypeFamilies + recentSceneObjectTags all populated). Architect re-review with `includeGitDiff=true`: PASS — composition/order in selectWithNovelty correct (scriptType rescue first → archetypeFamily rescue composes scriptType freshness → sceneObjectTag rescue composes both prior invariants → §6 / §7 preserve all three invariants), missing-field safety consistently handled, penalty arithmetic intentional and non-accidental (no hidden duplicate subtraction), derived-axis plumbing coherent through assembleCandidate + buildNoveltyContext. **HOOK LANGUAGE STYLE layer (final-spec follow-on, layered on top of all of the above):** new 12-value `HookLanguageStyle` axis (`confession`, `observation`, `absurd_claim`, `matter_of_fact`, `question`, `instruction`, `micro_story`, `comparison`, `object_pov`, `time_stamp`, `anti_hook`, `escalation_hook`) becomes the PRIMARY hook diversity axis on the generation Cartesian, replacing the legacy 5-value `HookStyle` on the H axis. Legacy `hookStyle` field stays populated on every candidate via a lossy `HOOK_LANGUAGE_STYLE_TO_LEGACY_HOOK_STYLE` mapping so memory aggregator + cached-batch readers + downstream UI keep working unchanged. ZERO schema changes — derived purely from the JSONB cache envelope, legacy entries pre-dating the axis silently drop to `undefined` and are skipped by recent-set derivation (cold-start / cache-cycle safe). **Generation Cartesian (`patternIdeator.ts`):** H constant switched from 5 → 12, styleOrder built from `HOOK_LANGUAGE_STYLES`, key construction + assembleCandidate signature updated to consume `hookLanguageStyle` (legacy `hookStyle` derived inside `assembleCandidate` via lossy mapping and populated on both `Idea` and `PatternMeta`). `PatternMeta` + `CandidateMeta` fallback union extended with optional `hookLanguageStyle?` field. **NEW catalog `HOOK_PHRASINGS_BY_LANGUAGE_STYLE` (46 entries across 12 styles, ≥3 per style):** rebuilt from scratch around the new axis with build-fns parameterized over `s.actionShort`/`s.realityShort`/`s.topicNoun`/`s.settingDetail`. NONE of the new entries produce text starting with banned prefixes (verified by QA). `pickValidatedLanguagePhrasing(hookLanguageStyle, scenario, tone, seed)` mirrors the existing `pickValidatedPhrasing` but reads from the new catalog AND additionally rejects any variant matching `BANNED_HOOK_PREFIXES`. **Banned-phrasing layer (PART 4 of spec):** new `BANNED_HOOK_PREFIXES: ReadonlyArray<RegExp>` (6 entries, case-insensitive `^\s*`-anchored: "the way i", "why did i", "i really thought", "me saying", "me, ", "what i said vs what i did") + `lookupBannedHookPrefix(hook): boolean` + extended `validateHook` to also reject any matching hook (catches Claude/Llama-fallback hooks that bypass the new catalog). PLUS `GENERIC_FILLER_PHRASES: ReadonlyArray<RegExp>` (5 entries: "this is so me", "we've all been there", "so relatable", "tell me you", "name a more iconic") + `containsGenericFiller(hook): boolean` + same `validateHook` extension. Pure additive — existing rejections preserved. **Novelty context — `NoveltyContext` extension:** added `recentHookLanguageStyles` (immediate-prior batch only — for the -3 cross-batch penalty) + `unusedHookLanguageStylesLast3` (`HOOK_LANGUAGE_STYLES − union(last3Batches)` — for the +2 boost). `buildNoveltyContext` extended to derive both from prev cache + last3 batches by reading `meta.hookLanguageStyle` directly (no derivation needed since it's a first-class field). `toCacheEntries` + `rescueEntries` extended to write `hookLanguageStyle` into the JSONB envelope so next-regen exclusion lists are accurate. **Scoring — `scoreNovelty` (now 11-dim):** added `hookLanguageStyleFresh` binary dim (+1 when hookLanguageStyle differs from cross-batch recent + already-picked) + `unusedHookLanguageStyleBoost` (+2 when hookLanguageStyle ∈ `unusedHookLanguageStylesLast3`). +2 here (not +3 like sceneObjectTag's unused-tag boost) because the H pool is 12 values vs 53 — the boost is sized proportionally to the cycling pressure. **Penalty — `selectionPenalty` (per-pick stacking):** -2 same hookLanguageStyle within batch; -3 hookLanguageStyle ∈ `recentHookLanguageStyles` (immediate-prior cross-batch); -5 when `idea.hook` matches `BANNED_HOOK_PREFIXES` (PART 4 banned-phrasing penalty — second line of defense for fallback hooks that bypass the validator). All axes saturate per-pick (no double-count), all skip when fields missing on legacy/fallback wraps. **Batch guard — NEW HARD rule in `batchGuardsPass`:** SOFTER than the archetype max-1 rule per spec — only HARD-rejects when ALL 3 picks share the same hookLanguageStyle (2-of-same allowed, the soft -2 within-batch penalty in `selectionPenalty` discourages it without rejecting the whole batch). Skip when fields missing on any entry. **Regen rescue — ONE additional exhaustive pass in `selectWithNovelty` (composes with the existing 3-axis rescue chain):** when `regenerate && recentHookLanguageStyles.size > 0` and current chosen batch has 0 fresh hookLanguageStyles, run `exhaustiveReselect` with `extraGuard = batchHasNewHookLanguageStyle(b, recent) && (recentScriptTypes ⇒ batchHasNewScriptType) && (recentArchetypeFamilies ⇒ batchHasNewArchetypeFamily) && (recentSceneObjectTags ⇒ batchHasNewSceneObjectTag)` — composes ALL THREE prior invariants so neither gets silently downgraded; warn-and-ship-best-effort with `hybrid_ideator.regen_no_new_hook_language_style_shipping_best_effort` log if pool can't satisfy. **§6 / §7 freshness invariants extended:** the existing soft-upgrade predicates extended again to also preserve the snapshotted ≥1-fresh-hookLanguageStyle invariant of the currently-chosen batch, so the soft active/scriptType passes can't silently downgrade a fresh-batch produced by the new hard rescue. **New helpers exported from hybridIdeator.ts:** `countNewHookLanguageStyles(batch, recent)` + `batchHasNewHookLanguageStyle(batch, recent)` — vacuously true on empty recent set (no-op on cold-start), skip picks with missing field. **Llama hook-mutator trigger extension (`llamaHookMutator.ts`):** the existing `templated_openers` trigger now ALSO fires when ≥2 candidates in the chosen batch share the same `hookLanguageStyle`; the existing `recent_history_similarity` trigger now ALSO fires when ≥2 batch entries' `hookLanguageStyle` ∈ `ctx.novelty.recentHookLanguageStyles`. Pure additive — existing same-opener / same-family triggers preserved. The mutated hook is re-validated against `validateHook` (which now includes banned-prefix + generic-filler checks) and re-scored; only replaces if better — no regression to existing quality-first override path. **QA: 128/128 pure-logic asserts** — taxonomy enumeration (12 styles, lossy mapping covers all 12 → valid legacy HookStyle), catalog (46 entries, every (style × every stress-scenario) build passes `validateHook` + zero `BANNED`/`FILLER` trips), validateHook extension (rejects all 6 banned prefixes + all 5 filler phrases, accepts clean catalog hooks), batchGuardsPass (HARD rejects 3-identical hookLanguageStyle, ACCEPTS 2-of-same + 1-different + all-3-distinct, SKIPS hard reject when any entry missing the field), selectionPenalty (-5 for banned-prefix candidate, 0 for clean candidate with no peers, -3 for recent-hookLanguageStyle candidate, 0 escape when fresh vs recent), helper exports (`countNewHookLanguageStyles` distinct counting + missing-field skip + cold-start vacuous-true behavior, `batchHasNewHookLanguageStyle` true/false/vacuous semantics), llamaHookMutator (`shouldMutateBatch` returns `templated_openers` for 2-same hookLanguageStyle + fires for cross-batch hookLanguageStyle overlap), regex set sizing (`BANNED_HOOK_PREFIXES.length === 6`, `GENERIC_FILLER_PHRASES.length === 5`). Architect re-review with `includeGitDiff=true`: PASS — composition order in `selectWithNovelty` correct (scriptType → archetypeFamily → sceneObjectTag → hookLanguageStyle rescue chain, each composing all prior invariants → §6 / §7 preserve all four invariants), missing-field safety consistently handled across all four derived axes, Cartesian H index correctly switched 5→12 with key construction + modulo + salt all updated, banned-prefix -5 correctly positioned as second line of defense behind validator (won't double-fire in normal pattern path since validator rejects first), penalty stacking distinct sets (within-batch vs cross-batch — intentional double-charge for both stalling AND cloning), legacy lossy mapping clean and complete (all 12 styles → valid 5-value legacy HookStyle). **VOICE PROFILE layer (final-spec follow-on, layered on top of all of the above):** new 8-value `VoiceProfile` axis (`dry_humor`, `chaotic`, `poetic`, `blunt`, `sarcastic`, `self_aware`, `deadpan`, `soft_confessional`) becomes a STYLE/TONE layer atop the now-frozen HookLanguageStyle pipeline — does NOT touch safety filters, quality threshold, scenario, or archetype selection (purely additive tone overlay). ZERO schema changes — derived purely from creator context (taste calibration / styleHints / visionStyleJson / default rotation), persisted through the JSONB cache envelope. **Selection priority (`selectPrimaryVoiceProfile` in `patternIdeator.ts`):** `tasteCalibration.preferredTone → styleHints.tone → visionStyleJson.deliveryStyle → default rotation [self_aware, dry_humor]` (rotation is deterministic via `rotationSeed` so a given creator's batches stay coherent). `strongPreference: true` ONLY when calibration set (the spec's "user strongly prefers" exception to the never-3-identical rule). **Mapping tables:** `CALIBRATION_TONE_TO_VOICE` (`dry_subtle→dry_humor` / `chaotic→chaotic` / `bold→blunt` / `self_aware→self_aware`), `HINTS_TONE_TO_VOICE` (`dry→dry_humor` / `chaotic→chaotic` / `self_aware→self_aware` / `confident→blunt` / `neutral→null` so the priority chain skips this tier when DerivedStyleHints carries no usable tone signal), `VISION_DELIVERY_TO_VOICE` (`deadpan→deadpan` / `awkward→soft_confessional` / `expressive→chaotic` / `confident→blunt` / `chaotic→chaotic`). **Allowed-voice rotation sets — `ALLOWED_VOICES_BY_PRIMARY: Record<VoiceProfile, VoiceProfile[]>`:** every primary unlocks 3-4 compatible rotation voices (e.g. `self_aware → [self_aware, soft_confessional, dry_humor, deadpan]`, `dry_humor → [dry_humor, deadpan, sarcastic, self_aware]`); `DEFAULT_ALLOWED_VOICES = [self_aware, dry_humor, deadpan, soft_confessional]` is the cold-start neutral coverage set (intentionally excludes the high-energy `chaotic` and high-decoration `poetic` voices — emitted only when positive signal). `pickVoiceForSlot(slotIndex, allowedList)` round-robin through the allowed list (deterministic primary-first bias for reproducibility). **Catalog tagging — `HOOK_PHRASINGS_BY_LANGUAGE_STYLE` extended:** every existing entry tagged with optional `voiceProfiles?: VoiceProfile[]` (undefined = voice-neutral, compatible with all). Entry-level voice tags anchored in entry text (e.g. `confession`/"I just realized..." entries lean self_aware/soft_confessional; `absurd_claim` entries lean chaotic/sarcastic; `matter_of_fact` entries lean blunt/deadpan). Catalog covers every (HLS × `DEFAULT_ALLOWED_VOICES`) cell with ≥1 valid build (verified by QA); voices outside `DEFAULT_ALLOWED_VOICES` rely on the runtime fallback in `pickValidatedLanguagePhrasing` (preferred-voice → voice-neutral → any non-banned), so catalog gaps for them are graceful rather than fatal. **Plumbing — `PatternMeta` / `CandidateMeta` extension:** `PatternMeta` gains optional `voiceProfile?: VoiceProfile` field; `CandidateMeta` fallback union extended (ideaScorer.ts) so Claude/Llama fallback wraps that omit it are type-safe. `assembleCandidate` signature gains `voiceProfile: VoiceProfile` parameter; `generatePatternCandidates` calls `selectPrimaryVoiceProfile` ONCE per generation and `pickVoiceForSlot(k, allowed)` per emitted candidate INSIDE the existing Cartesian inner loop (one voice per emitted candidate, NOT a new outer axis — would explode pool 8x). **Caption transform — `applyVoiceToCaption(caption, voice)`:** light tone tweak per voice (e.g. `chaotic` appends `😭` without separating space to stay within +2-char budget; `blunt`/`deadpan` strip trailing exclamations + collapse run-on dots; `soft_confessional` is idempotent passthrough since the catalog phrasing already reads gentle). Idempotent (`f(f(x)) === f(x)`) and never increases length by more than 2 UTF-16 code units. **Orchestrator wiring — `hybridIdeator.ts`:** between visionDoc analysis and `generatePatternCandidates`, parses tasteCalibration (null-coalesced via `parseTasteCalibration`), majority-mode-aggregates `visionDoc.perVideoSignals[].deliveryStyle` (skips `"unknown"`), then calls `selectPrimaryVoiceProfile` with `hintsTone=null` (DerivedStyleHints lacks tone field — preserved as null sentinel so the priority chain falls through cleanly). `noveltyContext.voiceStrongPreference = voiceSelection.strongPreference` so downstream batch guards see the strongPreference flag, and the full `VoiceProfileSelection` is threaded into `generatePatternCandidates`. **Cross-batch novelty — `NoveltyContext` extension:** added `recentVoiceProfiles` (immediate-prior batch only — for the -2 cross-batch penalty); `unusedVoiceProfilesLast3` not added because the 8-voice pool is small (would over-rotate the +1 boost given the 4-voice cold-start subset). `buildNoveltyContext` reads `meta.voiceProfile` directly from cache envelope (first-class field, no derivation needed); `toCacheEntries` + `rescueEntries` write voiceProfile so next-regen exclusion lists are accurate. **Scoring — `scoreNovelty`:** added `voiceProfileFresh` binary dim (+1 when voiceProfile differs from cross-batch recent + already-picked); legacy entries silently drop to undefined (no penalty / no boost — preserves cold-start safety). **Penalty — `selectionPenalty` (per-pick stacking):** -2 same voiceProfile within batch; -2 voiceProfile ∈ `recentVoiceProfiles` (cross-batch). Distinct sets — intentional double-charge for stalling AND cloning. Saturates per-pick (no double-count when both fire on different axes), skips when field missing. **Batch guard — NEW HARD rule in `batchGuardsPass`:** softer than the archetype max-1 rule per spec — only HARD-rejects when ALL 3 picks share the same voiceProfile UNLESS `ctx.voiceStrongPreference === true` (calibration explicitly picked that voice — user chose this tone, never override). 2-of-same allowed. Skip when fields missing on any entry. **Regen rescue — ONE additional exhaustive pass in `selectWithNovelty` (5th in the rescue chain after scriptType / archetypeFamily / sceneObjectTag / hookLanguageStyle):** when `regenerate && recentVoiceProfiles.size > 0` and current chosen batch has 0 fresh voiceProfiles, run `exhaustiveReselect` with `extraGuard = batchHasNewVoiceProfile(b, recent) && (recentScriptTypes ⇒ batchHasNewScriptType) && (recentArchetypeFamilies ⇒ batchHasNewArchetypeFamily) && (recentSceneObjectTags ⇒ batchHasNewSceneObjectTag) && (recentHookLanguageStyles ⇒ batchHasNewHookLanguageStyle)` — composes ALL FOUR prior invariants so none gets silently downgraded. Warn-and-ship-best-effort with `hybrid_ideator.regen_no_new_voice_profile_shipping_best_effort` log if pool can't satisfy. **§6 / §7 freshness invariants extended (now 5 axes):** the existing soft-upgrade predicates extended to also preserve the snapshotted ≥1-fresh-voiceProfile invariant of the currently-chosen batch, so neither the §6 ≥2-fresh-scriptType nor the §7 ≥1-active soft pass can silently downgrade a fresh-voice batch produced by the new hard rescue. **Voice-violation validator extension — `containsVoiceViolation(hook): boolean` + `VOICE_VIOLATION_PHRASES` regex set:** 4 entries covering the spec's reject criteria — mean (`pathetic|loser|cringe|embarrassing yourself`), motivational/advice-like (`/\byou\b\s+(?:\w+\s+){0,3}(got|deserve|owe yourself|earned|are worthy)\b/i` — the bounded interstitial allows up to 3 modifier words between "you" and the advice verb so spec-flagged hooks like "you absolutely deserve this" trip the guard), motivational-prescriptive (`own your|step into your|main character energy|life update incoming`), theatrical hyperbole stacking (`literally absolutely|absolutely literally|the most.*ever`). Pure additive — `validateHook` extended to also reject any matching hook (catches Claude/Llama-fallback hooks that bypass the new catalog). Existing rejections preserved; verified all 46 existing catalog entries × all stress scenarios still pass. **New helpers exported from hybridIdeator.ts:** `countNewVoiceProfiles(batch, recent)` + `batchHasNewVoiceProfile(batch, recent)` — vacuously true on empty recent set (no-op on cold-start), skip picks with missing field. **QA: 13 sections of pure-logic asserts all green** — 8-value enum exact (every spec voice present), priority chain (calibration / hints / vision / default for all input combinations including all-null + neutral hints sentinel skip + deterministic seed-based default rotation), allowed-voice maps cover all 8 primaries with primary-first bias + no-dupes invariant, hint-map neutral→null sentinel, catalog (every (HLS × `DEFAULT_ALLOWED_VOICES`) cell has ≥1 valid build), voice-violation regex (catches "you absolutely deserve this" + "pathetic loser" + "step into your..." + benign hooks NOT flagged), validateHook composes voice violation, `applyVoiceToCaption` idempotency + ≤+2-char-cap across all 8 voices, `generatePatternCandidates` emits non-empty voiceProfile on every candidate with ≥2 distinct voices per pool, helper exports vacuous-empty behavior (empty recent set → countNew = full count, batchHasNew = true), batchGuardsPass strongPreference monotonicity (3-identical voice REJECTED when !strongPreference; pass(true) ≥ pass(false) for any batch — strongPreference can never make a batch fail that would have passed without it), 2-of-same voice does NOT trip the 3-identical guard (invariance check across strongPreference toggle), `selectionPenalty` stacking (within-batch -2 + cross-batch -2 stacks distinctly when both fire — verified ≥-4 below baseline). Architect re-review with `includeGitDiff=true`: PASS — composition order in `selectWithNovelty` correct (scriptType → archetypeFamily → sceneObjectTag → hookLanguageStyle → voiceProfile rescue chain, each composing all prior invariants → §6 / §7 preserve all five invariants), missing-field safety consistently handled, voice-violation regex correctly catches all 4 spec failure-mode classes without killing the 46 catalog entries, priority chain priority order matches spec exactly, default rotation deterministic + cold-start safe, strongPreference flag plumbed cleanly from calibration through to batch guard. **TrendContext layer (light overlay, NOT a new generator):** spec source `attached_assets/Pasted-TREND-CONTEXT-LAYER-...txt`. ZERO schema changes — purely in-memory + additive JSONB envelope (`CachedBatchEntry.trendId?: string`, tolerantly parsed so legacy entries silently skip the cross-batch lever). New `lib/trendCatalog.ts`: `TrendItem { id, type: object|behavior|phrase, label, freshnessScore ∈ [0.3,1.0], compatibleFamilies[], compatibleArchetypeFamilies[] }`, `TREND_CATALOG` (27 hand-curated items: 12 objects + 10 behaviors + 5 phrases), `TREND_BY_ID` lookup, `MIN_FRESHNESS = 0.3`, `TREND_INJECTION_RATE_PCT = 30`. **Selector** `selectTrendForCandidate({slotIndex, scenarioFamily, archetypeFamily, salt})`: deterministic FNV-1a `hashTriple` → `bucket = h%100`; `bucket >= 30` → null (gate skips ~70% by design); else filter `getActiveTrends()` by strict `trendFitsCandidate` (BOTH family and archetypeFamily must match — null/undefined inputs ALWAYS reject so cold-start / fallback candidates without resolved derivation pipeline can't accidentally receive a graft); empty fit set → null (NO forced trend); else freshness-weighted pick using upper-16-bits of the same hash for reproducibility. **Caption transform** `applyTrendToCaption(caption, trend, scenarioTopicNoun)`: `object` → first-occurrence substitute of `topicNoun` with `trend.label` (no-op when noun absent — caller's validator catches it as "substitution failed silently"); `behavior` → append ` (${trend.label})` if result ≤ 80-char ceiling, else no-op; `phrase` → append ` — ${trend.label}` if result ≤ 80, else no-op. Idempotent (`f(f(x)) === f(x)`) for all 3 types. **Validation** `validateTrendInjection(original, transformed, isCleanCaption)` rejects when transform was a no-op OR caller-supplied predicate fails. In `assembleCandidate`: `isCleanCaption = lookupBannedHookPrefix||containsGenericFiller||containsVoiceViolation` (substring-only validators — `validateHook` requires 3-10 word count and would false-reject most captions). On rejection: caption REVERTS to original and `meta.trendId` stays undefined — candidate STILL ships (soft skip per spec — never a whole-batch reject). `assembleCandidate` signature gained `slotIndex`/`slotSalt` params (defaults 0/0); resolutions hoisted above `idea` literal so trend selection sees them; inner-loop call site passes `(out.length, scenarioSeed)`. `PatternMeta.trendId?: string` + `CandidateMeta` fallback union extended with same. **Cross-batch novelty** (light): `NoveltyContext.recentTrendIds?: ReadonlySet<string>` populated by `buildNoveltyContext` from immediate-prior batch envelope's `trendId` field directly (no derivation step needed). `selectionPenalty`: -2 when `meta.trendId ∈ recentTrendIds`, single-tier ONLY (no frequent-last-3 stack and no unused-last-3 boost — trends are an OPTIONAL overlay not a forced rotation axis); skips silently when `trendId === undefined` (the ~70%-by-design norm — gate skip + soft-skip both leave trendId undefined and the penalty NEVER fires against an absent tag). Same -2 sizing as voice-profile cross-batch lever (also single-tier) — strong enough to break ties, soft enough that a high-quality pattern with a repeated trend can still win. `toCacheEntries` + `rescueEntries` write trendId; `tryParseEntries` tolerantly parses (any non-empty string passes — catalog is curator-managed and grows/decays without code deploy, so no enum whitelist; empty strings drop to undefined so the Set never holds a `""` sentinel). NO batch guard, NO regen rescue — trends are an OPTIONAL overlay per spec; we never force ≥1 trend per batch. **QA harness** (`__qa_trend_context.ts`, deleted post-green): 6 sections — catalog invariants (27 items, all 3 types, freshness ∈ [0.3,1.0], unique ids, archetype IDs resolve to known taxonomy), `trendFitsCandidate` strict AND + null-input rejection, selector reproducibility (50 inputs same → same) + emission rate (1000 samples = 26.3% — under the ≤30% target) + fit-only emission (0 forced trends when archetype unsupported), `applyTrendToCaption` idempotency + length cap + per-type behavior, `validateTrendInjection` no-op/clean-predicate gating, `selectionPenalty` -2 cross-batch repeat + 0 fresh + missing-field skip + undefined-set skip — all green. Architect re-review with `includeGitDiff=true`: PASS — selector gate structurally `<=30%`, strict fit predicate enforced, soft-skip/revert behavior verified in `assembleCandidate`, NO schema/DB writes (additive JSONB only), cross-batch penalty wiring correct + skip on absent trendId, `tryParseEntries` keeps trendId optional + drops empty/non-string to undefined preserving legacy cache compatibility, `applyTrendToCaption` idempotency + 80-char ceiling enforced, no caption mutation when validator rejects.
  - **QA hardening (anti-clone, scene-specific copy, no broken hooks):** Layered on top of the novelty selector to fix the "different ideas that read like the same video filmed three ways" failure mode. (1) **Hook completeness validator** — `validateHook` rejects hooks ending in dangling words (`and`, `vs`, `with`, `to`, `the`, `my`, `me`, etc.), unsubstituted `${...}` leaks, ellipsis truncation, or word-count outside 3–10. The old `clampHookWords` chop-to-10 is gone; instead `pickValidatedPhrasing` walks all variants for the chosen style and `assembleCandidate` returns `null` if none ship — the generator silently skips that (template, scenario, style) triple. The `tryRewrite` rescue path (`ideaScorer.ts`) likewise iterates every other style + every variant and returns `null` if nothing validates, never truncating. (2) **HookOpener taxonomy + within-batch HARD guard** — new `HookOpener` enum (9 values: `i_just`, `i_really`, `me_saying`, `the_way_i`, `why_did_i`, `pov_check`, `tell_me_why`, `what_i_planned_vs`, `the_audacity`) tagged on each `HookPhrasingEntry` and resolvable from any hook string via `lookupHookOpener` regex table; `batchGuardsPass` HARD-caps each opener at 1/batch (so a batch can't ship three "I just realized…" hooks even when hookStyle differs). (3) **Setting expansion + within-batch HARD guard** — scenario pool grew to 25 entries spanning all 8 settings (`bed`/`couch`/`desk`/`bathroom`/`kitchen`/`car`/`outside`/`other`) — added new families (`skincare`, `mirror_pep_talk`, `walk`, `doom_scroll_car`, `closet_pile`); `batchGuardsPass` HARD-caps each `setting` at 1/batch so three kitchen scenes never ship together. (4) **Per-visualAction `howToFilm`** — `HOW_TO_FILM_BY_VISUAL_ACTION` lookup with 13 framing snippets keyed by `visualActionPattern` replaces the v1 boilerplate "Phone propped at chest height". `batchGuardsPass` also HARD-rejects byte-identical `howToFilm` strings within a batch as defense in depth. (5) **Scene-specific `whatToShow`** — new `sceneBeat` field on every `Scenario` (1–2 sentence physical moment, e.g. "You step in front of the mirror in the new outfit, hold the look for one beat, then turn away") drives `buildWhatToShow`, replacing the generic template. (6) **Cross-batch novelty extension** — `NoveltyContext` gained `recentHookOpeners` (derived from cached hook text via `lookupHookOpener`, zero cache-shape change) and `recentSettings` (from `idea.setting`); `scoreNovelty` is now 0–7 (added opener + setting axes); `selectionPenalty` adds -2 same-opener and -2 same-setting. **QA gate (5 regens, deleted post-pass):** every hook validated, no 2+ openers per batch, no 2+ settings per batch, no identical `howToFilm`, all `whatToShow` ≥20 chars + scene-specific, no v1 boilerplate substring, `source: "pattern"` and `usedFallback: false` on all 5 batches.
  - **Trend context layer + archetype pairing system:** Lightweight cultural-relevance overlay so the deterministic engine doesn't sound stuck-in-2023, layered on top of the novelty selector + QA validators without any schema/cache shape changes. (1) **Curated catalog (`trendCatalog.ts`)** — 31 hand-curated `TrendItem`s spanning 4 types: `object` (substitute the scenario topic noun, e.g. "matcha" replacing "coffee"), `behavior` (parenthetical append, e.g. "(stanley cup obsession)"), `phrase` (em-dash append, e.g. " — quiet luxury"), and `format` (parenthetical append describing a video format, e.g. "(voice memo style)"). Each item carries `compatibleFamilies` (positive scenario fit), `compatibleArchetypeFamilies` (positive archetype fit), `freshnessScore ∈ [0.3, 1.0]`, optional `region` (creator-locale gate), and optional `avoidArchetypeFamilies` (negative compat — hard-rejects e.g. matcha + social_observation, stanley_cup + escalation, tiktok_dance_format + low_energy_realism). (2) **Strict-fit selector** — `selectTrendForCandidate` hashes `(slotIndex, scenarioFamily, salt)` into a 100-bucket gate; `bucket >= 30` returns null (caps emission rate at 30%). Surviving candidates are filtered by `trendFitsCandidate` (negative compat hard-reject FIRST, then positive scenario + archetype fit, then optional region match with global-default semantics — either side absent → accept). Final pick is freshness-weighted via the upper-16-bits of the same hash, fully deterministic for cache replay. (3) **Caption injection + soft-skip validator** — `applyTrendToCaption` is pure/idempotent with a tight 80-char ceiling per type; `validateTrendInjection` rejects no-op transforms (`original === transformed`) AND any transform that fails the caller's `isCleanCaption` predicate (banned-prefix / generic-filler / voice-violation). Soft-skip: when validation fails, the candidate STILL ships, just without the trend tag (no whole-batch rejection — trends are an OPTIONAL overlay). **Frequency tuning (Session 3 — spec source `attached_assets/Pasted-TREND-FREQUENCY-TUNING-SAFE-MOVE-TOWARD-20-Goal-Increas_*.txt`):** moved Session 2's freshness-weighted random pick to a 0–10 best-score selector to lift visible emission from ~12% into the 15–25% target band WITHOUT forcing weak pairings. ZERO schema changes — purely additive optional fields on `TrendItem`. (a) **Schema additions** — `secondaryFitArchetypes` / `secondaryFitFamilies` (relaxed positive fit, only consulted when primary pool empty), `avoidFamilies` (negative scenario compat — hard-rejected by predicate alongside existing `avoidArchetypeFamilies`), `niche` flag (-2 score penalty for too-online/reference-locked items like labubu + delulu_planning). (b) **Selector overhaul** — `selectTrendForCandidate` keeps the 30% hash-bucket gate, then builds a PRIMARY pool via `trendFitsCandidate`; if empty, falls back to a SECONDARY pool via the new `trendFitsCandidateSecondary` predicate (union of secondary + primary fit lists, same hard-reject discipline). Every candidate scored via NEW exported `scoreTrendFit(trend, scenarioFamily, archetypeFamily, region?, recentTrendIds?)`: +4 primary archetype, +3 primary family, +2 secondary archetype (only if no primary archetype matched), +1 secondary family (only if no primary family matched), +1 region/global (predicate-pass implicit), +1 freshness ≥ 0.7, -5 avoid (defensive double-defense), -3 weak fit (BOTH primaries missing), -3 decorative phrase/format with no archetype anchor, -2 niche, -2 repeated trend (`recentTrendIds` accepts `Set` OR readonly array, narrowed via `Array.isArray`). Threshold `TREND_FIT_THRESHOLD = 6`; sub-threshold candidates skipped (return null rather than ship a forced graft). Highest-score wins; ties broken deterministically by `(hash >>> 16) % tied.length`. (c) **Catalog tightening** — `girl_dinner` removed `identity` from `compatibleArchetypeFamilies` and added `avoidFamilies: ["productivity","errands","emails"]` per spec ("girl dinner avoids productivity / to-do list / planning / work-task"); secondary tags added to `hot_girl_walk`, `quiet_luxury`, `fake_busy_mode`, `voice_memo_format` so the secondary pool has live coverage; `niche: true` on `labubu` + `delulu_planning`. (d) **Validation** — pure-logic harness `__qa_trend_tuning.ts` (deleted post-pass — 203/203 PASS) covered score-boundary matrix (every bonus/penalty), threshold gating (6 keep / 5 reject), `girl_dinner` full archetype matrix × 3 forbidden families, secondary fallback discipline (only consulted when primary empty, never stacks on top), per-combo simulation across 5000 random samples (18.7% emission — squarely in 15-25% band), all Session 1+2 invariants preserved (idempotency, region matrix, hard cap, format type, ~30% gate, no positive ∩ negative overlap). Runtime QA across 10 batches (mixed regions western/india/philippines/nigeria, count 3+5, regenerate=true, full-catalog label net): **17.5% rate (7/40 ideas)**, zero `girl_dinner` + productivity-flavor pairings, hard cap held in all 10 batches, 4 distinct trends fired (`zara_dress`, `girl_dinner`, `lazy_GRWM`, `doomscroll_arc`), all 4 regions covered. Architect verdict: PASS — no HIGH defects; predicate ↔ score semantic consistency confirmed; tie-break deterministic and modulo-bias-immaterial; `recentTrendIds` union-narrowing correct. (4) **Within-batch HARD CAP (`enforceTrendCap` in `hybridIdeator.ts`)** — finalization pass enforces ≤ N-1 trend-injected candidates per N-pick batch (so a 3-pick batch can never ship 3-of-3 trended). When the cap fires, the LOWEST-priority trend-injected candidate has `meta.trendId` cleared AND its caption reverted from `meta.originalCaption` (a paired snapshot captured by `assembleCandidate` at injection time) — the candidate STILL ships, just untagged. Priority semantics: main path uses `score.total` (drops lowest-scoring), rescue path uses `-i` (drops last in slice). Cap formula `Math.max(1, n-1)` allows single-pick batches to carry 1 trend. (5) **Cross-batch novelty lever** — `meta.trendId` persisted on the JSONB cache envelope alongside `voiceProfile` / `hookLanguageStyle`; `buildNoveltyContext` reads `recentTrendIds` from the immediate-prior batch and `selectionPenalty` applies -2 demotion so the same trend doesn't dominate back-to-back generations. **QA harness (`__qa_trend_pairing.ts`, deleted post-pass — 39/39 PASS):** format type produces parenthetical + idempotent + length-capped; avoidArchetypeFamilies hard-rejects (predicate-only); region matrix (US/US accepts, US/EU rejects, none/EU accepts, US/none accepts, none/none accepts); selector emission rate ≈ 30% over 1000 samples within ±5pp band; catalog invariants (≥28 items, all 4 types present, no positive ∩ negative archetype overlap, freshness ∈ [0.3, 1.0], TREND_BY_ID coverage); enforceTrendCap (3-of-3 → drops exactly 1, 2-of-3 no-op, 0-trend no-op, 1-pick allows 1, rescue drops LAST); idempotency for all 4 types + validator soft-skip rejects/accepts. **Architect verdict:** PASS — no medium/severe defects; pairing semantics sound; trendId ↔ originalCaption pairing preserved across `tryRewrite` / `maybeMutateBatch` (both spread `meta`); `final` const→let reassignment correctly propagates capped state to cache write + source counters.
- Users provide "Yes / Maybe / No" feedback on ideas, influencing future generation and trending boards.
- AI-generated ideas are the sole primary entry into the filming/create flow.
- The import stage supports adding 1-2 clips with "Film" or "Upload" options.
- The "Ready to post?" stage confirms video readiness with hook + clip + caption and offers "Export video" or "Make another version."
- Film-screen UX includes a "Why this works" block derived from idea metadata.
- Camera permission denial is handled gracefully, allowing users to upload instead.
- Four fixed timing templates are deterministically selected based on the Ideator's `templateHint`.
- **Enhancement Brain:** Provides up to 3 typed suggestions (caption, hook, start hint, manual) for video improvement, reusing style derivation and viral pattern memory.
- **Semi-auto Suggestion Apply:** Allows applying suggestions with immediate visual feedback in the Before/After frame, triggering an `applied_enhancement` signal.
- **Semi-auto Edit (stitch + trim):** Offers `stitch_clips` and `trim_start` actions with visual confirmation in the Before/After frame. No on-device video processing; changes are intent-only for future processing.
- **QA-mode onboarding bypass:** Allows skipping onboarding for e2e tests.
- **Visible adaptation flow (Quick Tune):** A 3-step auto-advancing flow (format → tone → hook) that biases idea generation based on user preferences, with visual feedback and a "Better match?" prompt.
- **Quick Tune onboarding trigger:** Triggers the Quick Tune flow based on client-side state after a certain number of ideas are viewed or scrolled.
- **Daily-habit copy lock:** Home screen H1 and sub are locked to "3 ideas for today" / "Made for your style." Daily return messages vary based on user history.
- **Single-idea swipe feed (preview):** New `/feed` route renders one idea at a time via `SwipeIdeaCard` (hero hook + non-numeric viral-feel badge + pattern preview + why-it-works). Static visual-direction pass — no gestures, no reaction persistence, no backend changes; reuses `dailyIdeasCache` with a built-in mock fallback. Existing tab nav untouched.
- **PHASE X — Default Taste Engine (Steps 1+2):** Backend lever that biases the existing 3-layer Ideator pipeline toward premise-first comedy. New `defaultTasteProfile.ts` is the single source of truth for mechanism/joke-shape/language taste mappings. Layer-2 `filterAndRescore` adds a bounded boost (cap +4) to candidates whose `premiseStyleId`, `hookLanguageStyle`, and `usedBigPremise` flags align with the profile (the +1.5 `PREMISE_FIRST_BONUS` is the dominant lever). Layer-3 (`ideaGen.ts`) Claude fallback prompt now requires a `premise` sentence FIRST, then derives hook + trigger + reaction + whatToShow + howToFilm from it; `premise` is added to `ideaSchema` as optional and stripped from the public API response (server-internal, same pattern as `meta.viralFeelScore`). PART 4 anti-loop (last-5 domains/mechanisms) was already implemented via the existing `MAX_HISTORY_BATCHES=4` + `recentPremiseStyleIds`/`recentFamilies` machinery — no code change required.
- **PHASE X2 — Default Taste Engine completion (heuristic, no LLM verifier).** New `comedyValidation.ts` adds three deterministic gates that fire inside `filterAndRescore` after scoring + DefaultTaste boost but before the score-floor cut: (1) PART 1 hard comedy validation requires ≥2 of {tension, contradiction, intention/reality clash, escalation, specific behavior, identity exposure, self-as-other framing} via regex, plus a softness-token rejection; vacuous-passes Layer-1 candidates whose `source === "pattern_variation"` OR `usedBigPremise === true` (curated catalog IS comedy ground truth). (2) PART 2 alignment guarantee: `whatToShow` must share ≥2 content tokens (length-3+ alphanum, stop-words removed) with the hook OR trigger individually (not summed); `howToFilm` must share ≥1 with `whatToShow`; when `idea.premise` is present (Layer-3 only), the hook must share ≥1 token with the premise. (3) PART 4 anti-copy guard fingerprints normalized hooks against ~200 curated `PREMISE_STYLE_DEFS.executions[].example` strings (lazy module-scope cache via `loadSeedHookFingerprints`); pattern_variation candidates exempt because they ARE the seed corpus by design. Premise-skeleton dup check uses a `recentPremises` set built from the last-5 cached batches' `idea.premise` fields, threaded into both local + Claude-fallback `filterAndRescore` calls from `hybridIdeator`. PART 3 cross-batch mechanism demotion bumped from -2 to -3 in `selectionPenalty` so a repeat fine-grained `premiseStyleId` stacks with the bucket-level -2 (combined -5 vs typical 3-4pt premise-vs-alt spread). PART 5 fail-open: no retry loop — the existing rescue path in `hybridIdeator` (final.length===0 ⇒ ship unfiltered top-of-pool) absorbs over-rejection. PART 6 telemetry: `FilterAndRescoreResult.rejectionReasons` per-reason counters surfaced in a new `hybrid_ideator.validation_summary` info log (always fires) and added to the existing `guards_failed_shipping_best_effort` warn log. PART 7 QA: 5-batch smoke against demo creator confirms 14/16 keep rate with measurable rejections (`hook_scenario_mismatch`, `filming_mismatch`) and zero empty batches. NO UI changes. NO Premise Core Library yet.
- **PHASE Y2 — Layer-1 Core-Aware Wiring (additive, surgical, NO UI/DB/schema changes).** Layered on PHASE Y to lift premise-core coverage from the Y1 baseline of ~3% (2/75 ideas) to ~9% (5/57 ideas) by promoting the Claude-with-cores call onto the PRIMARY `regenerate=true` path instead of only firing on Layer-1 underfill / guards-fail. ZERO schema / DB / UI / cache-shape / Phase 3-7 / Phase 6-series / Phase X / Phase X2 / Phase Y / hard-validator / scoring-philosophy changes; the cheap path (`regenerate=false` normal home-screen taps) still skips the Claude-with-cores call entirely so sub-second local-pattern latency is preserved. **`hybridIdeator.ts` PART 1a (eager Claude-cores trigger):** the existing `needFallback` gate now ORs in `regenerate` so `selectPremiseCores` + the `generateIdeas({ premiseCoreSeeds })` call fires on every `regenerate=true` batch (not just on Layer-1 fallback triggers). Telemetry: new `phase_y2.layer1_core_aware_used` info log emitted on every triggered call with `regenerate` / `desiredCount` / `claudeKept` / `localKeptBefore` / `mergedSize` / `promotionReason` / `coreTaggedInPool` fields. **`hybridIdeator.ts` PART 1b (pool promotion):** when Claude's wrapped+filtered pool can fill `desiredCount` solo (`claudeCanFillSolo`), the deterministic local pool is dropped from the merge so core-tagged ideas dominate selection. When Claude short-delivers, the merge falls back to today's behavior (claudeKept ∪ localKept). The promotion-vs-merge decision is recorded in `promotionReason` (`claude_solo_promoted` / `merged_core_aware_short` / `merged_local_only_short`). **`hybridIdeator.ts` PART 1c (core-priority swap, post-architect-iteration fix):** added a post-`selectWithNovelty` swap phase that forcibly displaces the lowest-scored non-core picks with any core-tagged Claude candidate from `merged` that didn't make the cut. Required because the +2 fresh-core scorer lever (`ideaScorer.ts` L3186-3192) self-suppresses on cold-start when `recentPremiseCoreIds.size === 0` — meaning the very batches we need to bootstrap core coverage on are also the ones where core-tagged candidates get ZERO selection bias against the local pattern pool's same-hook duplicates (e.g. `"i ghosted my own to-do list"` scoring composite ~10 vs core-tagged candidates' ~3). The Y2 round-1 QA (without the swap) shipped 1/57 = 1.8% core-tagged WORSE than Y1 baseline; round-2 (with the swap) shipped 5/57 = 8.8% — 3× Y1 baseline. Telemetry: new `phase_y2.core_priority_swap` info log on every fired swap with `swappedIn` / `coreIdsAdded` / `postCoreCount` fields. **`ai.ts` (SDK timeout):** added `timeout: 60_000` + `maxRetries: 1` to the `Anthropic` SDK constructor — without this, a stalled Claude call inherited the SDK's ~10min default which hung Y2 batches in `top-up`. Caller code already has graceful try/catch degradation; this just bounds the wait so undercount can fall through to local-pool top-up cleanly. **QA verification (`POST /api/_qa/phase-y2` temp route, 11 batches: 8×regenerate=true count=6 + 3×regenerate=false count=3, 57 shipped ideas total, JSON saved /tmp/qa_phase_y2.json):** `premiseCoreId` coverage 5/57 = 8.8% vs Y1 3% (3× lift, all 5 from regenerate-only slots = 5/48 = 10.4% on the regen path); top hook repeat 10× ("i ghosted my own to-do list") vs Y1 14×; cheap-path `regenerate=false` batches (8/9/10) shipped 0 core-tagged ideas as designed (preserves sub-second latency); 3 core-priority swaps fired across 8 regen batches contributing to the 5 shipped core ideas in 3 distinct batches; per-batch claudeKept variability remains the binding ceiling (Claude returns 1-3 valid candidates per call after `applyExclusionToFallback` + `filterAndRescore`, then top-up adds 1-2 more — net mean 0.6 per batch). Heuristic post-worthy (59.6%) and hook/scenario mismatch (80.7%) numbers used a strict programmatic 5+char-shared-noun rubric and are NOT directly comparable to Y1's 9% / 21% manual review numbers — the trustworthy comparators are the `premiseCoreId` coverage delta and top-hook repeat count. **Architect review (PASS, no blockers):** wiring is correct, telemetry fires, cheap path preserved, swap logic is bounded (only displaces non-core picks). The remaining ceiling is Claude's post-validation survival rate (out of scope for Y2 — flagged for a Y3 follow-up: rewrite the local pattern catalog's top scorers to stop dominating the merge OR tag the local pool's pattern variations with synthesized premise-core ids OR route a fixed slot quota to core-tagged candidates in `selectWithNovelty` itself). **Cleanup:** the temporary `POST /api/_qa/phase-y2` route file (`_qaPhaseY.ts`) + its import + `router.use` line in `routes/index.ts` were removed after validation; route returns 404 post-cleanup; `pnpm --filter @workspace/api-server exec tsc --noEmit` clean. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6B / Phase 6C / Phase 6D / Phase 6E / Phase 6F / Phase 7 / Phase X / Phase X2 / Phase Y all frozen — no scoring philosophy / HookIntent catalog / pattern catalog / Phase 5 / hard-validator / catalog-removal / DefaultTaste / cache-shape changes, no DB schema changes, no premise-share gate changes, no UI changes; public response keys still exclude both `premise` and `premiseCoreId`.
- **PHASE Y — Premise Core Library (additive, surgical, NO UI changes).** Adds 40 ultra-refined comedic-engine "cores" (8 families × 5) as the PRIMARY seed for the LLM (Layer-3 / Claude-fallback) generation path, working WITH (not replacing) `defaultTasteProfile.ts` + the 50-id `PremiseStyle` layer + the Layer-1 `pattern_variation` catalog. Spec source: in-session prompt — "PHASE Y PREMISE CORE LIBRARY". Pure TS-additive; ZERO schema changes; ZERO Phase 3-7 / Phase 6-series / Phase X / Phase X2 / hard-validator / cache-shape-breaking / scoring-philosophy changes; Layer-1 batches NEVER see cores (cores only injected on the Claude fallback path so the curated catalog remains untouched ground truth). **NEW MODULE `premiseCoreLibrary.ts` (~1255 lines):** exports `PremiseCore` type, `PremiseCoreFamily` union (`self_betrayal`, `absurd_escalation`, `self_as_relationship`, `adulting_chaos`, `dopamine_overthinking`, `social_mask`, `confident_vs_real`, `identity_exposure`), and `PREMISE_CORES` — `Object.freeze`d array of EXACTLY 40 cores (5-per-family invariant asserted at module load alongside count===40, unique-id, and `compatiblePremiseStyles ⊆ PREMISE_STYLE_IDS` checks). Each core carries `id` / `family` / `mechanism` / `tension` / `description` / `generatorRule` (the rule the LLM must use to write the premise sentence) / `compatibleDomains` / `compatiblePremiseStyles` / `examples` (2-3 anti-copy seeds — explicitly forbidden phrasings) / `antiCopyHints`. `FAMILY_DEFAULT_TASTE_WEIGHT` mirrors the `defaultTasteProfile.ts` mechanism-weight bias so the selector's family-level prior aligns with the system's existing taste prior without re-encoding it. **Selector (`selectPremiseCores`):** weighted-without-replacement draw with three stacked recency demotions — id-recency ×0.2 (last-5 batches' core ids), mechanism-recency ×0.4 (last-5 batches' mechanisms), per-pick family-demotion ×0.5 (re-applied after each pick to discourage same-family back-to-back inside the seed set), all clamped to a `>=0.01` floor so the picker never starves. Post-pick the selector enforces a `≥1 fresh core` invariant when `recentIds.size > 0`: if all picks are recently-seen, the selector swaps the lowest-effective-weight pick for the highest-effective-weight unpicked-and-fresh candidate using the SAME composite (family taste × id-recency × mechanism-recency × accumulated family-demotion) the main draw uses (post-architect-review fix — earlier draft used raw family weight which mis-aligned the swap with the rest of the picker). Returns `{ cores, hasFreshCore }`. `formatPremiseCoresForPrompt(cores)` renders the SYSTEM-prompt block listing each core's id / family / mechanism / tension / generatorRule / compatibleDomains / forbidden-seed-phrasings (built from `examples`) / anti-copy hints, with the explicit instruction header forbidding example-phrasing regurgitation. `_resetPremiseCoreStateForTests()` ships as a no-op forward-compat seam (the catalog is frozen with no module-scope mutable state today). **`ideaGen.ts` integration:** `ideaSchema` gains optional `premiseCoreId: z.string().min(1).max(120).optional()` (server-internal — same strip-from-public-response discipline as `premise`). `GenerateIdeasInput` gains `premiseCoreSeeds?: readonly PremiseCore[]`. The system prompt array prepends `premiseCoreBlock` (rendered via `formatPremiseCoresForPrompt`) ABOVE the existing PHASE X premise-first instructions; when seeds are absent the block is `""` and `.filter(Boolean)` collapses it to a no-op. Both `=== TASK ===` JSON-shape directives (the count==1 path at L1100 and the count>1 path at L1271) list `premiseCoreId` alongside `premise` so the model knows to emit the chosen core's id back. **`routes/ideator.ts`:** L305 destructure now strips `premiseCoreId` alongside `premise` from the public response (verified: shipped JSON contains neither field). **`hybridIdeator.ts`:** imports `selectPremiseCores` + `getPremiseCoreById`. `wrapFallbackIdea` mirrors `idea.premiseCoreId` → `meta.premiseCoreId` so the scorer's meta-accessor sees it on Claude-fallback candidates (Layer-1 catalog candidates never carry the field). `buildNoveltyContext` populates `recentPremiseCoreIds` from `last3Batches` reading `e.idea.premiseCoreId` directly off the cached envelope (NO cache-shape change — pre-PHASE-Y rows simply contribute nothing, since the field is opportunistically read as `undefined`). Pre-Claude-fallback the function builds `recentPremiseCoreIdsForSeeding` + `recentMechanismsForSeeding` from a `last5BatchesForPremises` window and calls `selectPremiseCores({ count: desiredCount + 2, recentCoreIds, recentMechanisms })`, threads `coreSelection.cores` into `generateIdeas` via `premiseCoreSeeds`, and emits a `phase_y.premise_cores_selected` info log (always when fallback fires) with `selectedCoreIds` / `selectedFamilies` / `recentCoreCount` / `recentMechanismCount` / `hasFreshCore`. **`ideaScorer.ts`:** `CandidateMeta` Claude-fallback branch gains optional `premiseCoreId?: string`; new `metaPremiseCoreId` accessor mirrors `metaPremiseStyleId`. `NoveltyContext` gains optional `recentPremiseCoreIds?: ReadonlySet<string>`. `selectionPenalty` adds a -3 cross-batch demotion when the candidate's `meta.premiseCoreId` is in `recentPremiseCoreIds` and a +2 fresh-core boost when it's not — exactly mirroring the existing `premiseStyleId` -3/+2 lever (so a Claude wrap that re-uses BOTH a core AND a premiseStyleId from a recent batch eats -6, while a fresh-on-both wrap gains +4; intentionally aggressive rotation given the +3 PREMISE_PREFERENCE bonus + typical 1-2pt scoring spread, treated as a deliberate calibration lever). **QA verification:** `pnpm --filter @workspace/api-server run typecheck` clean; 5 sequential normal batches all HTTP 200 with 3 ideas each (no Layer-1 regression); forced-fallback batch (`count=6, regenerate=true`) HTTP 200 with 6 ideas, `phase_y.premise_cores_selected` log fired with `seedCount=8` (=6+2), 8 distinct cores spanning 7 of 8 families, `hasFreshCore=true`, and the `usedFallback=true / fallbackKept=2` validation summary confirming the cores propagated into the Claude prompt and produced shipping ideas; public response keys exclude both `premise` and `premiseCoreId`. **Architect review (PASS, no blockers):** core library integrity strong, selector math sound (non-starvation guaranteed), integration surgical/additive, cache compatibility correct (pre-PHASE-Y rows tolerate the new field's absence), score-stack magnitude flagged as a deliberate calibration lever; one non-blocking math fix (fresh-swap effective-weight alignment) applied in this same task. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6B / Phase 6C / Phase 6D / Phase 6E / Phase 6F / Phase 7 / Phase X / Phase X2 all frozen — no scoring philosophy / HookIntent catalog / pattern catalog / Phase 5 / hard-validator / catalog-removal / DefaultTaste / cache-shape changes, no DB schema changes, no premise-share gate changes, no UI changes.
- **PHASE Y4 — Strengthened Cross-Batch Repeat Penalties (additive, surgical, NO UI/DB/schema changes).** Layered on PHASE Y3 to fix the Y3 top-hook regression (Y2 10× → Y3 14× because Y3's D-lite +3 cap left `min(+7,+3) − 2 = +1` still positive — capped premium hooks still beat neutral legacy 0). Pure selection-pressure tightening; ZERO mapping / library / Claude / quota / UI / API / schema / cache-shape / scoring-philosophy / Phase 3-7 / Phase 6-series / Phase X-series / Phase Y-series changes; public API still strips `premiseCoreId` (route strip site at `routes/ideator.ts` L305-308 unchanged); LLM cost flat. **`ideaScorer.ts` `selectionPenalty` (PART 1 — strengthen recent-id demotes):** the existing per-axis cross-batch demotes were tightened in place — `premiseStyleId` recent: `−3 → −5`; `executionId` recent: `−2 → −5`; `premiseCoreId` recent: `−3 → −5`. Symmetric `+2` fresh-axis boosts kept unchanged so a fresh creator's first batches still get the cold-start premium. Y3 D-lite `Math.min(baseBoost, 3)` cap on `premiseComedyBoost` PRESERVED unchanged — the Y3 lever is still useful as the upper bound on the boost itself. **PART 2 — combo demote when style + execution both recent:** new `if (styleRecent && execRecent) p −= 4;` block immediately after the per-axis blocks; fires even when the candidate's `premiseCoreId` is fresh, so a same-(style, execution) shell with a different core still loses to a fresh neutral candidate. **PART 3 — near-shell demote when style + execution + core ALL recent:** stacks an additional `if (coreRecent) p −= 4;` inside the same combo block (only fires when PART 2 already fired), so a complete (style, execution, core) re-pick is the worst-case candidate the selector can see. Combined worst-case math (documented in JSDoc): score-10 premium with all three axes recent and D-lite cap engaged = `min(+7, +3) − 5 − 5 − 5 − 4 − 4 = −20` (strictly loses to fresh neutral legacy 0); fresh-axis premium unchanged at `+7` (still wins vs fresh legacy `+5`). **`hybridIdeator.ts` telemetry on existing `hybrid_ideator.served` log (additive, no new event):** 5 new fields appended to the served log — `selectedRepeatedPremiseStyleCount` / `selectedRepeatedExecutionCount` / `selectedRepeatedPremiseCoreCount` / `selectedSameStyleExecutionCount` / `selectedSameStyleExecutionCoreCount`. Computed in-memory from `final[]` ∩ `noveltyContext.recent*` sets at zero LLM cost; the (style+exec+core)-all-recent counter is the binding acceptance metric (a near-shell shipped despite stacked −20 demote = penalty failure). The early `source: "cache"` return path emits the same five fields with literal `0` values so the served-log schema is identical across all paths (cache replay doesn't run `selectionPenalty` against a fresh novelty context, so the counters aren't measurable there — emitting 0 keeps downstream parsers / dashboard aggregations correct without NPE on missing keys). **QA verification (`POST /api/_qa/phaseY` temp route, 15 batches: 10×regenerate=true count=6 + 5×regenerate=false count=6, 77 shipped ideas total — 13 short of 90 due to harness-side daily AI cost cap exhaustion mid-run, JSON saved `.local/reports/qa_phase_y4.json` + report `.local/reports/phase_y4_report.md` + computed metrics `.local/reports/y4_metrics.json`):** **top hook repeat 6 vs Y3 14 (−57%) ✅ — Y3 regression undone.** All 15 served events emit the new selected-set counters; across the full run, sum of `selectedSameStyleExecutionCount` = 0, `selectedSameStyleExecutionCoreCount` = 0, `selectedRepeatedPremiseCoreCount` = 0 (zero near-shells shipped — exact spec PART 6 acceptance); `selectedRepeatedExecutionCount` = 1 in 2 of 15 events, 0 elsewhere; `selectedRepeatedPremiseStyleCount` = 0 across all 15. Distinct hooks 43/77 = 55.8% (vs Y3 30.0% — +25 pp). Honest caveats called out in report: (1) daily AI cap (`$10.0992 of $10.00`) hit at batch ~6 — subsequent batches degraded to local+fallback only, dropping `premiseCoreId` coverage from Y3 baseline 40.0% → 16.9% (cap-driven, NOT a Y4 regression), and (2) batches #11-14 served from same-day cache (`durationMs: 0`) so the normal slice's apparent variety is effectively n=1 — both artefacts are harness-level, not Y4-changes-level, but they mean the cap-driven rows of the delta table (latency / coverage / underfill) are NOT informative deltas vs Y3. **Cleanup:** the temporary `POST /api/_qa/phaseY` route file (`_qaPhaseY.ts`) + its import + `router.use` line in `routes/index.ts` were removed after validation; route returns 404 post-cleanup; `pnpm --filter @workspace/api-server exec tsc --noEmit` clean. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6B / Phase 6C / Phase 6D / Phase 6E / Phase 6F / Phase 7 / Phase X / Phase X2 / Phase Y / Phase Y2 / Phase Y3 all frozen — no scoring philosophy / HookIntent catalog / pattern catalog / Phase 5 / hard-validator / catalog-removal / DefaultTaste / cache-shape changes, no DB schema changes, no premise-share gate changes, no UI changes; public response keys still exclude both `premise` and `premiseCoreId`.
- **PHASE Y3 — Local-Pool premiseCoreId Tagging + D-lite Boost Cap (additive, surgical, NO UI/DB/schema changes).** Layered on PHASE Y2 to lift premise-core coverage from the Y2 baseline of 8.8% (5/57 ideas) to 40.0% (36/90 ideas — 4.5× lift) by deterministically tagging local pattern-pool (Layer-1) candidates with a `premiseCoreId` synthesized from a high-confidence `(premiseStyleId, executionId) → premiseCoreId` lookup table, AND capping runaway `premiseComedyBoost` at +3 (D-lite) when the candidate's premise style/execution is already in the recent set. ZERO schema / DB / UI / cache-shape / Phase 3-7 / Phase 6-series / Phase X / Phase X2 / Phase Y / Phase Y2 / hard-validator / scoring-philosophy / LLM-cost changes; public API still strips `premiseCoreId` (route strip site at `routes/ideator.ts` L305-308 unchanged). **`lib/premiseCoreLocalMapping.ts` (new module):** exports a 41-entry frozen `LOCAL_CORE_MAPPING_ENTRIES` tuple list + a derived `Map<string, string>` keyed `${premiseStyleId}::${executionId}` → `premiseCoreId`, plus a pure deterministic `resolvePremiseCoreIdForLocalCandidate({ premiseStyleId, executionId })` resolver returning `string | undefined`. Mapping is conservative: a pair is mapped only when its `premiseStyleId` appears in the chosen core's `compatiblePremiseStyles[]` allow-list AND the `executionId` semantics align with the core's `mechanism` / `tension` (verified against `premiseCoreLibrary.ts` source). 8 distinct premiseStyleIds × 4-5 executions = 41 high-confidence pairs covering the most prolific premium catalog families (`todo_termination` / `procrastination_paradox` / `self_destruction_speedrun` / `self_sabotage_scrollstop` / `main_character_meltdown` / `mundane_meltdown` / `overdramatic_reframe` / `collapse_core` / `duality_clash` / `dream_disappointment` / `pattern_exposure` / `self_roast_reactor`); ambiguous pairs (e.g. `burnout_betrayal × metaphor_mayhem`, `dopamine_denial × *`) deliberately left unmapped — telemetry surfaces them for follow-up. **Boot-time drift validation (cycle-safe):** the mapping module deliberately has ZERO runtime import on `premiseCoreLibrary.ts` — only `import type { PremiseStyleId }` from `patternIdeator.ts` (erased at emit). Any runtime back-edge would close a 3-way cycle on the hot path (`patternIdeator → premiseCoreLocalMapping → premiseCoreLibrary → patternIdeator`) whose worst case is ESM partial-snapshot TDZ on whichever module the entry point loads first. Drift detection moved to an exported `validateLocalCoreMappingAgainst(cores)` function called once from `src/index.ts` `boot()` BEFORE `runMigrations()` + `app.listen()` — the api-server refuses to start (boot().catch logs + exits) if any mapped `premiseCoreId` is missing from `PREMISE_CORES`. Earlier draft tried lazy first-call validation; architect review surfaced that the static `import { PREMISE_CORES }` participates in module resolution itself (not just first-read), so the lazy guard could not actually neutralize the cycle — only removing the runtime import edge does. **`patternIdeator.ts` wiring (additive):** new optional `premiseCoreId?: string` field on `PatternMeta` (with detailed JSDoc explaining the cache-resident `idea.premiseCoreId` mirror vs the runtime-only `meta.premiseCoreId` mirror — same value, two surfaces). `assembleCandidate` computes `localPremiseCoreId` once via `resolvePremiseCoreIdForLocalCandidate` (returns undefined when entry is not `bigPremise`, when either fine-grained tag is missing, or when the pair is intentionally unmapped); spread-when-present onto BOTH `idea.premiseCoreId` (cache-resident, picked up by `buildNoveltyContext`'s existing `e.idea.premiseCoreId` read site introduced in PHASE Y for Layer-3 Claude candidates — no envelope-shape change required) AND `meta.premiseCoreId` (runtime-only, mirrors hybridIdeator's existing meta.premiseCoreId mirroring at the Claude wrap site for in-flight `recentCoreIds` rescue-filter dedup). **`ideaScorer.ts` D-lite cap:** the existing unconditional `premiseComedyBoost(c.meta.premiseComedyScore?.total)` at the `usedBigPremise` site is now wrapped in a recent-id check — when `c.meta.premiseStyleId` is in `ctx.recentPremiseStyleIds` OR `c.meta.executionId` is in `ctx.recentExecutionIds` (same OR-discipline as the existing `-2` cross-batch demote levers below), the boost is capped via `Math.min(baseBoost, 3)`; fresh entries get the full boost unchanged. JSDoc block walks through the math: fresh score-10 premium = +7 (preferred over fresh legacy +5); recent score-10 premium = `min(+7, +3) − 2 = +1` (loses to fresh legacy +5, ties / wins vs neutral legacy 0); fresh score-5 = -2 unchanged; recent score-5 = `min(-2, +3) − 2 = -4` (unchanged demote). **`hybridIdeator.ts` telemetry on existing `hybrid_ideator.served` log:** 5 new fields appended (no new event) — `localPremiseCoreTaggedCount` (count of `pattern_variation` candidates with `usedBigPremise === true` AND a resolved `meta.premiseCoreId`), `localPremiseCoreTaggedRate` (tagged / (tagged + unmapped-with-fine-grained-tags)), `localPremiseCoreUnmappedCount` (premium with both fine-grained tags but no resolved core — the actionable signal), `mappedPremiseCoreIds` (array of distinct cores resolved this batch), and `topUnmappedPremiseHooks` (top 3 `{hook, count}` premium hooks whose pair didn't resolve — feeds the next mapping-spec pass). **QA verification (`POST /api/_qa/phaseY` temp route, 15 batches: 10×regenerate=true count=6 + 5×regenerate=false count=6, 90 shipped ideas total, JSON saved `.local/reports/qa_phase_y3.json` + report at `.local/reports/phase_y3_report.md`):** `premiseCoreId` coverage 36/90 = 40.0% vs Y2 8.8% (4.5× lift); regen slice 26/60 = 43.3% (below 50-70% spec target — see honest-gap below), normal slice 10/30 = 33.3%; 10 distinct cores shipped (vs 4 in Y2); telemetry verified live (`localPremiseCoreTaggedRate` observed 0.27-1.00 per batch, `topUnmappedPremiseHooks` populating consistently with 3 candidate hooks per batch). Mean batch latency 63.9s, p95 88.9s — flat vs Y2 (no new LLM calls, deterministic-only added work). **Honest gap (D-lite cap is too soft — flagged for Y4):** top hook repeat regressed to 14× ("yesterday me booked chaos for today me's calendar") vs Y2 10× — the +3 boost cap fires correctly (verified by inspecting `selectionPenalty`'s recent-id check) but `min(+7, +3) − 2 = +1` is still positive, so the capped premium hook still wins vs neutral legacy 0 (matches the spec PART 6 documented behaviour, just not strict enough to break the same-hook re-pick loop the demo creator's pool exhibits). Y4 candidates: tighten the cross-batch demote from `−2` to `−5` on a second consecutive (style OR execution) repeat producing net `min(+7, +3) − 5 = −2` (strictly demoting below neutral legacy), OR add ~15 more high-confidence mapping pairs (top unmapped: `my body quit. my brain kept screaming` / `i checked one thing. ruined my day` / `i ghosted my own to-do list`), OR inject more variation upstream so the candidate set itself doesn't keep returning to the same 5-6 winning hooks. **Architect review:** see commit message — wiring correct, lazy validation breaks the cyclic-import deadlock without losing drift detection, telemetry fires, public API strip preserved, D-lite cap is mathematically sound (just under-powered for the observed pool concentration). **Cleanup:** the temporary `POST /api/_qa/phaseY` route file (`_qaPhaseY.ts`) + its import + `router.use` line in `routes/index.ts` were removed after validation; route returns 404 post-cleanup; `pnpm --filter @workspace/api-server exec tsc --noEmit` clean. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6B / Phase 6C / Phase 6D / Phase 6E / Phase 6F / Phase 7 / Phase X / Phase X2 / Phase Y / Phase Y2 all frozen — no scoring philosophy / HookIntent catalog / pattern catalog / Phase 5 / hard-validator / catalog-removal / DefaultTaste / cache-shape changes, no DB schema changes, no premise-share gate changes, no UI changes; public response keys still exclude both `premise` and `premiseCoreId`.

- **PHASE Y6 — Single-Pass Cohesive Core-Native Author + Seed-Hook Voice Training + Scenario Fingerprint (additive overlay on PHASE Y5, NO UI/DB/schema/cache-shape/Claude-cost/public-API changes).** Layered on Y5 to REPLACE the Y5 fragment-assembly inside `coreCandidateGenerator.ts` with a SINGLE-PASS authoring function that, given a `(core, domain, anchor, voice)` recipe, emits ONE complete Idea where hook / premise / whatToShow / howToFilm / shotPlan / caption / whyItWorks all share the same anchor noun and the same contradiction beat BY CONSTRUCTION, NOT by post-hoc validation. Flip the 150 seed hooks from anti-copy hard-reject guards to voice TRAINING exemplars (Jaccard ≥ 0.85 token-bigram still hard-rejects near-verbatim duplicates so the rail isn't merely removed). Compute a deterministic `scenarioFingerprint` per candidate (telemetry-only in Y6 — Y8 will enforce per-creator semantic dedup using these fingerprints). ZERO new Claude calls (Claude stays as fallback only via the existing `needFallback` gate, untouched), ZERO DB / schema / cache-shape / public-response changes; public API still strips `premiseCoreId` (route strip site at `routes/ideator.ts` L305-308 unchanged). **`lib/voiceClusters.ts` (new module):** exports a frozen `VOICE_CLUSTERS` const with 4 voice clusters (`dry_deadpan`, `chaotic_confession`, `quiet_realization`, `overdramatic_reframe`), each with `id` / `tonalSignature` / `hookTemplates` (10 templates per cluster with `${anchor}` / `${mechanism}` / `${contradiction}` / `${action}` / `${actionPast}` / `${ingForm}` substitution placeholders) / `lengthTargetWords` (range like 4-9) / `softnessCeiling` (0-1) / `seedHookExemplars` (3-4 of the existing 150 seed hooks bucketed into each cluster as voice exemplars by manual classification — `dry_deadpan` gets "i ghosted my own to-do list", `chaotic_confession` gets "okay just one more → 47 posts later", etc.). Boot-time assert: every cluster has ≥ 8 templates and ≥ 3 seed exemplars. **`lib/coreDomainAnchorCatalog.ts` (new module):** exports `CORE_DOMAIN_ANCHORS`: a map from `coreId` → array of `{domain, anchors[], exampleAction}` rows. 12 canonical domains (work / dating / mornings / money / friends / fitness / social / home / etc.) × 5 anchors each, with all 40 PremiseCores covered via 5 canonical anchors per core (SYNONYM_MAP-padded where the raw catalog had < 5 unique entries — anchors filtered through STOPWORDS so `phone`/`camera`/`scene`/`shot`/`show*`/`see*` route to non-stopword aliases like `screen`/`lens`/`feed`). `FAMILY_ACTIONS` map declares the per-family contradiction-beat verb (`ghost`, `abandon`, `negotiate`, `lie`, `fake`, etc.). Boot-time assert: every coreId in `PREMISE_CORES` is present; no anchor list shorter than 3; ≥ 800 `(core, domain, anchor)` triples total. **`lib/scenarioFingerprint.ts` (new module):** exports `computeScenarioFingerprint({mechanism, anchor, action}): string` — lowercases, strips punctuation, lemmatizes the verb via a small suffix-stripping table (-ing → "", -ed → "", -s → "" when noun-like), normalizes anchor via a `SYNONYM_MAP` (list/checklist/tasks → "list", phone/screen/feed → "phone", gym/workout/fitness → "gym"), sorts tokens, hashes via djb2 × 2 to a 12-char hex string prefixed `sf_`. Exports `extractAnchorAndAction(idea: Idea): {anchor, action}` that probes the idea's `whatToShow` against the catalog's known anchor list. Pure / deterministic / no external deps; fingerprint stable across token-order permutations of the same scenario. **`lib/cohesiveIdeaAuthor.ts` (new module — the single-pass core):** exports `authorCohesiveIdea({core, domain, anchor, action, voice, regenerateSalt}) → {idea: Idea; meta: CandidateMeta; scenarioFingerprint: string} | null`. Deterministic given inputs (no randomness — pure salt-seeded). Construction order: pick a hook template from `voice.hookTemplates` via salt-seeded index → substitute `${anchor}` / `${mechanism}` (from `core.mechanism`) / `${contradiction}` (built from `core.tension`) → cap to `voice.lengthTargetWords` upper bound → build `premise` as `${core.tension}, but in ${domain} via ${anchor}` stitched through `core.generatorRule` → build `whatToShow` as a scene that LITERALLY contains the anchor doing/being subjected to the action verb, ending on the contradiction beat → build `howToFilm` (single-shot or two-shot, anchor referenced again) → build `shotPlan` (1-3 shots, anchor in each) → build `caption` (≤140 chars, callback to hook + reaction to whyItWorks) → build `whyItWorks` (1-2 sentences mechanism → contradiction → relatability arc) → build structural fields (`pattern` / `emotionalSpike` / `structure` / `hookStyle` / `triggerCategory` / `setting` / `templateHint` / `contentType` / `videoLengthSec` / `filmingTimeMin` / `payoffType` / `hasContrast` / `hasVisualAction` / `visualHook` / `trigger` / `reaction`) deterministically by core family → mark `usedBigPremise: true` + populate `premiseCoreId` / `premiseStyleId` / `executionId` (latter two derived from `core.compatiblePremiseStyles[0]` and the matched execution in `PREMISE_STYLE_DEFS`). Construction precondition: `whatToShow` MUST contain `anchor` AND end on the contradiction (verb from action). Both checked structurally inside the author — failure returns `null` cleanly (no throws). Run `ideaSchema.safeParse` + `validateComedy` + `validateAntiCopy` on the constructed Idea; on any failure return `null` with the rejection reason carried by caller. **`lib/coreCandidateGenerator.ts` (rewritten):** the Y5 per-core verbatim/arrow-swap/tagged 3-variant loop replaced with a recipe-iteration loop. For each selected core: enumerate compatible `(domain, anchor)` pairs from `CORE_DOMAIN_ANCHORS[coreId]`; pick `voice` deterministically by core family via `FAMILY_VOICE` map (Y6 hardcoded default cluster per family — Y7 will replace this with taste-driven selection); for each `(core, domain, anchor, voice)` recipe in salt-seeded rotation order, call `authorCohesiveIdea`; on null, advance to next recipe; on success, keep and advance to next core. Cap attempts per core at 5 recipes (was 3 in Y5). Rotation prefers anchors NOT in `noveltyContext.recentPremises` by promoting fresh anchors to front of the queue. Output shape unchanged: `{candidates: [{idea, meta}], stats: {generatedCount, keptCount, rejectionReasons, perCoreAttempts}}`. The new `scenarioFingerprint` is added to `meta` via `as unknown as CandidateMeta` cast (telemetry-only — Y8 will harden into a proper `CandidateMeta` field). New rejection reason `construction_failed` added to the `rejectionReasons` Record so the recipe-precondition failure mode surfaces in stats. **`lib/comedyValidation.ts` (`validateAntiCopy` flipped):** the previous exact-fingerprint hard-reject (`return "copied_seed_hook"` on any normalized hook fingerprint matching a seed hook fingerprint) replaced with a Jaccard token-bigram similarity check. `loadSeedHookBigrams()` lazy cache builds the bigram sets once on first call. Reject only if Jaccard ≥ 0.85 (catches "i ghosted my own to-do list" vs "i ghosted my todo list" — true near-duplicates) but allows "i ghosted my own gym routine" (same voice, fresh scenario — the whole point of voice training). The existing `near_duplicate_premise` check against `recentPremises` unchanged (recency dedup is orthogonal to seed dedup). Backwards-compat: `_seedFingerprints` param renamed but kept in the signature so existing callers don't break. JSDoc rationale block references PHASE Y6. **`lib/hybridIdeator.ts` served-log telemetry (additive):** 2 new fields appended to the existing `hybrid_ideator.served` log on the main path — `scenarioFingerprintsThisBatch` (array of `sf_*` strings, one per shipped core_native idea this batch — pulled from `c.meta` via `(c.meta as { scenarioFingerprint?: string }).scenarioFingerprint` cast) and `coreNativeAnchorsUsed` (sorted distinct anchors used by core_native candidates this batch — extracted via `extractAnchorAndAction(idea)` against the catalog's known anchor list). Cache-path served log emits zero-default copies (`scenarioFingerprintsThisBatch: []`, `coreNativeAnchorsUsed: []`, `construction_failed: 0` added to the cache-path `coreNativeRejectionReasons`) so dashboards never NPE on cache replay. **QA verification (10 batches × `count=3`, fresh non-demo creator `00000000-0000-0000-0000-000000a6e006`, all `regenerate=true`, JSON `.local/reports/qa_phase_y6.json` + report `.local/reports/phase_y6_report.md`):** **30 / 30 ideas returned**, no underfill. **50 / 50 core_native generated→kept** across all 10 batches; **ZERO rejections** across every reason (`no_contradiction`, `no_tension`, `generic_observation`, `too_soft`, `hook_scenario_mismatch`, `filming_mismatch`, `copied_seed_hook`, `near_duplicate_premise`, `schema_invalid`, `construction_failed` all = 0). **core_native selected 14 / 30 = 46.7 %** vs pattern_variation 16 / 30 = 53.3 % vs claude_fallback 0 / 30 = 0 % (Claude wraps fired in every batch via cold-start `needFallback`, but never shipped on score). **14 distinct `scenarioFingerprint`s shipped, 14 / 14 = 100 % unique** — zero collisions sets the right baseline before Y8 turns on the enforce gate. Coherence by construction hand-verified on 6 sample core_native ideas (hook anchor ↔ whatToShow anchor ↔ howToFilm anchor all literal-string-match in 6 / 6). Voice training works: 0 of 50 core_native attempts produced a hook within Jaccard ≥ 0.85 of any of the 150 seed hooks — the new templates produce fresh anchor + verb combinations that read in the cluster voice without lifting the seed string. Mean batch latency 31.9 s; p95 40.5 s — flat vs Y5. **Y6 ceiling (flagged, not a defect — Y7 follow-up):** only 4 distinct anchors exercised across this run (`calendar`, `highlighter`, `profile`, `screen`) because the recipe queue prefers fresh-from-`recentPremises` and `recentPremises` doesn't carry anchor information. Y7 will add a `recentAnchors` channel to `NoveltyContext` so the queue rotates on anchor freshness directly. Voice diversity is similarly capped because `FAMILY_VOICE` defaults most families to `dry_deadpan` — taste-calibration-driven voice rotation is also a Y7 follow-up. **Cleanup:** ephemeral `POST /api/dev/y6-qa/run` route file (`y6Qa.ts`) + its import + dev-gated `router.use` block in `routes/index.ts` removed after validation; `/tmp/y6_qa_progress.json` retained for one cycle then deleted; `pnpm --filter @workspace/api-server exec tsc --noEmit` clean. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6B / Phase 6C / Phase 6D / Phase 6E / Phase 6F / Phase 7 / Phase X / Phase X2 / Phase Y / Phase Y2 / Phase Y3 / Phase Y4 / Phase Y5 all frozen — no scoring philosophy / HookIntent catalog / pattern catalog / Phase 5 / hard-validator / catalog-removal / DefaultTaste / cache-shape changes, no DB schema changes, no premise-share gate changes, no UI changes; public response keys still exclude both `premise` and `premiseCoreId`.

- **PHASE Y5 — SAFE PARALLEL Deterministic Core-Native Candidate Generator (additive, surgical, NO UI/DB/schema/cache-shape/Claude-cost changes).** Layered on PHASE Y4 to lift shipped `premiseCoreId` coverage from the Y4 baseline of 17 % (Y3 was 40 %, but cap-driven; Y4 was 17 % under cap exhaustion) to **90 % (27 / 30 ideas)** by adding a NEW deterministic local candidate generator that turns selected `PremiseCore` rows directly into Layer-1 candidates competing alongside the existing `pattern_variation` pool. ZERO replacement of the existing local generator (`pattern_variation` candidates still flow normally), ZERO Claude / cost increase (no LLM calls added — generator is fully deterministic), ZERO API / DB / cache-shape / schema / public-response changes; public API still strips `premiseCoreId` (route strip site at `routes/ideator.ts` L305-308 unchanged); Y2 fallback path's own `selectPremiseCores` call inside `needFallback` untouched. **`ideaScorer.ts` `CandidateMeta` widening:** the non-`PatternMeta` arm's `source` union widened from `"llama_3_1" | "claude_fallback"` to `"llama_3_1" | "claude_fallback" | "core_native"`. Verified every existing `meta.source ===` / `meta.source !==` site (`tryRewrite` L1382 → not pattern_variation = not rewritten ✓, sort tiebreak L1892-93 → loses ties to pattern_variation ✓, `validateComedy` L292 → vacuous-passes when usedBigPremise=true ✓, `validateAntiCopy` L387 → still subject to seed-fingerprint check but core.examples are NOT in the seed corpus only `PREMISE_STYLE_DEFS[*].executions[*].example` is ✓, `toCacheEntries` L2639 → unchanged shape ✓) — no new branch needed. **`lib/coreCandidateGenerator.ts` (new module, ~340 lines):** exports a pure deterministic `generateCoreCandidates({ cores, count, noveltyContext, regenerateSalt, recentPremises })`. djb2-seeded per-core RNG; for each core, picks `domain` from `core.compatibleDomains`, `premiseStyleId` from `core.compatiblePremiseStyles` preferring not in `noveltyContext.recentPremiseStyleIds`, `executionId` from `PREMISE_STYLE_DEFS[styleId].executions` preferring not in `noveltyContext.recentExecutionIds`, hook from a mutation of `core.examples[saltIdx]` (3 variants: verbatim → arrow-swap → tagged), premise from `core.tension` + `core.mechanism` + chosen domain (≥1 token shared with hook), `whatToShow` anchored to ≥2 hook/trigger tokens AND ≥1 howToFilm token (matching the comedy validator's `tokensOf` + STOPWORDS local mirror), then full Idea required fields including `pattern` mapped from execution shape, `emotionalSpike` mapped from core family, `usedBigPremise: true`, `premiseCoreId` populated, `premise` short and core-anchored. Validated via `ideaSchema.safeParse` (cheap structural) + `validateComedy({source:"core_native", usedBigPremise:true})` + `validateAntiCopy(..., seedFingerprints, recentPremises)`. On any failure, retries the next of 3 hook variants (verbatim→arrow→tagged) before dropping the core. Returns `{candidates, stats: {generatedCount, keptCount, rejectionReasons, perCoreAttempts}}`. Meta shape per kept candidate: `{source: "core_native", usedBigPremise: true, premiseCoreId, premiseStyleId, executionId, scenarioFamily: undefined}` — the undefined `scenarioFamily` mirrors the fallback discipline (structurally a no-op for `applyExclusion`'s family check). **`hybridIdeator.ts` wiring (additive):** new import L101; immediately after `localCandidates = applyExclusion(rawCandidates, exclude)` (L3219), calls `selectPremiseCores({count: desiredCount + 2, recentCoreIds: recentPremiseCoreIdsForSeeding, recentMechanisms: recentMechanismsForSeeding, rng: Math.random})` then `generateCoreCandidates({cores, count: desiredCount + 2, noveltyContext, regenerateSalt, recentPremises})`, emits a `phase_y5.core_native_generated` info log with `{desiredCount, seedCoreCount, generatedCount, keptCount, rejectedCount, rejectionReasons}`, builds `mergedLocalCandidates = [...localCandidates, ...coreNativeResult.candidates]`, and passes the merged pool to `filterAndRescore`. Y2 fallback path untouched. **`hybridIdeator.ts` telemetry on existing `hybrid_ideator.served` log:** 8 new fields appended (no new event) — `coreNativeGeneratedCount`, `coreNativeKeptCount`, `coreNativeSelectedCount`, `coreNativeSelectedRate`, `localSelectedCount` (pattern_variation winners), `topSelectedSources` (top-3 source histogram of `final[]`), `selectedPremiseCoreIds` (truthy `meta.premiseCoreId`s in `final`), `coreNativeRejectionReasons`. Cache-path served log (L3030+) emits zero-default copies of all 8 keys so dashboards never NPE on cache replay. **QA verification (10 batches × count=3, fresh non-demo creator `00000000-0000-0000-0000-000000a2e0a5`, all `regenerate=true`, JSON `.local/reports/qa_phase_y5.json` + report `.local/reports/phase_y5_report.md`):** **27/30 = 90.0 % `premiseCoreId` coverage** (vs Y4 17 %, Y3 40 %, Y2 8.8 %). **`core_native` selected 19/30 = 63.3 %** vs `pattern_variation` 8/30 = 26.7 % vs `claude_fallback` 3/30 = 10.0 % (batch #7 only). **50/50 generated→kept across all 10 batches; ZERO rejections** (`no_contradiction`, `no_tension`, `generic_observation`, `too_soft`, `hook_scenario_mismatch`, `filming_mismatch`, `copied_seed_hook`, `near_duplicate_premise`, `schema_invalid` all = 0 throughout). **21 distinct `premiseCoreId`s shipped** across 27 tagged ideas — 3.5× lift over Y4's 6 distinct cores; all 8 core families represented. **Top hook repeat = 2** (best of the Y-series; vs Y4 6, Y3 14, Y2 10). Mean batch latency 38.5s, p95 47.1s — flat vs Y4 local-only path; <1ms generator overhead per batch. Y4 selected-set repeat counters (`selectedSameStyleExecutionCount`, `selectedSameStyleExecutionCoreCount`, etc.) continue to fire correctly under the merged pool — the new candidates carry their own `premiseStyleId`/`executionId`/`premiseCoreId` so cross-batch demote levers operate on them. **Risks documented in report:** STOPWORDS list duplicated between `coreCandidateGenerator.ts` and `comedyValidation.ts` (recommend exporting from validator long-term); 3-variant hook pool is thin if catalog ever ships a `core.examples[*]` colliding with a `PREMISE_STYLE_DEFS[*].executions[*].example`; recommend monitoring `coreNativeSelectedRate` and alerting if rolling 10-batch >80 % (would indicate pattern catalog decay, not core-native over-strength). **Cleanup:** ephemeral `POST /api/_y5_qa/run` + `GET /api/_y5_qa/status` route file (`y5Qa.ts`) + its import + dev-gated `router.use` block in `routes/index.ts` removed after validation; `/tmp/y5_qa_progress.json` deleted; `pnpm --filter @workspace/api-server exec tsc --noEmit` clean. Phase 3 / 3B / 3C / 3D / Phase 4 / Phase 5 / Phase 6 / Phase 6 EXPANSION / Phase 6B / Phase 6C / Phase 6D / Phase 6E / Phase 6F / Phase 7 / Phase X / Phase X2 / Phase Y / Phase Y2 / Phase Y3 / Phase Y4 all frozen — no scoring philosophy / HookIntent catalog / pattern catalog / Phase 5 / hard-validator / catalog-removal / DefaultTaste / cache-shape changes, no DB schema changes, no premise-share gate changes, no UI changes; public response keys still exclude both `premise` and `premiseCoreId`.

# External Dependencies

- **Expo (React Native):** Mobile application framework.
- **Express:** Web application framework.
- **Replit Postgres:** Database hosting.
- **Drizzle ORM:** Object-Relational Mapper.
- **Clerk:** Authentication service.
- **Claude Haiku 4.5 (via Replit AI Integrations):** Large Language Model.
- **Zod:** Schema declaration and validation.
- **Orval:** OpenAPI client code generator.
- **esbuild:** JavaScript bundler.
- **pino:** Structured logging library.